# CHANGELOG

## [0.39.2] - 2026-08-03

0.39.1 で入った回帰（クリアしても `<select>` の選択が戻る）を解消した版です。あわせて、この種の不具合が単体テストをすり抜けていた原因に対処し、実ブラウザのテストを CI で実行するようにしました。

### Fixed

- **`data-{event}-reset` / `-reset-before` でクリアしても、候補を `data-each` で流し込んだ `<select>` だけがクリア前の値へ戻る問題を修正した**（0.39.1 の回帰。検索条件のクリアが効かず、そのまま検索するとクリア前の条件で送信される）。バインドデータの更新は「`data-bind` 属性の反映 → 入力欄への書き戻し → 再評価（行生成）→ 載らなかった書き込みの載せ直し」を非同期に行うため、呼び出しの後もしばらく DOM を書き換え続ける。入力欄を離れた直後にクリアを押すと、その `change` による双方向コミットがまだ走っており、後段の書き戻しがリセットの途中へ割り込んでいた。リセットは開始時に通番を発番して対象部分木の各要素へ記録し、それより前に始まったバインドデータ更新については入力欄への書き戻しを行わないようにした。
  - 同じフォームのテキスト入力が正しくクリアされていたのは、割り込みの書き戻しの後に `form.reset()` が走る順序に助けられていたためで、`<select>` だけの問題ではなく順序依存の競合だった。0.39.1 で入った「候補が揃った時点での載せ直し」が、この競合を `<select>` で顕在化させていた。
  - 判定は宛先ごとに行い、自フォームへの書き戻し（`Form.syncValues()`）、祖先の更新から `data-form-arg` フォームへの流し込み（`Form.syncAncestorArgForms()`）、候補が揃った後の載せ直し（`ElementFragment.retryUnappliedValueWrites()`）の 3 経路すべてを対象とした。
  - リセットの後に始まった更新（フェッチ応答の反映など）は従来どおり反映される。リセット自身が行うデータの復元・再評価も影響を受けない。
  - 内部値のクリアで、載せ直し待ちの書き込みも併せて破棄するようにした。
- **`data-form-list` の行で 1 か所を編集して確定すると、入力欄に対応しない行のフィールドが失われる問題を修正した**（0.39.1 以前から。バージョン非依存）。双方向コミットは収集値でバインドデータを置き換えるが、収集値は入力欄が表す部分だけなので、行データの表示専用ラベル・`data-attr-value` へ渡す送信値・保存に要る `id` などが消えていた。実害は「行のラベルが編集のたびに空になる」「チェックボックスの送信値が既定の `on` に戻る」など。`data-form-arg` で定めている「土台に収集値を重ねる」規則を、収集値の重ね合わせ全体（`Form.mergeCollectedValues()`）へ広げた。
  - 行（配列）は**出現順**で対応させる。入力欄への書き戻しが同じ規則なので、収集 → 重ね合わせ → 書き戻しで対応がずれない。要素数は収集値に従うため、行の追加・削除もそのまま反映される。
  - 双方向コミット（`change` / `input`）、`Core.changeValue()`、リセット後のコミットの 3 経路へ適用した。
- **`localStorage` / `sessionStorage` の参照が例外になる環境で、画面上のすべての `{{...}}` が評価できなくなる問題を修正した**（0.39.1 以前から。バージョン非依存。`data-bind` の既定値まで空になる）。式評価は「バインド値に危険なホストオブジェクトが紛れ込んでいないか」を照合するため、その一覧を組み立てる際に両ストレージを読んでいたが、参照を try で囲んでいなかった。ここは全式評価の共通経路（`Expression.evaluateDetailed()`）のため、例外がそのまま各評価へ伝わっていた。読めない値は「その環境では入手できない値＝照合する必要も無い値」として候補から外すようにした。
  - `allow-same-origin` の無い `sandbox` iframe や、サイトデータをブロックした状態のクロスサイト iframe では、両ストレージは参照しただけで `SecurityError` になる。
  - `data-store` は同じ状況を以前から正しく処理しており（種別ごとに一度警告して保存・復元だけを止める）、影響は式評価に限られていた。
- **一度も編集していない `data-form-arg` フォームをリセットすると、祖先が所有するレコードへ戻らず空になる問題を修正した**（0.39.1 以前から。バージョン非依存）。編集済みのフォームは、双方向コミットが作ったフォーム自身のコピーを消す過程で祖先のレコードが書き戻されるが、コピーが無いフォームには戻す契機が無かった。再評価の後に、祖先のレコードを入力欄へ流し込み直すようにした。
  - 仕様書が掲げる「リセットは祖先が所有するレコードの内容へ戻す。空にはならない」が、編集していない場合にだけ満たされていなかった。
  - 再評価の後に行うため、候補を `data-each` で流し込む `<select>` でも選択が戻る（前に行うと該当する `<option>` がまだ無く、代入が無視される）。

### Changed

- CI で実ブラウザのテスト（Playwright / Chromium）を実行するようにした。あわせて `npm run test:e2e` を追加した。今回の回帰は単体テスト（jsdom）では再現せず実ブラウザでのみ失敗しており、描画タイミングに依存する不具合を公開前に捕まえるため。
- Playwright の webServer が、CI では直前のビルド成果物をそのまま配信するようにした（`CI` 環境変数で切り替え）。ローカルでは従来どおりビルドしてから配信する。

### Docs

- 技術仕様書の `data-{event}-reset` に、進行中のバインドデータ更新の書き戻しを無効化する手順を追加し、リセットの手順を 6 段階に整理した。通番で判定する 3 つの経路と、候補を `data-each` で流し込む `<select>` で問題が現れる理由も記載した。
- 技術仕様書の「収集は DOM を真とする」に、載せ直しがリセットをまたがないことを追記した。
- 技術仕様書の「祖先所有レコードの流し込み」に、リセットが編集の有無によらず祖先のレコードへ戻すことを明記した。

### Tests

- `tests/reset-late-option-select.test.ts` を追加（7 件、うち 1 件が修正前に失敗）。双方向コミットが進行中でもクリアが効くこと、候補は残したまま選択だけが既定値へ戻ること、テキスト入力も空へ戻ること、リセットの後に始まった更新（自フォーム宛・祖先宛）は従来どおり入力欄へ反映されることを検証する。
- `playwright/reset-late-option-select.spec.cjs` と repro ページを追加（2 件、いずれも修正前に失敗）。実ブラウザでクリア後の `<select>` の表示と、続く再取得の送信クエリが既定値であることを確認する。
- `tests/reset-each-render.test.ts` に、`reset-before` の後の `data-{event}-bind` が入力欄へ入ることの検証を追加した。書き戻しの抑止が正常な供給まで止めないことを固定する。
- `tests/form-arg-ancestor-scope.test.ts` に「編集していないフォームのリセットでも祖先のレコードへ戻る」を追加した（修正前に失敗）。
- **回帰防止のためテストを拡充した**（1251 → 1365 件、実ブラウザ 194 → 195 件）。これまでの回帰はいずれも「画面の値・収集値・バインドデータの三者が食い違う」同じ形で現れており、個別の再現テストだけでは次の組み合わせを守れないため、組み合わせを一覧にした不変条件のテストを置いた。
  - `tests/value-roundtrip-matrix.test.ts`（44 件）: 入力種別 11 種（text / number / textarea / hidden / boolean チェックボックス / チェックボックス群 / `data-attr-value` のチェックボックス群 / ラジオ群 / 単一 select / 複数 select / 候補を `data-each` で流し込む select）× 供給経路 4 つ（初期 `data-bind` / `setBindingData` / 利用者の操作 / リセット）で三者の一致を検証する。
  - `tests/row-value-roundtrip.test.ts`（10 件）: `data-each` と `data-form-list` の行について、行の追加・削除・行内編集・リセットを挟んでも一致が保たれることを検証する。
  - `tests/error-path-resilience.test.ts`（14 件）: 不正なセレクタ、失敗した描画タスク、危険な戻り先パスなど、異常な入力で全体が止まらないことを検証する。
  - `tests/visible_range.test.ts` に登録の同期と解除の 7 件を追加した（分岐網羅 66.7% → 81.9%）。
  - `tests/observer.test.ts` に DOM 変更の取り込み 4 件と、監視の異常系 9 件を追加した（文 77.4% → 99.4%、分岐 55.9% → 95.3%）。初期スキャンの失敗、`data-external` 配下・クリック抑止中の `disabled`・`data-attr-*` の実属性反映といった除外経路、取り込み中の例外、`MutationObserver` では作れない種別の記録の読み飛ばしを検証する。
  - `tests/expression-storage-unavailable.test.ts` を追加（4 件、いずれも修正前に失敗）。両ストレージの参照が例外になる環境でも、式の評価・`data-bind` の既定値の描画・危険値の持ち込み拒否が従来どおり動くことを検証する。
  - `playwright/rapid-checkbox-group.spec.cjs` を追加した。同名チェックボックス群を間隔なしで 20 回連続クリックし、画面のチェック状態と送信値が一致することを実ブラウザで確認する。
  - 網羅が手薄だった機能のテストを追加した。`tests/intersect.test.ts` に登録の判定と解除の 8 件（分岐 76.7% → 92.0%）、`tests/poll.test.ts` に設定値の解釈と監視解除の 6 件（82.0% → 85.4%）、`tests/auth_guard.test.ts` に URL 解決の 5 件（81.8% → 89.7%）、`tests/store.test.ts` に破棄失敗時とストレージが使えない環境の 3 件（83.8% → 87.2%）を追加した。
- `npm run test:coverage` を追加した（`@vitest/coverage-v8` / MIT を devDependency に追加）。網羅率を測ってから拡充先を決められるようにするため。文 89.4% → **90.6%**、分岐 89.0% → **90.2%**。

## [0.39.1] - 2026-08-03

入力値が黙って失われる経路をまとめて塞いだ版です。0.39.0 で入った回帰（続けて確定した入力が消える）を解消し、あわせて以前から残っていた 4 件（同名チェックボックス群のチェック落ち、条件評価のスコープ、候補が後から届く `<select>` の表示、反映待ちの間の値の取りこぼし）を修正しています。

### Fixed

- **`data-validity` があるフォームで、間隔を空けずに続けて確定した入力の値が消える問題を修正した**（0.39.0 の回帰。値は表示・収集値・保存値のすべてから失われ、`required` の欄なら以後の送信がブロックされる）。0.39.0 で「収集の直前に DOM の値を内部値へ取り込む」ようにしたが、収集は**バインドデータへ反映しない目的でも走る**（`data-validity` / `data-{event}-if` の条件評価は、バインド更新のたびに収集値でスコープを組み立てる）。そこで内部値を書き換えると「バインドデータには載っていないのに内部値だけが新しい」状態が生まれ、続く逆方向同期が古いバインドデータと不一致とみなして入力欄を上書きしていた。収集を読み取りに徹する形へ改め、内部値は書き換えないようにした（`ElementFragment.syncValueFromDom()` を `getValueForCollection()` へ置き換え）。
  - 収集が DOM を真とすることは変わらない（0.39.0 で解消した「外部ライブラリが代入した値の欠落」はそのまま）。DOM の値が内部値・バインドデータへ入るのは、収集結果がバインドへコミットされ、そこから書き戻される経路だけになる。
  - 消えるのは `data-validity` を宣言した欄とは限らず、同じフォームの無関係な欄。宣言があること自体が引き金で、式の内容や検証の実行有無には依存しない。
  - 人手の入力速度では踏みにくく、`fill()` のように間隔を置かずに確定を続けると表面化する。
- **`data-each` の行に置いた同名チェックボックス群で、2 つ目をチェックすると全部のチェックが落ちる問題を修正した**（0.37.1 以前から。バージョン非依存。画面上はチェックが付いているのに送信値が空になる）。チェック状態を決める送信値をフラグメントの属性マップから引いていたため、`data-attr-value` で値を与えた構成では属性マップに `value` が無く `null` になり、収集値（配列）と比較して常に不一致になっていた。`value` 属性の宣言が無い場合は DOM の `value`（= 実際の送信値）を使うようにした。
  - 1 つだけチェックした場合は収集値がスカラで内部値と一致し、書き戻し自体が起きない。そのため 2 つ目のチェックで初めて表面化していた。
  - 併せて、`value` 属性を持たないチェックボックス（送信値は HTML 既定の `"on"`）にも、バインドデータからチェック状態が反映されるようになった。
  - `data-each` の行に置いたラジオでも、`data-attr-value` で送信値を与えた場合に保存済みレコードの選択が復元されるようになった。
- **候補を `data-each` で流し込む `<select>` で、供給された値が画面に載らない問題を修正した**（保存済みの値が画面では別の候補に見え、送信内容と食い違う）。入力欄への書き戻しは行生成より前に走るため、代入した時点では該当する `<option>` がまだ無く、`<select>` は候補に無い値の代入を無視する。ブラウザが先頭の候補を自動選択するため、画面だけが別の値になっていた（0.39.0 では収集値を失わないことだけを担保していた）。候補が揃った時点（行生成の直後）で、DOM が受け付けられなかった書き込みを再試行するようにした。
  - 保存済みレコードと候補が同じ応答で届く構成（`data-fetch` の応答を `data-{event}-bind` で流し込む画面など）で、選択が正しく復元される。
  - 候補にその値が無い場合は書き込まない（現在の選択を消さないため）。収集値だけが供給された値を保つのは従来どおり。
  - 再試行の対象は `<select>` だけ。テキスト系の入力は同じ値を書き直しても結果が変わらない。
- **反映待ちの間に来た値の反映が黙って捨てられる問題を修正した**（潜在。同一フレームに 2 回反映が要求されると、後から来た値が画面にもバインドにも載らないまま失われる）。描画キュー待ちの間の要求を捨てず、先の書き込みの完了を待って改めて反映するようにした（後勝ち）。
  - あわせて、**反映待ちの間に利用者が確定した入力を、待っていた書き込みが上書きしないようにした**。反映を要求した時点より後の編集は「編集済みの印」と同じ理由で保護する（要求より前の編集は、明示的な供給が権威なので従来どおり上書きする）。チェック状態も同じ扱い。
- **フォームコンテナ自身に `data-form-object` / `data-form-list` を宣言すると、`data-validity` / `data-{event}-if` の条件が収集値ではなくバインドデータで評価される問題を修正した**。宣言キーの列挙が走査の起点（フォームコンテナ自身）の宣言を見ておらず、宣言キーは配下の `name`、収集値は入れ子という食い違いが起きていた。起点自身の宣言も見るようにした。
  - バインドデータへのコミットは非同期のため、**最後の欄を直してそのまま「次へ」を押す操作で 1 手前の値により誤判定**していた（不正な値の見逃し、または直した直後の誤ブロック）。仕様書が掲げる「クリック時点で最新なのは収集値」がこの構成でだけ効いていなかった。
  - 副次的に、配下の `name` が最上位の宣言キーとして扱われ、同名のバインドキーを未定義で隠すことがなくなった。
  - 入れ子の `data-form-object`（`<div data-form-object="…">`）、`data-form-arg`、宣言の無いフォームは従来どおり。

### Docs

- 技術仕様書の「収集は DOM を真とする」に「収集は読み取りに徹し、内部値は書き換えません」を追加した（バインドへ反映しない収集があること、書き換えると逆方向同期が古い値で上書きすること）。
- 技術仕様書の「初期 `data-bind` からの入力欄復元」に、チェック状態が送信値との一致で決まること、送信値は `value` 属性の宣言が無ければ DOM の `value`（既定は `"on"`）であることを追記した。
- 技術仕様書の「条件の評価スコープ」に、置き換えの単位が最上位の収集キーであること、宣言をフォームコンテナ自身へ書いた場合も同じであることを追記した。
- 技術仕様書の「収集は DOM を真とする」に、候補が揃った時点で書き込みを再試行すること、候補に無い値は書き込まないことを追記した。
- 技術仕様書の「ユーザー編集と宣言バインドの権威」に「反映待ちの間に起きた変化」を追加した（後勝ちの反映と、待機中の利用者編集の保護）。

### Tests

- `tests/collection-read-only.test.ts` を追加（3 件、いずれも修正前に失敗）。収集が内部値を書き換えないこと、収集の後に古いバインドデータで同期しても入力値が消えないこと、`data-validity` の条件評価が収集値（画面の値）を見ることを検証する。
- `tests/each-checkbox-group-value.test.ts` を追加（3 件、いずれも修正前に失敗）。`data-each` の行の同名チェックボックスを 2 つ選んでも落ちないこと、行のラジオへバインドデータの選択が反映されること、`value` を持たないチェックボックスへも反映されることを検証する。
- `playwright/consecutive-commit.spec.cjs` と repro ページを追加（1 件、修正前に失敗）。実ブラウザで 20 回連続、間隔を空けずに 5 欄を確定し、表示と送信値の双方が入力どおりであることを確認する。人手より速い入力で踏む不具合のため実ブラウザで押さえる。
- `playwright/row-checkbox-group.spec.cjs` と repro ページを追加（1 件、修正前に失敗）。実ブラウザで 2 つチェックした後もチェックが残り、送信値が `optionIds=11&optionIds=12` になることを確認する。表示より先に収集値が壊れるため、送信値でも確かめる。
- `tests/late-option-select-render.test.ts` を追加（4 件、うち 2 件は修正前に失敗）。値と候補を同じ更新で渡した場合と、候補が後の更新で届いた場合に画面へ値が載ること、利用者が選び直した後は巻き戻さないこと、候補に無い値は画面へ載せずに収集値を保つことを検証する。
- `tests/pending-value-write.test.ts` を追加（4 件、いずれも修正前に失敗）。反映待ちの間に来た値が後勝ちで載ること（2 回・3 回）、待機中の利用者編集を巻き戻さないこと（値とチェック状態）を検証する。
- `playwright/late-option-select.spec.cjs` と repro ページを追加（1 件、修正前に失敗）。実ブラウザで、応答で届いた保存済みの値が `<select>` の表示と送信値の双方に載ることを確認する。候補に無い値の代入時の挙動はブラウザ依存のため実ブラウザでも押さえる。
- `tests/condition-scope-container-key.test.ts` を追加（6 件、うち 3 件は修正前に失敗）。バインドデータと画面の値をずらしたうえで、フォームコンテナ自身の `data-form-object` / `data-form-list` でも収集値で条件が評価されること、入れ子の宣言・`data-form-arg`・宣言の無いフォームが従来どおりであること（回帰ガード）、配下の `name` が同名のバインドキーを隠さないことを検証する。

## [0.39.0] - 2026-08-02

宣言だけで完結させるための 3 点を解消した版です。外部ライブラリが書き込んだ入力値が保存されるようになり（画面へ JavaScript を戻さずに住所補完を使えます）、`data-import` で取り込んだ断片が取り込み側のバインドスコープを継承するようになりました。あわせて開発モードの誤警告を 1 件解消しています。

### Fixed

- **外部ライブラリがスクリプトから代入した入力値が収集されない問題を修正した**（保存値・送信データの欠落）。値収集は内部値（`ElementFragment` が保持する値）を読んでおり、その更新契機は `change` / `input` の委譲と宣言バインドの評価だけだった。外部ライブラリ（郵便番号からの住所補完など）やブラウザの自動入力は `element.value` へ直接代入してイベントを発火しないため、**画面には表示され `required` の検証も通るのに、収集値・保存値には載らない**欄ができていた（他の欄を編集して収集が走っても載らない）。収集の直前に DOM の値を内部値へ取り込むようにした（`ElementFragment.syncValueFromDom()`）。チェック状態（checkbox / radio）と `input[type=file]` がすでに DOM を真として収集しているのと扱いを揃え、値も画面の見たままを収集する。
  - `data-enhance-new="YubinBango.MicroformatDom"` で補完した都道府県・市区町村・町域が、そのまま送信・保存されるようになる。宣言だけで連携が完結する。
  - 取り込みは収集の契機（`change` の双方向コミット、`data-{event}-form` での送信など）で行う。代入した瞬間にバインドデータへ反映されるわけではない。
  - 外部からの代入は**ユーザー編集としては扱わない**（通し番号を発番しない）。代入の時点を観測できないため。送信中に書き込まれた値は応答の書き戻しで上書きされることがある。
  - 併せて、同名グループの checkbox / radio でチェック済みの値も DOM の `value` から収集するようにした。従来は内部値を使っていたため、イベントを伴わずにチェックされた要素の値が `null` として収集されていた（「チェック状態は DOM を真とする」という仕様の記述と食い違っていた）。
  - 副作用として、静的な `data-attr-value`（`{{式}}` を含まない値）で入った値も収集対象になる。
  - 次の 2 つの場合は DOM を取り込まない。DOM が内部値より古い、または内部値を表現できない状態で取り込むと供給された値を失うため。(1) Haori 自身の書き戻しが描画キュー待ちの間。(2) 直近の書き込みを DOM が受け付けなかった場合（`<select>` は該当する `<option>` が無いと代入が無視される。候補を `data-each` で流し込む構成では、入力欄への書き戻しが行生成より前に走るため起こり得る）。(2) を判定するため、書き込み結果を `ElementFragment` に記録するようにした。
- **`data-import` で取り込んだ断片が、取り込み側のバインドスコープを継承しない問題を修正した**。`data-import` は `innerHTML` で子を差し替えるが、フラグメント木がそれに追従していなかった。断片の要素は親を持たないフラグメントとして扱われ、祖先をたどれないため**評価スコープが空**になっていた（`{{...}}` が空表示になり、`data-if` が常に偽で非表示になる）。取り込み先要素自身に `data-bind` を書いても参照できず、断片へ値を渡す手段が無かった。差し込んだ子を取り込み先の子としてフラグメント木へ繋ぐようにした。取り込んだ断片は通常の子要素と同じスコープ解決になり、「共通マークアップ＋画面ごとの差分」を 1 ファイルへ集約できる。
  - あわせて、差し替えで DOM から外れた子をフラグメント木から落とすようにした。残していたため、再取り込みのたびに存在しないノードが積み上がり、以降の再評価がそれをたどっていた。
- **開発モードの診断が、行の中の式が参照する `data-each-arg` / `data-each-index` のキーを「別のスコープでは供給されているキー」と誤警告する問題を修正した**（0.36.0 で追加した診断。取得も評価も正しく、警告のみ）。`data-each` はコンテナが未マウント（または非表示）のあいだ差分更新を待つため、その間はテンプレートが切り出されず、行のマークアップが**コンテナのスコープ**で評価される。行スコープの名前はそこには無く、行が描画された後に供給されるため、「別のスコープでは供給されている」という報告条件を満たしてしまっていた。行スコープの名前はバインド先の取り違えでは供給されない名前のため、診断の対象から外した。
  - 行の中で `data-each-arg` のキーから `data-fetch` の URL を組み立てる構成（`data-fetch="{{c.optionsAgreed ? '…' : null}}"` のように、条件が成立するまで取得しない書き方）で警告が出ていた。
  - 報告条件は「その名前が別のスコープで一度でも供給されている」ことなので、同じ引数名を使う一覧が先に描画されている画面で表面化する。

### Changed

- **`data-import` の断片の評価を、初期化の前後で同じ経路にした**（上の修正に伴う堅牢化）。従来は初期化完了後（`data-haori-ready` 付与後）だけ監視（`Core.addNode`）へ委ねていたが、この経路は同じ変更通知で追加された 2 つ目以降のノードを取りこぼす（`insertBefore` が挿入のあいだ立てる抑止フラグに、同じ通知の後続の `addNode` が掛かるため）。取りこぼしが表面化するかどうかは監視の通知順序に依存し、実ブラウザでは通る一方で jsdom では断片が `{{...}}` のまま評価されなかった。取り込み側で常に走査するようにして依存を断ち、繋ぎ済みの子は `addNode` 側で除外するようにした。

### Docs

- 技術仕様書の「チェック状態の収集は DOM を真とする」を「収集は DOM を真とする」へ改め、値を持つ入力も対象になったこと、イベントを伴わない代入に追随すること、取り込みを見送る 2 つの場合、外部からの代入をユーザー編集として扱わないことを追記した。
- 技術仕様書の「外部ライブラリ連携」に「外部ライブラリが書き込んだ入力値」を追加した（住所補完の最小例と、反映の契機が収集時であること）。
- 利用ガイドの `data-enhance-new` の例に、補完された値がそのまま収集・送信・保存されることを追記した。
- `demo/enhance/data-enhance-demo.html` に「外部ライブラリが書き込んだ値も収集される」を追加した。郵便番号の入力で住所欄へ直接代入し（`keyup` のみ。`change` は発火しない）、フォーカスを外すと収集された値が表示に反映される。
- 技術仕様書の `type="number"` の説明から、`data-attr-value` は内部値を同期しないという記述を削除した（実装では `value` の宣言バインドとして同期・正規化される）。
- 技術仕様書の「条件の評価スコープ」の注記を、収集が DOM を真とすることに合わせて改めた（外部ライブラリの代入も含めて収集値が最新になる）。
- 技術仕様書の `data-import` に「評価スコープ」を追加した（取り込んだ断片は通常の子要素と同じスコープ解決になること、取り込み先要素自身の `data-bind` も参照できること、断片の中で宣言した場合の扱い、未解決参照になる場合）。
- 利用ガイドの `data-import` に「共通マークアップ＋画面ごとの差分」の実用例（ウィザードのステップインジケータ）を追加した。
- `demo/import/data-import-demo.html` と `demo/import/components/header.html` を、取り込み側のスコープを参照する構成へ更新した。断片の中は `data-if` で示している（断片は単体でも開けるようにテキストへ `{{...}}` を書かない。単体では本体を読み込まないため未展開のまま見えてしまう）。
- 技術仕様書の「未解決参照の診断」に、行スコープの名前を報告しないことと、その理由（行の描画より前のテンプレート評価では必ずスコープ外になる）を追記した。

### Tests

- `tests/external-value-collection.test.ts` を追加（10 件、うち 8 件は修正前に失敗）。スクリプトからの代入（`<input>` / `<select>` / `<textarea>` / `type="hidden"`）、値を空へ戻した場合、`data-form-list` の行の中、`type="number"` の数値化、`<select multiple>` の配列収集、双方向バインディングでバインドデータへ載ること、チェック状態の収集（回帰）を検証する。残る 2 件は非退行のガードで、書き戻しが描画キュー待ちの間に DOM の古い値を取り込まないこと、`<option>` が未描画の `<select>` へ供給された値を失わないことを確認する（どちらも修正前後で通る）。
- `playwright/external-value-collection.spec.cjs` と repro ページを追加（2 件、いずれも修正前に失敗）。実ブラウザで、`data-enhance-new` から適用した住所補完ライブラリ（`keyup` で入力欄へ直接代入する代役）が書き込んだ値が、そのまま送信ボディへ載ること、および他の欄の確定でバインドデータへ載ることを確認する。
- `playwright/enhance.spec.cjs` に 1 件追加。デモの住所補完でも、代入された値が収集されて表示へ反映されることを確認する。
- `tests/import-scope.test.ts` を追加（15 件、うち 14 件は修正前に失敗）。祖先・取り込み先要素自身の `data-bind` の継承、断片の中の `data-if` の評価（真・偽の両方）、入れ子の取り込み、`data-each` の行の中での行スコープの参照、取り込み後のバインド更新が断片へ届くこと、断片の中の `data-bind` がそこから下だけのスコープになること、断片の中の `data-each` / `data-fetch` が取り込み側の値で動くこと、断片の中の入力欄が外側のフォームの収集対象になること、空の断片・取り込み失敗・再取り込みでフラグメント木が壊れないことを検証する。
- `tests/import-scope-ready.test.ts` を追加（1 件、修正前に失敗）。初期化後に URL が確定した取り込みも評価されることを、実ページと同じ入口（`Observer` の初期化 → 走査 → ready → 監視開始）で検証する。`src/observer` は読み込みだけで初期化が走るためファイルを分けている。
- `playwright/each-done-stress.spec.cjs` と repro ページを追加（2 件）。同一バインドホストの配下に `data-each` が複数あるとき、描画完了マーカー（`data-each-done`）が確定しないことがあるという報告（0.24.0 で観測、以降は未再検証）の回帰ガード。実ブラウザで 20 回連続の再バインドと、確定を待たない重ね合わせ（タブ切り替えを含む）を実行し、その回のデータが描画されたうえで両方のマーカーが付くこと、JS エラーと `console.error` が出ないことを確認する。0.38.0 では jsdom・実ブラウザとも再現しない。
- `playwright/import-scope.spec.cjs` と repro ページを追加（3 件）。実ブラウザで、初期読み込みの継承（祖先・取り込み先自身の両方、`data-if` の真偽を含む）、取り込み後のバインド更新が断片へ届くこと、初期化後に URL が確定した取り込みの評価を確認する。監視の通知順序は実ブラウザでしか確かめられないため、jsdom と両方で押さえている。
- `tests/row-scope-identifier-diagnostics.test.ts` を追加（9 件、うち 4 件は修正前に失敗）。未マウント走査を経ても行スコープの名前を報告しないこと、その構成で行と行の中の `data-fetch` が正しく評価されること、引数名とインデックス名の両方を登録すること、接頭辞を変えたページと引数名を式で書いた構成でも外れること、同じ名前を使う別の式も報告しないこと、応答のバインド先を取り違えた宣言は従来どおり報告すること、本番（開発モード無効）では記録しないことを検証する。

## [0.38.0] - 2026-08-01

仕様書に記載がありながら実装や公開物が追いついていなかった 5 点と、不具合 4 件を解消した版です。不具合のうち 2 件は報告によるもの、2 件は同じ原因を追ううちに見つけた「編集可能な行への書き戻し」の隣接不具合です。

### Fixed

- **行への `data-{event}-copy` が、同じ手続きの起点になった入力の編集値を巻き戻す問題を修正した**（入力の消失）。行への書き戻しは配列を読み直してから行うが、`change` を起点にした手続きでは、起点になった入力の編集がまだ要素データへ確定していないことがある（収集の宣言を持つ `<form>` が外側に無い構成では確定の機会そのものが無い）。読み直した要素データをそのまま土台にしていたため、コピーしないキーの編集値が旧値へ戻り、画面表示だけでなく収集値・保存値からも消えていた。書き戻す土台に行の入力欄の編集値を重ねるようにした。
  - 「住所欄を編集したら『契約者住所と同じ』のチェックを外す」構成で、編集した住所が旧値へ戻っていた。
- **行への `data-{event}-bind-merge` でも、取得の起点になった入力の編集値が巻き戻る問題を修正した**（上と同じ原因）。行の中で「郵便番号を入れたら住所を引く」構成にすると、入力した郵便番号そのものが画面からも収集値からも消えていた。`data-fetch` を伴う手続きでは所有者への暗黙のコミットが走らないため、収集の宣言を持つ `<form>` がある構成でも起きる。書き戻す土台を行の入力欄の現在の状態まで進めるようにした。全置換（`data-{event}-bind`）では要素データに無いキーを空にするのが仕様のため、土台は使わない。
- **行への `data-{event}-bind` が、応答の内容が現在の要素データと一致するときに画面へ届かない問題を修正した**。要素データの内容が変わらないときは書き戻さない規則により差分更新が走らず、利用者が手で書き換えた欄が応答の値へ戻らないまま残っていた（画面と収集値が食い違う）。行の中の「再取得」ボタンのように、押しても要素データが変わらない構成で起きる。`data-{event}-copy` と同じく、この場合も行の入力欄へ反映するようにした。ただし編集の印は解除しない（解除すると送信後の編集の保護と `data-poll` の編集保護が壊れるため）。`data-{event}-bind-merge`・全置換・`data-{event}-bind-arg` のいずれも対象。
- **行への `data-{event}-copy` が、利用者が操作した入力へ届かない問題を修正した**。要素データの内容が変わらないときは書き戻さない規則（行の中の `data-fetch` の往復を止めるための規則）により差分更新が走らず、コピーした値が行の入力欄へ反映されなかった。コピーは明示的な値の供給なので、この場合も対象行の入力欄については編集の印を解除して値を反映するようにした。
  - 上と同じ構成で、住所を編集してもチェックが外れなかった。
- **開発モードの診断が、アロー関数の引数を「別のスコープでは供給されているキー」と誤警告する問題を修正した**（0.36.0 で追加した診断。式の評価結果は正しく、警告のみ）。`items.find(p => p.id === target)` の `p` はバインドで解決されるべき名前ではないため、診断の対象から外した。同じ名前を `data-each-arg` に使う兄弟要素があるだけで警告が出ていた。括弧付きの引数リストと分割代入も対象。引数の既定値の中で呼び出す関数の引数は束縛ではないため、従来どおり報告する。

### Added

- **`haori:rowadd` / `haori:rowremove` / `haori:rowmove` を実装した**。仕様書には記載があったものの発火箇所が無く、購読しても何も起きなかった。`data-each` の差分更新で行ごとに行要素で発火する（伝播するためコンテナや `document` でも購読できる）。
  - `rowadd` は行内容の描画と `data-form-list` 配下の入力欄への反映を終えてから発火するため、購読側から行内の DOM をそのまま参照できる。
  - `rowremove` は行が DOM から外れる**前**に発火する。外れた後では祖先へ伝播せず、コンテナで購読できないためである。
  - `rowmove` は行の位置が実際に変わったときだけ発火する。インデックスは `data-each-before` / `data-each-after` の固定要素を除いた行だけの並びで数える。
- **`haori:ready` を実装した**。こちらも記載のみで発火箇所が無かった。初期スキャン・初期フェッチの完了、`data-haori-ready` の付与、監視の開始、保留していた手続きの解除まで済んだ後に `document` で発火する（`detail.version`）。
- `demo/click/data-click-copy-demo.html` を追加した。`data-{event}-copy` で「フォーム → 確定パネル」と「一覧の行 → 行の外の共有パネル」を写す例で、後者はコピー元の指定を省いた場合の比較も含む。
- デモの整合性検査に「ライブラリの読み込み方が揃っていること」「固定版の CDN を読まないこと」の 2 件を追加した（`tests/demo-consistency.test.ts`）。
- バージョン表記の一致検査を追加した（`tests/version.test.ts`）。`package.json` を正として `src/version.ts`・`package-lock.json`・README・ガイド・技術仕様書・CHANGELOG の記載を突き合わせる。

### Changed

- **iife 配布物（`haori.iife.js`）のグローバル `Haori` を、モジュールの名前空間オブジェクトから `Haori` クラス本体へ変更した**。これまではクラス API を `Haori.Haori.addMessage(...)` と 2 段で取り出す必要があり、`window.Haori.setRuntime` などが存在しなかった。
  - `Haori.addErrorMessage(...)` のようにクラスの静的 API を直接呼び出せる。
  - `Core` / `Enhance` / `Env` / `Form` / `Fragment` / `Log` / `Queue` / `version` は同じグローバルのプロパティとして参照する（`Haori.Core.dumpScope(...)` など、従来から文書に載っていた書き方が実際に動くようになった）。
  - `Haori.Haori` と `Haori.default` は自己参照として残すため、0.37.1 以前の書き方も引き続き動作する。
  - iife だけエントリーポイントを分けたため（`src/global.ts` / `vite.config.iife.ts`）、`npm run build` は ES・CJS と iife の 2 回のビルドを実行する。ES / CJS 版の名前空間は変わらない。
- **公開デモサイト（GitHub Pages）でライブラリが読み込めなかった問題を修正した**。個別デモは `../../dist/haori.iife.js` を読み込むが、公開ビルドは `dist/demo/` 配下だけを配信するためリポジトリのルートを前提とした相対パスが解決できず、すべての個別デモが 404 になっていた。デモのビルドで本体を `dist/demo/lib/` へ複製し、HTML の参照を書き換えるようにした（書き換え漏れはビルドが失敗する）。
- `demo/click/repro-*-bootstrap-demo.html` が読み込む本体を、固定版の CDN（`haori@0.15.0`）からローカルのビルド成果物へ変更した。公開当時の版しか検証できず、現在のコードの回帰を捕まえられなかった。haori-bootstrap 側はバージョン指定を外し、常に最新の公開版との組み合わせを確かめる。
- `demo/message/data-message-demo.html` と `demo/fetch/data-fetch-runtime-demo.js` から、グローバルの形を吸収していた回避コードを削除した。
- `demo/event/haori-events-demo.html` の購読対象へ `ready` と行イベントを追加した。

### Docs

- **`data-{event}-copy` のコピー元が 0.33.0 で変わったことの移行注記を追加した**（技術仕様書・利用ガイド）。0.32.0 以前は祖先から継承した値もコピー元に含めていたため、`data-{event}-copy-source` を省いても行の値が写っていた。変更後は、コピー元の指定を省いた箇所がエラーも警告も出さずに何もコピーしなくなる。行の中のボタンから行の外へ写している箇所には `data-{event}-copy-source` の追記が必要である。
- 行イベントの発火対象・発火順序・インデックスの基準を技術仕様書へ明記した。`haori:eachupdate` との使い分けと、外部ライブラリ連携なら `data-enhance` を使えることも併記した。
- `haori:ready` の発火時点と、購読をライブラリの読み込みより前に登録する必要があることを明記した。
- ブラウザのグローバル `window.Haori` の形を技術仕様書・利用ガイド・README へ明記した。
- 技術仕様書の「エクスポート」節が古かったため、実際のエクスポート（`Enhance` / `enhancers` / `Enhancer` 型）に合わせ、`version` の固定値表記（`0.10.0`）を削除した。
- 技術仕様書の属性名の誤記を 2 件修正した。開発モードの有効化は `data-haori-dev` ではなく読み込み時の `<script data-dev>`、検証失敗時のスクロールは `data-{event}-scroll-on-error` ではなく `data-{event}-scroll-error` である。

### Tests

- 単体テストを追加した（行イベント 4 件、iife グローバル 7 件、バージョン一致 4 件、デモ整合性 2 件、行への書き戻しとユーザー編集 9 件、診断の誤警告 3 件、レビューで挙がった残存リスクの確認 5 件）。行への書き戻しの 9 件には、要素データが変わらない場合の画面同期 3 件と `data-poll` の編集保護の非退行 2 件を含む。
- Playwright を追加した（グローバルの形 2 件、`data-click-copy` 3 件、行への書き戻しとユーザー編集 2 件）。`haori:*` イベントデモのテストへ `ready` / `rowadd` / `rowremove` / `rowmove` の確認を追加した。
- 報告された構成の再現ページを `playwright/row-copy-user-edit-repro.html` として残した（収集の宣言がある構成と無い構成の 2 例）。

### Chore

- **`npm run format:check` が通るようにした**。20 ファイルで失敗していた。
  - うち 10 ファイルは改行コードだけの不一致だった。Prettier は `endOfLine: "lf"` を指定しているが、`.gitattributes` が無いため Windows（`core.autocrlf=true`）の作業ツリーでは CRLF に変換されていた。`.gitattributes` に `* text=auto eol=lf` を宣言し、作業ツリーの改行を LF に統一した（リポジトリに格納される内容は従来から LF のため、差分は生じない）。
  - 残る 10 ファイルは `npm run format` で整形した。処理の変更は無い。
  - Prettier と ESLint が両立しない指摘を解消した。`indent` は Prettier に委ねて検査を止め（三項演算子やメソッドチェーンの折り返しで結論が異なり両方を満たせない。幅は `max-len` で担保する）、`quotes` は `avoidEscape` を有効にした（文字列にシングルクォートを含む場合はダブルクォートを許す）。Prettier が 80 桁へ折り返せない 4 行は、変数の抽出・引数の見直し・行末コメントの移動で解消した。
- `npm test` の `posttest` へ `format:check` を追加した。整形漏れが CI（`npm run test` を実行）とローカルの両方で検知される。

## [0.37.1] - 2026-07-31

### Fixed

- **`data-prefix` で接頭辞を変えたページで `data-if` / `data-each` / `data-derive` の式が評価されない問題を修正した**。これらの属性は値に `{{}}` を書かずに式を渡すため「プレースホルダが無くても評価する属性」として名前で判定していたが、その判定が `data-` / `hor-` の固定リストだったため、`data-prefix="haori-"` のページでは属性値が文字列のまま扱われていた。`data-each` は `Invalid each attribute` でコンソールエラーになり、`data-if` は常に真（非空文字列）と評価されて条件が効かなかった。判定を実行時の `Env.prefix` から組み立てるようにした（既定の `data-` と旧称 `hor-` も引き続き有効）。

### Added

- デモの一覧を `demo/index.html` に追加した。`demo/` 配下のすべてのページをカテゴリ別に辿れる。
- 仕様書にあってデモが無かった機能のデモを追加した。
  - `demo/derive/data-derive-demo.html`（親の選択から子候補を導出する）
  - `demo/each/data-each-visible-demo.html`（スクロール中の可視行範囲）
  - `demo/external/data-external-demo.html`（外部ライブラリの生成 DOM を監視から外す）
  - `demo/click/data-click-run-demo.html`（宣言から任意の JS を実行し、既定動作を抑止する）
  - `demo/runtime/data-prefix-demo.html`（属性の接頭辞を変える）
  - `demo/runtime/data-dev-demo.html`（開発モードの診断）
  - `demo/bind/data-bind-nested-demo.html`（バインドの入れ子・継承と上書き）
  - `demo/event/haori-events-demo.html`（`haori:*` イベントの観測）

### Changed

- 実装に存在しない属性を使っていたデモを削除した（`demo/event/data-event-demo.html` の `data-event` / `data-event-arg`、`demo/event/event-test.html` の `data-haori` / `data-procedure-url` / `data-procedure-method`）。イベントの確認は `demo/event/haori-events-demo.html` へ置き換えた。
- 属性名と内容が一致していなかったデモを差し替えた（`demo/bind/data-bind-arg-demo.html` は存在しない `data-bind-arg` を名乗って `data-each` を見せていたため、`demo/bind/data-bind-nested-demo.html` へ置き換え）。
- 誤った書き方をしていたデモを修正した。
  - `demo/fetch/data-fetch-demo.html`: 禁止キーワード `typeof` を使った式を暗黙のオプショナルチェーンへ、`data-each-key="'id'"` を `id` へ
  - `demo/fetch/data-fetch-method-demo.html`: スクリプトの二重読み込みと `</body>` 後のスクリプトを整理し、`data-fetch-method` を実際に使う形へ
  - `demo/each/data-each-before-demo.html` / `data-each-after-demo.html`: 固定要素をループコンテナの直接の子へ移した（テンプレート内側に置くと行と一緒に複製される）
  - `demo/click/data-click-click-demo.html`: 存在しない JSON への取得と `alert` を廃止し、連鎖の結果を観測できる形へ
  - `demo/click/data-click-row-demo.html`: 行テンプレートを 1 要素にまとめた（複数の子を並べると最初の要素だけがテンプレートになる）
  - `demo/click/data-click-reset-refetch-demo.html`: 取得結果とリセット対象を表示するようにした
  - `demo/click/data-click-before-after-demo.html`: `alert` を記録表示に変え、`false` を返して中止する例を追加した
  - `demo/form/data-form*-demo.html`: 収集結果を表示するようにした（初期値は `data-bind` から入力欄へ反映する構成へ）
  - `demo/if/data-if-false-demo.html`: `<head>` に置かれていた本文要素を `<body>` へ移し、状態表示を属性の変化監視で更新するようにした
  - `demo/message/data-message-demo.html`: CSS による表示とスクリプトからの付け外しの例にした
  - `demo/row/data-row-demo.html`: `data-row` の説明を `data-each-key` との関係を含めて正した
  - `demo/components/header.html` / `demo/import/components/header.html`: 壊れたリンクと、断片に混ざっていた文書全体の記述を修正した
- `demo/vite.config.ts` のビルド入力を自動列挙にした。これまでは 4 ページを手書きで指定していたため、デモを追加しても公開ビルドに含まれず、削除した場合はビルドが失敗していた。

### Tests

- `playwright/demo-display.spec.cjs` の全ページ検査を強化した。可視領域に未展開の `{{式}}` が残らないこと（説明用の `code` / `pre` と `data-if` の非表示分岐は対象外）、Haori を読み込むページで `data-haori-ready` が付くこと、記述ミスを示す警告（禁止キーワード・属性値の解釈失敗）が出ないことを追加し、固定待ちを初期化完了とレンダリング完了の待機へ置き換えた。
- `tests/demo-consistency.test.ts` を追加（6 件）。デモが使う `data-*` 属性がすべて仕様書に載っていること、すべてのデモページが `demo/index.html` から辿れること、相対リンク・スクリプト・部分テンプレート・取得先の参照先が存在すること、セレクタ属性が指す id が同じページにあることを、ファイルの内容から検査する。
- デモの操作テストを追加した（Playwright、53 件）。`playwright/demo-bind-each-if.spec.cjs`（12 件）、`demo-form.spec.cjs`（5 件）、`demo-click.spec.cjs`（10 件）、`demo-fetch.spec.cjs`（10 件）、`demo-state.spec.cjs`（8 件）、`demo-advanced.spec.cjs`（8 件）。
- `tests/prefix-force-evaluation.test.ts` を追加（3 件）。接頭辞を変えても `data-each` が配列として、`data-if` が条件として評価されることを検証する（本修正の回帰ガード）。

### Docs

- `docs/ja/specs.md` の `data-each-before` / `data-each-after` に、ループコンテナの直接の子へ置く必要があること（テンプレート内側では固定要素にならないこと）を明記した。
- `docs/ja/specs.md` の `data-message` に、ライブラリは属性の付け外しだけを行い表示はページの CSS が担うこと、スクリプトからの付け外しの窓口と付与先の要素を明記した。

## [0.37.0] - 2026-07-31

### Added

- **外部ライブラリ連携の宣言 `data-enhance` / `data-enhance-new` を追加した**。DOM を走査して機能を付加する外部ライブラリ（Choices.js・郵便番号補完など）は、`data-each` の行追加や `data-if` の再表示で DOM が入れ替わるたびに再適用が必要になる。従来は `data-each-rendered-run` から画面側の JavaScript を呼び、適用対象の判定・冪等性・インスタンスの保持をアプリ側で持つ必要があった。
  - `data-enhance="名前"`: `Haori.enhancers.register(名前, {init, refresh, destroy})` で登録した連携を適用する（iife グローバルは `Haori.enhancers`、ESM は `import {enhancers} from 'haori'`）。契機は、初期スキャン・後から追加されたノード・`data-each` の新規行で `init`、`data-each` の描画確定（`data-each-done` 付与時）と `data-if` の非表示→表示で `refresh`（未適用なら `init`）、要素が DOM から外れたときに `destroy`。
  - 適用は**要素ごと・名前ごとに一度だけ**で、走査は宣言した要素の配下に限定される。空白区切りで複数の連携を宣言できる。
  - **登録はスクリプトの読み込み順に依存しない**。未登録の名前は適用を保留し、`register()` の時点で `document.body` 配下を遡って適用する（開発モードでは保留を一度だけ警告する）。
  - `init` / `refresh` / `destroy` の例外は `error` ログへ記録して続行する。1 つの連携の失敗で描画や他要素の適用を止めない。
  - `data-each` の描画確定では、`data-enhance` の `refresh` を `data-each-rendered-run` より前に実行する（外部ウィジェットの再同期を先に済ませる）。
  - `data-enhance-new="Global.Ctor"`: 登録なしでグローバル参照を `new` するだけの簡易形。値はドット区切りのグローバル参照だけを許し（属性値をコードとして実行しない）、対象要素を引数に渡して要素ごとに一度だけ呼ぶ。再同期と後始末は無いため、インスタンスの再同期が必要なライブラリは `data-enhance` を使う。
  - 引数を受け取らず自分で `document` 全体を走査するライブラリでは、Haori が担保できるのは呼び出し回数だけである（走査範囲の限定はライブラリ側の対応が必要）。
  - 外部ライブラリの生成 DOM は従来どおり `data-external` で監視対象から外せる（`data-enhance` と併用できる）。

### Tests

- `tests/enhance.test.ts` を追加（14 件）。初期スキャンでの適用、再スキャンでの二重適用の防止、空白区切りの複数指定、行追加で追加行にだけ適用すること、`data-each` の描画確定と `data-if` の再表示での再同期（`init` を繰り返さないこと）、行削除での `destroy`、描画より後の登録で遡って適用すること、未登録名の開発モード警告が一度だけであること、`data-enhance-new` がグローバル参照を要素ごと一度だけ `new` すること、式やコードを受け付けないこと、解決できない参照の警告と描画継続、`init` の例外でも描画が続くこと、`data-external` 配下でも適用されることを検証する。
- `tests/enhance-external-move.test.ts` を追加（2 件）。ライブ監視下で対象要素を生成コンテナへ再配置したとき、`data-external` を併用すればインスタンスが保持され、併用しないと `destroy` と再 `init` が走ることを検証する（併用が必要な理由の回帰ガード）。
- `playwright/enhance.spec.cjs` を追加（2 件）。実ブラウザで、適用・再同期（選択肢の追加に追随）・追加行だけへの適用・行削除での生成 DOM の片付け、および `data-enhance-new` の `new` を確認する。

### Docs

- `docs/ja/specs.md` に「`data-enhance`」「`data-enhance-new`」を追加し、`data-each-rendered-run` の説明から相互参照した。
- `docs/ja/guide.md` に「外部ライブラリを宣言で適用する（`data-enhance`）」を追加し、既存の Choices.js レシピを `data-each-rendered-run` を使う書き方として明示した。
- `README.md` / `README.ja.md` に項目を追加した。
- `demo/enhance/data-enhance-demo.html` を追加し、`demo/index.html` に 23 番のカードを追加した。

## [0.36.0] - 2026-07-31

### Fixed

- **`data-each` の行への値反映が、宣言バインドで決まる入力の値を潰す問題を修正した**。行の値反映は行の再評価（`Core.evaluateAll`）の直後に走るため、`data-attr-value="{{...}}"` で評価したばかりの値を収集値（多くは空文字）で上書きしていた。上書きされた空値は次の収集で行データへ焼き付くため、行の中で「取得した候補から選択中の 1 件を引いて hidden へ載せる」構成は永久に空になっていた（0.35.0 実測。同じ式を `data-attr-title` へ書いた場合は正しく反映されるため、値の同期経路だけで起きていた）。
  - 対象は行単位の値反映（`Form.syncRowValues`）で、宣言バインド（`data-attr-value` / `value="{{式}}"` / `data-attr-checked` / `<option>` の `selected`）の評価が**解決している**場合は、行データにキーがあっても上書きしない。フォーム全体への逆方向同期（`Form.syncValues`）の扱いは変更していない。
  - 評価が**未解決**の場合は従来どおり行データを反映する。保存済みレコードから復元した直後は候補が届くまで式が解決しないため、宣言バインドを権威にすると復元した値を失う。
  - 行データにキーが無い場合の扱い（宣言バインドの値を空にしない）は従来どおり。
  - 反映した値がバインディングデータ（`data-store` のミラーを含む）へ入るのは次の収集の時点。送信値は収集時に入力欄から読み直すため、選択した直後の送信でも評価結果が送られる。
  - **後方互換の注意**: 行データが持つ値で宣言バインドの評価結果が上書きされていた構成（`data-attr-value="{{token}}"` の入力に対して、サーバーが行ごとに `token` を返す構成など）では、表示と収集値が宣言バインドの評価結果へ変わる。行ごとの値を使いたい場合は、宣言バインドを外して行データに委ねるか、式を行データ参照（`{{c.token}}`）へ変更してください。

### Added

- **開発モードで「別のスコープでは供給されているキー」を参照している式を警告するようにした**。`??` や `?.` で既定値を書いた式は値のある結果になるため未解決参照にならず、応答のバインド先を兄弟要素にしてしまった宣言（バインドは対象要素とその子孫にしか見えない）が無言で既定値のまま表示され続けていた。描画完了後に集約して 1 回だけ報告し、同じ式でキーが解決した評価があれば報告しない（行ごとに応答を取得する構成では、ある行が先に解決している間だけ別の行が一時的にスコープ外になるため。誤検知より見落としを選ぶ）。本番（開発モード無効）では記録も出力も行わない。

### Tests

- `tests/row-declarative-value.test.ts` を追加（9 件）。選択に追随して hidden の値が入ること、その値が収集値へ載ること、選択を変えると追随すること、行の値反映（`Form.syncRowValues`）を直接呼んでも解決済みの宣言バインドを上書きしないこと、評価が未解決なら行データを反映すること、行データが同名キーを持っていても宣言バインドの評価結果が勝つこと（本変更の優先順位）、利用者が編集した行の値が巻き戻らないこと、宣言バインドの無い入力は従来どおり行データで上書きされること、行データにキーが無い宣言バインドは空にされないこと（既存仕様の回帰）を検証する。
- `tests/cross-scope-identifier-diagnostics.test.ts` を追加（5 件）。別スコープで供給されたキーの参照を警告すること、同じ式とキーは一度だけ報告すること、どこにも供給されていないキーは従来の集約警告に任せること、同じ式が後で解決した場合は報告しないこと、開発モード無効では出力しないことを検証する。
- `playwright/row-declarative-value.spec.cjs` を追加（2 件）。実ブラウザで、選択したプラン名が hidden に残り選択変更に追随すること、バインド先の外に置いた宣言は既定値のままになることを確認する。

### Docs

- `docs/ja/specs.md` の「`data-each` で生成された行への値反映」に宣言バインドと行データの優先順位（解決済みは宣言バインド優先、未解決は行データ優先、バインディングデータへ入る時点）を追記し、「未解決参照の診断」に別スコープ供給の警告を追加した。`data-fetch` の節に、行の中で取得した候補を行全体で使う書き方（行の内側のラッパへ `data-fetch-bind` で寄せる。行要素自身を指すと行データへの書き戻しになる）を追記した。
- `docs/ja/guide.md` に「行の中で候補から選択中の 1 件を引く」を追加した。
- `demo/form/row-declarative-value-demo.html`（＋候補の応答 JSON）を追加し、`demo/index.html` に 22 番のカードを追加した。

## [0.35.0] - 2026-07-31

### Changed

- **応答のバインドより後に実行するアクションの属性値を、使用する直前に評価するようにした**。従来は手続きの開始時（クリック時点）に評価した文字列を使っていたため、`data-{event}-redirect="{{redirectUrl || '/complete.html'}}"` と書いても応答の値が反映されず、遷移先が既定のままだった（バインドは処理順 9、リダイレクトは 20 で、応答は遷移前に確定していた。0.34.0 実測）。同じ原因で `data-{event}-dialog` / `-toast` のメッセージも応答の値が欠落していた。
  - 対象は `data-{event}-dialog` / `-toast`（17、表示直前）、`-history`（19、`pushState()` 直前）、`-scroll`（19 の後、スクロール直前）、`-redirect` / `-redirect-return-param`（20、遷移直前）。これにより `{{nextAction === 'pay' ? redirectUrl : '/complete.html'}}` のような応答による振り分けを属性だけで書ける。
  - 評価スコープは属性を宣言した要素のバインディングデータ（祖先からの継承を含む。`data-each` の行では行スコープ）。応答を参照するには `data-{event}-bind` の対象を**その要素自身または祖先**にする（兄弟要素などへのバインドはスコープに入らないため参照できない）。
  - 使用直前の評価で参照が解決できず、かつ手続き開始時には解決できていた場合は、開始時の値を使い開発モードで警告する。`data-{event}-bind` の全置換で参照キーが消える構成で、再評価結果（空）を採ると遷移や表示そのものが静かに止まるため。開始時から解決できない場合は従来どおり「指定なし」として扱う。
  - **後方互換の注意**: 応答に式が参照しているキーと同名のキーがある場合、遷移先やメッセージが応答の値へ変わる（これが本変更の目的）。開始時の値を使い続けたい場合は、参照するキーを応答が上書きしない位置（別のバインド先や `data-{event}-bind-arg`）へ分けてください。
  - 対象外は、バインドより前に使う属性（`-confirm` / `-fetch` / `-data` / `-form` など）、式を使えない生値（`-store-clear` / `-store-clear-type` / `-toast-level`）、検証失敗時に使う `-scroll-error`、および従来から実行時に解決している `-history-data` / `-history-form`（`-reset-before` 直後のスナップショット規則も現状維持）。
  - 反映されるのはその手続き自身が完了させた更新だけで、`-refetch` / `-click` が起動した別の手続きの完了は待たない。`data-store` のミラーはバインディングの確定と同期なので、遷移の前に必ず完了している（破棄と遷移は `-store-clear` → `-history` → `-redirect` の順）。
  - `ProcedureOptions` を直接渡す内部経路（属性を伴わない）では再評価せず、渡された値をそのまま使う。

### Tests

- `tests/late-attribute-evaluation.test.ts` を追加（19 件）。応答の値による遷移先の切り替えと振り分け、自要素へのバインド、非祖先バインドでは参照できないこと、参照が消えた場合の開始時の値へのフォールバックと警告、開始時から未解決なら遷移しないこと、`-redirect-return-param` の再評価、`data-each` の行スコープ、`-bind-arg` 配下の参照、`data-{event}-if` が偽なら評価も遷移も行わないこと、`data-store` のミラーが遷移前に完了していること、`-store-clear` が遷移より前に実行されること、`ProcedureOptions` 直接指定の不変、`-dialog` の `
` 復元、`-toast`（レベルは生値）、`-history` の URL、`-scroll` のセレクタ、`data-{event}-reset` と併用してもフォーム値を参照できること、同じ要素を 2 回操作しても毎回応答の値で切り替わること（属性の描画で DOM 上の値が評価結果へ置き換わっても、再評価は宣言に対して行う）を検証する。
- `playwright/late-attribute.spec.cjs` を追加（2 件）。実ブラウザで、応答の `nextAction` によって決済ページと完了ページへ振り分けられること、`-toast` と `-history` にも応答の値が入ることを確認する。

### Docs

- `docs/ja/specs.md` に「バインド後に実行するアクションの評価タイミング」を追加し（対象属性・評価スコープ・未解決参照の 4 象限・対象外・注意）、`-dialog` / `-toast` / `-redirect` / `-redirect-return-param` / `-history` の各節と処理順序・実行フローへ反映した。
- `docs/ja/guide.md` に「応答の値で遷移先やメッセージを決める」を追加した。
- `README.md` / `README.ja.md` に項目を追加した。
- `demo/form/late-attribute-demo.html`（＋遷移先 2 ページと応答 JSON 2 件）を追加し、`demo/index.html` に 21 番のカードを追加した。

## [0.34.0] - 2026-07-31

### Added

- **フィールド間の条件を宣言でブロックできるようにした**。従来、`data-attr-required` / `data-attr-disabled` と `data-if` のメッセージはフィールド間の条件を**表示**できたが、**押下のブロックには使えなかった**。属性値の反映はキュー（`requestAnimationFrame`）で行われるため、「最後の欄を直してそのまま次へを押す」操作ではクリック時点の属性が 1 フレーム古く、条件を満たしていない状態で手続きが走った（逆方向でも、直した直後は `disabled` が残っていてクリックが無視された。どちらも実測で再現）。ネイティブ検証（`data-{event}-validate`）はクリック時点の DOM を見るため確実だが、等値・いずれか必須はネイティブの制約では表現できなかった。
  - **`data-validity="{{式}}"` / `data-validity-message="…"`** を追加した。入力要素へ宣言し、条件が偽のとき `setCustomValidity()` にメッセージを設定する。ネイティブ検証に相乗りするため、バブル表示・フォーカス移動・`data-{event}-scroll-on-error`・CSS の `:invalid` がそのまま働く。メッセージを省略すると「入力内容を確認してください」を使う。
  - **`data-{event}-if="{{式}}"`**（非イベントは `data-fetch-if`）を追加した。条件が偽のときは以降のアクション（`-run` / `-confirm` / `-reset-before` / `-before-run` / `-fetch` / `-bind` / `-store-clear` / `-history` / `-redirect`）をすべて実行しない。停止するだけで、メッセージ表示は `data-if` / `data-message` と併用する（開発モードでのみ中断のログを出す）。表示制御の `data-if` とは別物であることを文書で明示した。
  - どちらも手続きの**実行時に同期評価**する。評価順は `data-{event}-validate` の後、`data-{event}-run` の前（ネイティブ検証で表現できるエラーを先に見せ、条件が偽なら run の副作用も起こさない）。
  - 評価スコープはバインディングデータ（継承込み）に、フォーム内で**宣言されている**収集キーを収集値で重ねた値。クリック時点で最新なのは収集値だけ（入力欄の内部値は `change` / `input` の委譲内で同期更新される）ため、直前に変更した入力が必ず条件に入る。収集値に現れないキーは未定義として扱い、`data-if` で非表示になった欄の古い値による誤判定を防ぐ。`data-form-arg` / `data-each-arg` の指定時はそのキー配下へ重ね、`data-form-list` の行ではその行の収集値を使う。
  - 参照が解決できない条件は「満たしていない」と扱い実行しない（ブロック目的の宣言なので安全側へ倒す）。警告ログに属性名と式を出す。
  - `data-fetch-if` は自動取得の再取得判定にも参加する（条件の真偽をシグネチャに含める）。含めないと、条件が偽で見送った後に条件だけが真へ変わっても再取得が起きない。
  - `data-{event}-validate` はフォームが解決できない手続きでは働かないため、`data-validity` が宣言されているのに検証されない構成を開発モードで警告するようにした。

### Changed

- **押下のブロックに `disabled` を使わない方針を文書化した**。HTML 仕様上、無効化されたフォーム部品はクリックイベントを発火しないため、「直したのに押せない」方向は実行時の判定では救えない。ブロックは `data-validity` / `data-{event}-if` で行い、`data-attr-disabled` は視覚的な合図に限る（または `data-attr-class` に置き換える）。
- `data-validity` / `data-validity-message` は評価結果を DOM 属性へ書き戻さない（属性には宣言したテンプレートが残る）。実行時は常に生値から評価する。この経路は共通の属性反映を通らないため、属性値を実行中に外部から書き換えても反映されない（`data-store` と同じく宣言は静的なものとして扱う）。

### Tests

- `tests/field-condition-validation.test.ts` を追加（18 件）。`data-{event}-if` は、一致→不一致に変えて即クリックしても実行されないこと、不一致→一致に直して即クリックすると実行されること、祖先が持つ古い値を非表示分岐の宣言キーがシャドーすること、偽のとき `run` / `confirm` / `redirect` が実行されないこと、中断後も再度押せること、未解決参照で実行しないこと、`data-form-arg` 構成、集約条件、非イベントの `data-fetch-if` を検証する。`data-validity` は、偽で止まりメッセージが設定されること、真で解除されること、既定文言、`data-{event}-validate` が無い場合の非検証と開発モード警告、非表示分岐の除外、いずれか必須、`data-form-list` の行ごとの判定、未解決参照、属性へ書き戻さないことを検証する。
- `playwright/field-condition.spec.cjs` を追加（3 件）。実ブラウザで、`data-validity` がネイティブ検証としてブロックすること、最後の欄を直してそのまま押すと通ること、`data-{event}-if` が条件を満たすまで止めること、条件を壊した直後に押しても送信されないことを確認する。
- `tests/procedure.test.ts` の疑似フラグメントに属性参照（`hasAttribute` / `getAttribute` / `getRawAttribute` / `getParent`）を補った。

### Docs

- `docs/ja/specs.md` に「フィールド間の条件」（`data-validity` / `data-validity-message` / 評価スコープ）と `data-{event}-if` を追加し、手続きの処理順序へ組み込んだ。`data-{event}-validate` の対象フォームの条件も明記した。
- `docs/ja/guide.md` に「フィールド間の条件でボタンを止める」を追加した。
- `README.md` / `README.ja.md` に両属性の項目を追加した。
- `demo/form/field-condition-demo.html` を追加し、`demo/index.html` に 20 番のカードを追加した。

## [0.33.0] - 2026-07-31

### Added

- **編集可能な行（`data-each` と `data-form-list` を併用したコンテナの行）を `data-{event}-copy` / `data-{event}-bind` / `data-fetch-bind` の対象にできるようにした**。従来これらの属性は対象要素のバインディングデータを更新するだけで、入力欄へ同期するのはコピー先が `<form>` のときだけだった。行の入力欄の値は配列の要素データが権威なので、行要素・行内の `<div>`・外側の `<form>` のいずれを指しても入力欄は変わらなかった（行フラグメントのバインディングデータは描画のたびに作り直される一時スコープで、書き込みは次の再描画で消える）。`data-form-list` を持つ外側の `<form>` が必要な構成では入れ子の `<form>` を置けず（HTML の制約）、「契約者住所と同じ」の複写のような処理を属性だけで書けなかった。セレクタが行要素に解決した場合は、行に対応する配列要素を書き換えて所有者へ書き戻すようにした。以降は `data-each` の差分更新から行の入力欄へ反映されるため、画面と収集値が常に一致する。
  - 対象は、コンテナが `data-each` と `data-form-list` の**両方**を持ち、セレクタが解決した要素がそのコンテナの**行要素自身**である場合だけ。行の内側の要素を指した場合と、`data-form-list` を持たない `data-each` の行は従来どおり（行内のスコープ用要素へのバインドを保つため）。
  - 書き込み内容は `copy` が浅いマージ、`bind` が既定で置換（要素データに無いキーの入力欄は空になる）、`-bind-merge` が浅いマージ、`-bind-arg="k"` が要素データの `k` キーへの格納。`data-each-arg` の包みは付けない（入力欄の `name` が要素データのキーと対応するため）。
  - 配列は**書き戻す直前に読み直す**。手続きの開始時点のコピーを使うと、送信から応答までの間に他の行で確定した編集を巻き戻す。同一コンテナの複数行が対象のときは 1 回の書き戻しにまとめる（行ごとに書き戻すと後の書き込みが前を消す）。
  - `data-each-key` の指定時は位置ではなく**キー**で配列要素を特定する（応答を待つ間の並べ替えや行の増減で別のレコードへ書かないため）。行だった要素は属性を読んだ時点で記録し、応答が届くまでに削除された場合は警告ログを出して書き込みを捨てる。
  - `data-{event}-bind` では、送信後に行った行の編集を応答へ上書きし直す（フォームへのバインドと同じ扱い）。行は `<form>` ではないため、従来の再上書き処理は行の入力欄を保護していなかった。
  - 実際の書き込み先は所有者になるため、`haori:bindchange` は所有者要素で発火する（`haori:bindcomplete` は従来どおりバインド先の行で発火）。
  - 要素データの内容が変わらないときは書き戻さない。書き戻すと所有者の再評価が走り、行の中の `data-fetch` が再発火して往復が止まらない（同じ値を書く二度目のコピーで無用な再描画を起こさないためでもある）。
  - 行の描画中に起動された処理（行の中の `data-fetch` など）からの書き戻しは、完了を待たずに次へ進む。描画ループ側はその行の初期化（= 当該処理）の完了を待っているため、書き戻しの完了を待つと相互に待ち合って `Core.scan()` が終わらない。バインディングデータは同期で確定し、描画は進行中のループが再実行で拾う。判定のため `Core.isEachUpdateRunning()` を追加した。所有者が実行中のバインドワークを持つ場合に reentrant 実行へ切り替える扱いは、`bindResult()` の各分岐と揃えた。
  - `data-each` の式が単純な識別子パスでない、配列の所有者が解決できない、要素データがオブジェクトでない（プリミティブの配列）、`data-each-before` / `data-each-after` の固定要素を指している場合は、いずれも警告ログを出してスキップする（手続きは止めない）。

### Fixed

- **`data-{event}-copy` が祖先から継承したキーまでコピーしていた問題を修正した**。コピー元・コピー先のどちらも継承済みのバインディングデータ（`getBindingData()`）を基底にしていたため、祖先が持つ無関係なキー（一覧の配列など）が転送・複製され、コピー先の生データへ焼き付いて以降その祖先の更新がシャドーされていた。どちらも**その要素自身の**バインディングデータだけを対象にした（フォームからの収集がその要素の入力欄だけを対象にしているのと扱いを揃えた。双方向コミットや `-bind-arg` が生データを基底にしているのと同じ理由）。行への複写ではこの継承分が配列の各要素へ入り込むため、修正なしでは要素データが壊れる。

### Tests

- `tests/row-write-copy-bind.test.ts` を追加（19 件）。非イベントの `data-fetch-bind`（行ごとの `data-fetch`）で `Core.scan()` が完了し、行ごとに別の手続きが同じ配列へ書き込んでも互いの結果を消さないことを含む。行を指した copy が対象行の入力欄と配列へ届き他の行を変えないこと、`-copy-params` の絞り込み、複数行の同時指定で互いの書き込みが消えないこと、`data-each-key` 併用時に並べ替え後も同じレコードへ届くこと、bind の全置換・`-bind-merge`・`-bind-arg` の各挙動、送信後の行編集が応答で消えないこと、祖先が `data-form-arg` のキーを所有する構成でも成立すること（全置換の規則が構成によらないこと）、行の内側の要素と `data-form-list` の無い行が従来どおりであること、祖先のキーが焼き付かないこと、識別子パスでない `data-each`・プリミティブ配列の行・`data-each-before` の固定要素・応答前に削除された行で警告してスキップすることを検証する。
- `playwright/row-copy-bind.spec.cjs` を追加（1 件）。実ブラウザで行要素へのコピーが入力欄と収集値へ届き、他の行が変わらないこと、行を追加した直後の行へも複写できること、複写後の編集が配列へ確定することを確認する。

### Docs

- `docs/ja/specs.md` に「編集可能な行への書き込み」を追加し、`data-{event}-copy` / `-copy-source` の基底を「その要素自身のバインディングデータ」に改めた。`ProcedureOptions` の `rowWriteTargets` と、ユーザー編集の印の解除範囲の表も更新した。
- `docs/ja/guide.md` の「繰り返し行の中で『その行の要素』を対象にする」に、`data-form-list` の行へ複写する書き方とフェッチ結果を行へ入れる書き方を追加した。
- `README.md` / `README.ja.md` に行への copy / bind の項目を追加した。
- `demo/form/row-copy-bind-demo.html` を追加し、`demo/index.html` に 19 番のカードを追加した。

## [0.32.0] - 2026-07-30

### Added

- **バインディングデータの指定キーをブラウザストレージへミラーする `data-store` を追加した**。従来、画面をまたいで入力状態を持ち回るには、フォーム値の退避・次画面の初期 `data-bind` への書き戻し・応答の一部の退避・破棄をすべて画面個別の JavaScript で行う必要があった（Haori の読み込み前に `data-bind` 属性を書き換える構成になりがちだった）。1 ストレージキーに 1 つの JSON レコードを持ち、宣言した要素のバインディングデータと双方向にミラーすることで、複数画面のウィザードを `<script>` なしで宣言できるようにした。
  - **復元**は優先属性として `data-bind` の直後に処理する（処理順は `bind` → `store` → `url-param`）。復元値は初期 `data-bind` と同じ扱いになるため、`data-if` の条件・`data-each` の配列・入力欄の初期値としてそのまま機能する。キー単位の差し替えで、レコードに無いキーは `data-bind` の既定値を保つ。復元は各要素の初回スキャン時のみで、`data-if` の再表示では繰り返さない（利用者の編集を初期値へ巻き戻さないため）。
  - **保存**は対象キーの値が変わったときに自動で行う。フォームの双方向コミット・`data-{event}-bind`・`data-fetch` の応答反映はいずれも `Core.setBindingData()` を通るため、**フェッチ応答の一部の退避**も宣言だけで成立する（明示的な保存属性は設けていない）。
  - 書き出しはバインディングデータの確定と**同期**で行う。`Queue`（`requestAnimationFrame`）へ遅延させると、`data-{event}-redirect` による遷移や背面タブで次フレームが来ず、遷移直前の保存を取りこぼすため。間引きは「直前の書き出し内容と変わらなければ書かない」だけで行い、まずキーごとの参照同一性で比較して直列化を省く。
  - 書き込みは宣言したキーだけを置換し、レコード内の他のキーは保持する。画面ごと・要素ごとに担当キーだけを宣言でき、他の画面が保存した値を壊さない。宣言したキーがその要素のバインディングデータに**存在しない**場合はレコードを変更しない（削除は破棄属性だけが行う）。復元より前には書き出さない（`data-bind` の既定値で保存済みの値を潰さないため）。
  - 対象キーの指定（`data-store-params` または `data-store-arg`）を**必須**にした。個人情報を含む状態を扱うため、保存範囲をマークアップ上で常に明示させる。`_fetch` / `_poll` などの予約キー（先頭が `_`）は常に対象外。
  - 対象は宣言した要素**自身**のバインディングデータ。フォームの双方向コミットはフォーム要素自身へ書き込むため、入力状態を保存するなら `<form>` に宣言する（祖先に宣言しても入力値は入らない）。
  - `data-each` の行の内側では無効（同一レコードへ全行が書き込むため）。`input[type=file]` は復元不可（バインディングデータにはファイル名しか入らない）。属性値に式は使用できない。他タブ同期は行わない。いずれも該当時は警告ログを出す。
  - ストレージが無効な環境・容量超過・保存済み JSON の破損・オブジェクトでないレコードは、警告ログを出して継続する（画面は壊さない）。
- **レコードを破棄する `data-{event}-store-clear` を追加した**（`-type` でストレージ種別を指定、非イベントは `data-fetch-store-clear`）。手続きの実行順は `data-{event}-history` の直前。破棄後もミラーは停止せず、宣言要素の書き出し基準を現在値へ更新して、以後は値が変わったときだけ再保存する。復元は破棄より先に済むため、保存した値を表示してから破棄できる。

### Changed

- **セレクタを値に取る属性で、テンプレート式（`{{}}`）を評価した結果をセレクタとして扱うようにした**。従来これらの属性は生の属性値をそのままセレクタとして扱っていたため、`data-change-bind="#plan-scope-{{i}}"` は `querySelectorAll` の例外になり、`data-each` の行の中から「その行の要素」をバインド先・コピー先に指定できなかった（`id` 属性は補間されるのに、それを指す手段が無かった）。行ごとのバインド（行スコープへのフェッチ結果の反映）や、契約者住所をその行の住所欄へ複写する処理（`data-{event}-copy` ＋ `data-{event}-copy-source`）が属性だけで書ける。
  - 対象は `data-{event}-bind` / `data-fetch-bind` / `data-{event}-fetch-state` / `data-fetch-state` / `data-{event}-form` / `data-fetch-form` / `data-{event}-history-form` / `data-{event}-copy` / `data-{event}-copy-source` / `data-{event}-reset` / `data-{event}-reset-before` / `data-{event}-refetch` / `data-{event}-click` / `data-{event}-open` / `data-{event}-close` / `data-{event}-adjust` / `data-{event}-row-*`。`data-{event}-scroll` / `data-intersect-root` / `data-each-visible-root` / `data-poll-state` は従来から評価済みで、扱いを揃えた。
  - `data-{event}-bind-arg` / `-bind-params` / `-copy-params` のようなキー名を並べる属性は対象外（セレクタ属性のみ）。
  - 単体プレースホルダが未解決参照になった場合は「値の指定が無い」ものとして扱う（通常属性の未解決参照を属性削除として扱う規則に合わせた）。値を省略したときの既定動作になる。
- **CSS セレクタとして不正な値で例外にせず、ログ出力してスキップするようにした**。従来は `querySelectorAll` の例外がそのまま手続きを止めていた。同じ手続きの後続のアクションは実行される。セレクタの照会を `Selector` へ集約し、`data-intersect-root` / `data-each-visible-root` / `data-poll-state` / `data-{event}-scroll` も同じ扱いにした。
- `data-url-param` を `data-url-arg` なしで `data-store` と併用した場合に警告ログを出すようにした。`data-url-arg` を省略した `data-url-param` は要素のバインディングデータを全置換するため、復元した値も消える（既存の「同一要素の `data-bind` で宣言した既定値が消える」と同じ理由）。

### Tests

- `tests/store.test.ts` を追加（41 件）。復元（式・`data-if`・`data-each`・`<form>` の text / select / checkbox・`data-form-arg` との併用・`params` と `arg` の併用・既定値の保持・不要なシャドーを作らないこと・`local` の読み取り・破損 JSON・非オブジェクト）、保存（フォーム値・`data-form-arg` 構成・部分更新・`arg` スロット内の保持・非宣言キーの除外・予約キーの除外・同値では書かないこと・宣言キー不在時にレコードを壊さないこと・祖先更新でコピーが解除された直後・フェッチ応答・初期スキャンで既定値が上書きしないこと）、破棄（クリック・`load`・再シード・破棄後の再保存・`local`・キー未指定）、無効条件と異常系（キー指定なし・`data-each` 行内・式を含むキー・不正な種別・容量超過・読み取り失敗）、`data-url-param` との優先順、`data-store` 属性の削除では復元しないこと、原因が複数あるときに種別ごとに警告することを検証する。
- `tests/selector-template.test.ts` を追加（14 件、うち 11 件は修正前に失敗）。行ごとのバインド先・コピー先・コピー元・フェッチ状態の注入先・行コンテナ・フォーム・値調整・リセット対象・ダイアログ・履歴フォーム・スクロール先を式で指定できること（他の行に影響しないこと）、報告の構成（`change` → フェッチ → 行スコープへバインド）が成立すること、不正なセレクタで例外にせず後続のアクションが実行されること、未解決参照のセレクタを指定なしとして扱うこと、静的なセレクタが従来どおり解決することを検証する。
- `playwright/store-wizard.spec.cjs` と 2 画面の HTML を追加（4 件）。実ブラウザで画面をまたいだ持ち回り（入力 → 遷移 → 復元、`data-if` と入力欄の初期値）、バインドと遷移が同一手続きでも応答の退避が間に合うこと、別画面で追加した配列の共存と往復、破棄後に復元されないことを検証する。

### Docs

- `docs/ja/specs.md`: 「ブラウザストレージ」節（`data-store` / `data-store-params` / `data-store-arg` / `data-store-type`）と `data-{event}-store-clear` の節、「セレクタを値に取る属性の解決」節、コアモジュールの `Store` を追加。属性処理の優先順位・プレースホルダ評価順・イベント属性の処理順序・ディレクトリ構造・モジュール依存関係も更新した。
- `docs/ja/guide.md`: 「画面をまたいで入力を持ち回る（`data-store`）」と「繰り返し行の中で「その行の要素」を対象にする」を追加した。
- `README.md` / `README.ja.md`: よく使う属性へ `data-store` と、セレクタ属性のテンプレート式評価を追加した。

## [0.31.0] - 2026-07-30

### Added

- **`data-form-arg` を指定したフォームへ、祖先の `data-bind` が持つ同名キーを反映するようにした**。従来、逆方向同期（バインドデータ → 入力欄）が走るのは `Core.setBindingData()` が**そのフォーム自身**を対象にしたときだけだった。そのため祖先がレコードを所有する構成では、フォーム内の式は `{{detail.name}}` を正しく解決できているのに**入力欄だけが空**という非対称が残り、`data-attr-value` を編集可能な入力へ使う以外にレコードを表示する手段がなかった。祖先が当該キーを所有していれば入力欄へ反映するようにし、参照キー（式が読むキー）と書込キー（`name` が書き込むキー）を構造的に一致させられるようにした。
  - 反映するタイミングは「初期スキャンの完了時」と「祖先のバインドデータ更新で**そのキーの値が実際に変わったとき**」の 2 つ。値が変わっていない更新（同じ祖先の別キーの更新など）では入力欄に触らない。同じ値でも書き戻すと、利用者が確定した編集を巻き戻すため。
  - 対象は祖先の `data-bind` が持つキーだけで、`data-each` の行データと `data-derive` の派生データは対象外。どちらも描画のたびに作り直される仮想スコープで、行単位の書き戻しは `data-form-list` が担う。
  - 同じキーを持つ祖先が複数ある場合は最も近い祖先が権威（式の識別子解決と同じシャドーの規則）。
  - 一覧で選んだ行を編集フォームへ出す構成では、祖先のキーを差し替えるだけで入力欄が入れ替わる。

### Changed

- **祖先が所有するキーについて、双方向コミットが祖先の値を土台に収集値を重ねるようにした**。従来は収集値で丸ごと置き換えていたため、入力欄に対応しないフィールド（`id` など）がフォーム自身のコピーから抜け落ち、そのコピーが祖先をシャドーする結果 **1 回目の編集以降 `{{detail.id}}` が参照できなくなっていた**（URL に使っている場合はリクエストが壊れる）。祖先が当該キーを更新したときはこのコピーを解除して入れ直すため、古い値が残り続けることはない。
  - フォーム自身が `data-bind` で当該キーを宣言している場合の扱いは変えていない（従来どおり収集値で置き換える）。
- **祖先が所有するキーのリセットは、祖先のレコードの内容へ戻すようにした**（フォーム自身に初期 `data-bind` 宣言が無い場合）。あわせて、リセット後にフォーム自身へ当該キーのコピーを作らないようにした。作ると以降その祖先を更新してもシャドーされて届かなくなる。
- **送信後に行われた編集の保護を、祖先へのバインドにも適用するようにした**。従来「入力欄への書き戻しはフォーム自身へのバインドでしか起きない」ことを前提に、上書きし直す対象を `<form>` へのバインドに限っていた。祖先の更新が入力欄へ流し込まれるようになったため、配下に `data-form-arg` フォームを持つ要素へのバインドも同じ扱いに揃えた。揃えないと、保存ボタンの応答が届くまでに入力した値が静かに消える。

### Tests

- `tests/form-arg-ancestor-scope.test.ts` を追加（9 件）。初期表示での反映、祖先の更新による入れ替え、シャドーの解除と式の一致、コミット後も入力欄に無いフィールドが参照できること、値が変わらない更新で編集を巻き戻さないこと、近い祖先が権威になること、`data-each` の行データが対象外であること、リセットの戻り先、送信後の編集が祖先へのバインド応答で巻き戻らないことを検証する。

### Docs

- `docs/ja/specs.md`: 「祖先が所有するレコードの反映（`data-form-arg`）」を追加。`data-form-arg` の属性説明と「送信後に行われた編集の保護」の対象範囲も更新した。
- `docs/ja/guide.md`: 「参照スコープと書込スコープが違うとき」のレシピに、祖先がレコードを所有したまま `data-form-arg` で編集する書き方（推奨③）を追加した。

## [0.30.1] - 2026-07-30

### Fixed

- **属性の削除がキューで実行されるまでの間に書き込まれた値を、あとから消してしまう不具合を修正**。`MutationObserver` は「DOM から属性が消えた」状態を観測すると内部の属性マップへ反映し、あわせて DOM の削除タスクをキューの**末尾**へ積んでいた。一方 `Haori.addMessage` / `addErrorMessage` などの直接書き込みはキューの**先頭**へ積まれるため、削除タスクが待機している間にメッセージを付与すると、付与のあとに削除が走って**値が消えたまま復帰しない終端状態**になっていた。DOM に属性が無い場合は削除タスクを積まないようにした（判定方式は属性設定側の「書き込み不要なら積まない」と同じ）。
  - 実害が出るのは**フェッチエラーの再試行**。`data-{event}-fetch` のエラー応答は描画前に対象スコープのメッセージを一度クリアするため、クリア → 観測反映（削除タスク）→ 最新応答のメッセージ付与 の順で必ずこの並びが起こる。競合が成立すると、**2 回目以降のエラーでフィールドのメッセージが 1 つも表示されない**（`data-message` が付かない）。バリデーションエラーの原因が画面に出ないため、利用者からは「送信できないのに理由が分からない」状態になる。
  - 発生はキューの実行タイミング依存（1 フレームの処理時間上限で分割された場合に成立しやすい）で、常には起きない。単体テストでは全体実行時に数回に 1 回再現していた。
  - `data-attr-*` の削除（生属性と反映先属性）も同じ経路のため同時に修正した。DOM に残っている属性の削除は従来どおり反映される（意味は変えていない）。

### Tests

- `tests/attribute-removal-echo-race.test.ts` を追加（3 件、うち 2 件は修正前に失敗）。DOM から消えた属性の観測反映が後から付与した値を消さないこと、DOM に残っている属性の削除は従来どおり反映されること、`data-attr-*` の別名属性でも成立することを、キューへの積み順を明示して検証する。
- `tests/fetch_error_array.test.ts`: 「再試行時に既存メッセージをクリアし、常に最新の1応答へ置き換える」が負荷時に不定に失敗していた原因は本修正の不具合そのものだった（テスト側の待機不足ではない）。判定内容は変更していない。

## [0.30.0] - 2026-07-29

### Fixed

- **`data-if` が偽の分岐配下の入力がバリデーション対象から外れない不具合を修正**。非表示分岐（`data-if-false`）の入力は値収集（`Form.getValues`）では除外されるのに、ブラウザの制約検証では対象に残っていた。そのため非表示分岐に `required` の入力があると、`data-{event}-validate` もネイティブの `checkValidity()` も通らず、**表示中の分岐だけを入力しても送信できなかった**。しかも `reportValidity()` は `display: none` の要素へフォーカスできないため、ブラウザは何も表示せずに止まり、利用者からは原因が分からない状態だった。非表示のあいだ配下のフォームコントロールへ `disabled` を付与し、収集と同じ基準で検証対象から外すようにした。
  - 付与した `disabled` にはエンジン管理の印（`data-haori-if-disabled`）を付け、表示へ戻すときに**印がある要素だけ**を復帰させる。利用者が指定した `disabled`（`data-attr-disabled` の評価結果を含む）は表示後も維持される。入れ子の `data-if` が偽の分岐へは踏み込まないため、外側だけが表示に戻っても内側の入力は検証対象外のままになる。
  - `data-{event}-validate` の走査（`findFirstInvalid`）でも `data-if-false` 配下をスキップするようにした。非表示になった後に差し込まれた要素を取りこぼさないための保険。
  - 値収集の挙動は変えていない。副次的に、`new FormData(form)` によるネイティブ直列化からも非表示分岐が除外され、`Form.getValues` と一致するようになった。
  - `data-if` が偽のあいだ配下が再評価されない（`data-attr-required` などで制約を解除できない）点は仕様どおりで変更していない。この修正により、その回避策は不要になった。
- **識別子として使えないバインドキーが 1 つあると、同じスコープのすべての式が評価できなくなる不具合を修正**。式は `new Function(...バインドキー, 本体)` として組み立てるため、バインドキーは関数の引数名になる。`<input name="customer.email">` のようにドットを含む `name` があると、そのキーがそのまま引数名に並んで**引数リスト自体が壊れ**、`Failed to compile expression`（`Arg string terminates parameters early`）となって**無関係な `{{…}}` や `data-if` まで巻き添えで壊れていた**。引数名にできないキーは式のスコープへ載せないようにした。
  - 対象は `name` 由来のキーだけではない。`data-fetch` 応答の JSON キーや `data-bind` の JSON キーも同じ経路でバインドキーになるため、`{"total-count": 5}` のような応答を受けた時点でその要素配下の全式が壊れていた。
  - **`a,b` のようにカンマを含むキーは、例外も警告も出さずに引数の位置をずらし、他のキーの値を壊していた**（そのキーより後ろのキーが `undefined` になる）。`{a}` / `a=1` のような引数パターンになる形も同様に除外する。
  - 載せなかったキーを式が参照した場合は**未解決参照**として扱う（表示は空、`data-attr-*` は属性削除）。収集値・送信形式は変えていない（`name="customer.email"` はフラットなキーとして収集され、クエリでも `customer.email=...` のまま）。
- **入力要素自身に付けた `data-{event}-bind` が、その編集自身の「編集済みの印」を解除する不具合を修正**。フェッチを伴わない `change` / `input` のバインドは、入力欄から収集した編集値をバインドデータへ写す双方向コミットであり、値の供給ではない。にもかかわらずフェッチ応答の反映と同じ経路を通り、**バインド先配下すべての印を解除していた**。参照キーと書込キーが別の構成（`data-attr-value="{{record.a}}"` の欄を `data-change-bind-arg="draft"` へ書き込む等）では、次の項目を編集した瞬間に、まだ更新されていない参照キーの評価結果（空）で前の項目が上書きされ、**画面表示だけでなく収集値とバインドデータも空になっていた**（実ブラウザで確認。入力したはずの値が空で登録される）。バインド先を指定しない暗黙のコミットと同じ扱いに揃え、印を解除しないようにした。
  - 0.29.0 の CHANGELOG で「印を解除する操作」に挙げた `data-{event}-bind` は、フェッチを伴う場合に限る。同版で案内した「参照キーと書込キーを揃える制約は編集値を守るためには不要」も、この修正で `data-{event}-bind` を使う構成へ正しく当てはまるようになった。
  - 挙動差が出るのは「利用者が手で編集した欄の値が、同じイベントの `data-{event}-bind` が書き込むキーから導出される」場合のみで、その欄は編集値が維持される（従来は導出値へ再計算されていた）。**手編集していない欄は従来どおり更新される**。常に追従させたい導出値の欄は `readonly` にする（編集できない欄には印が付かない）。バインド先を指定しない暗黙のコミットでは従来から編集値が維持されるため、両者の扱いが揃った。
- **`data-{event}-reset` / `data-{event}-reset-before` がフォーム配下の `data-each` の描画結果を消したまま復元しない不具合を修正**。リセットは `data-each` の複製行を削除し、その後の再評価で描き直す前提だったが、**「この要素データで描画済み」という記録（要素データの署名）を削除時に破棄していなかった**ため、続く再評価が差分更新を省略して行が復元されなかった。フォーム内に API 取得の選択肢を `data-each` で描く構成では、リセットを伴う操作の後に選択肢が 0 件のまま残り、要素データを同じ場所から供給し直しても（`data-{event}-refetch` を併用しても）署名が同値のため戻らなかった。複製行の削除時に署名を破棄し、リセット後の再評価で現在のデータから描き直すようにした。
  - 「ダイアログを開くボタンには必ず `data-{event}-reset-before` を付ける」という運用と、フォーム内の `data-each` 選択肢を併用できるようになった。回避策として選択肢の描画をフォームの外へ出す必要はない（送信値になる入力欄では外へ出せない）。
  - リセットの意味は変えていない。前回の選択状態は解除され、`data-form-list` の複製行はフォーム自身のバインドデータが初期宣言へ戻ることで初期件数へ戻り、要素データが空になっていれば行は復元されない。

### Added

- **識別子として書けないバインドキーを式から読むための `haori.data` を追加**。予約名前空間 `haori` に、そのスコープの解決済み要素データを載せる（例 `haori.data['customer.email']`）。ドットや記号を含む `name` 由来のキー、非 ASCII のキーが対象。従来の組み込みヘルパーと同じく**式が `haori` を参照している場合のみ注入**するため、他の式のコストは変わらない。読み取り用途の浅いコピーで、書き込んでもバインドデータは変わらない。
- **式のコンパイルに失敗した `data-if` を、falsy とは区別して報告するようにした**。コンパイル失敗の評価結果は `null`（= falsy）になるため、開発モードの診断が「参照スコープでは条件が真に見えるのに falsy で非表示」という矛盾した内容になり、原因に辿り着けなかった。コンパイルできなかったことを名指しで報告する。

### Docs

- `docs/ja/specs.md`: `data-if` の動作を実装に合わせて訂正した。非表示時に「子要素をDOMから除去 (unmount)」と記載していたが、**要素は DOM に残る**（本件の原因そのもの）。あわせて非表示時の `disabled` 付与と検証対象外、配下を再評価しないこと、表示時の復帰順序を明記し、`data-if-false` 分岐の節へバリデーションの扱いを追記した。
- `docs/ja/guide.md`: 「非表示にした入力はバリデーションにも含まれない」を追加し、構成例、利用者指定の `disabled` が維持されること、`data-attr-required` では解除できないこと、`fieldset` の `disabled` 併用が不要になったことを記載した。
- `docs/ja/specs.md`: 「バインドキーと識別子（`name` の命名）」を追加し、引数として載せるキーの条件、載せなかったキーの扱い（未解決参照）、収集値・送信形式が変わらないこと、開発モードの警告を表で明記した。組み込みヘルパーの節へ `haori.data` を追記した。
- `docs/ja/guide.md`: 「ドットを含む `name`（識別子として書けないキー）」のレシピを追加し、`haori.data` での参照、`data-form-object` へ変える場合の送信形式の違い、開発モードの警告を記載した。
- `docs/ja/specs.md`: 「ユーザー編集と宣言バインドの権威」の解除条件を、`data-{event}-bind` はフェッチを伴う場合のみ解除する形へ書き分けた。導出値の欄を常に追従させたい場合の書き方（`readonly` にする、明示的な供給を契機にする）も追記した。
- `docs/ja/guide.md`: 「参照スコープと書込スコープが違うとき」のレシピへ、入力要素自身の `data-change-bind` でも編集が保持されることと構成例、導出値の欄を `readonly` にする注意を追記した。
- `docs/ja/specs.md`: `data-{event}-reset` の手順に描画済み判定の破棄を追記し、`data-each` の行がリセットでどう扱われるか（要素データの置き場所で決まること）を明記した。
- `docs/ja/guide.md`: `data-click-reset` に、フォーム内の `data-each` 選択肢がリセット後も残ることと構成例を追記した。
- `docs/ja/specs.md` / `docs/ja/guide.md`: 見出しのバージョン表記が 0.11.1 のまま取り残されていたため、公開済みの 0.29.0 へ揃えた。

### Tests

- `tests/if-false-validation-exclusion.test.ts` を追加（7 件、うち 5 件は修正前に失敗）。非表示分岐の `required` が検証対象外になること、値収集は変わらないこと、表示中の分岐は従来どおり止まること、表示と非表示の往復に追随すること、利用者指定の `disabled` が維持されること、入れ子の `data-if` が独立して追随すること、`data-each` の行を含む分岐でも成立することを検証する。
- `playwright/if-false-validation.spec.cjs`（および repro HTML）を追加（2 件）。ネイティブの検証 UI とフォーカス可否は実ブラウザでしか確認できないため、実際に操作して `willValidate` / `checkValidity()` / 手続きの通過と収集値を確認する。
- `playwright/commit-bind-user-edit.spec.cjs`: 最後の項目の `change` コミットを待たずに収集していたため、負荷時に「DOM と収集値は正しいがバインドデータの最後の項目だけ空」を掴んで不定に失敗していた。バインドデータが揃うまで待つようにした。
- `tests/binding-key-identifier.test.ts` を追加（20 件、うち 16 件は修正前に失敗）。ドット・ハイフン・先頭が数字・空白・予約語・カンマ・分割代入の形・既定値の形の各キーが他の式を壊さないこと、カンマ入りキーが他のキーの値をずらさないこと、分割代入の形が同名のキーを遮蔽しないこと、`haori.data` で読めること、載せなかったキーが未解決参照になること、開発モードでキーごとに一度だけ警告し本番では警告しないこと、コンパイル失敗の `data-if` が falsy と区別して報告されることを検証する。
- `playwright/binding-key-identifier.spec.cjs`（および repro HTML）を追加（1 件、修正前に失敗）。実ブラウザで、ドットを含む `name` があっても同スコープの `data-if` と `{{…}}` が評価され、収集値がフラットなキーのまま変わらないことを確認する。
- `tests/commit-bind-user-edit.test.ts` を追加（6 件）。参照キーと書込キーが別でも順に編集した 3 項目が DOM / 収集値 / バインドデータのすべてで保持されること、同一 `name` ラジオの bind では `data-if` の依存欄が従来どおり切り替わること、手編集していない導出値の欄は更新されること、手編集済みの欄は維持されること、`readonly` の欄は常に追従すること、`data-{event}-reset` は従来どおり印を解除することを検証する。
- `playwright/commit-bind-user-edit.spec.cjs`（および repro HTML）を追加（2 件、1 件は修正前に失敗）。実ブラウザは dirty value flag を持ち値の権威の伝わり方が異なるため、実際に 3 項目を順に入力して DOM・収集値・バインドデータを確認する。修正前は 3 つすべてで前 2 項目が空になっていた。
- `tests/fetch_error_array.test.ts`: 「再試行時に既存メッセージをクリアし、常に最新の1応答へ置き換える」の待機条件がクリア直後の中間状態でも成立し、負荷時に「name が未反映」の瞬間を掴んで失敗していたため、最新応答の name が書き直されるところまで待つようにした。
- `tests/reset-each-render.test.ts` を追加（6 件、うち 4 件は修正前に失敗）。`reset-before` の後も選択肢が残ること、選択状態だけが解除されること、`reset` 単体でも復元されること、リセット後に同じ要素データを供給し直しても（再取得相当）復元されること、要素データが空なら復元しないこと、`data-form-list` の複製行は初期件数へ戻ることを検証する。
- `playwright/reset-each-options.spec.cjs`（および repro HTML / JSON）を追加（1 件、修正前に失敗）。実ブラウザで、フェッチで取得した選択肢がリセット後も `<select>` に残り、選択だけが解除されることを確認する。
- `tests/attr-value-collection.test.ts`: 0.29.0 で追加した「短絡で値が確定する式は未解決参照にならない」が、他ケースの後追い評価による警告を拾って不定に失敗していたため、判定対象を対象の式に限定した。

## [0.29.0] - 2026-07-28

### Changed

- **確定したユーザー編集を宣言バインドの再適用より優先するようにした（破壊的変更）**。宣言バインド（`value="{{式}}"` / `data-attr-value` / `checked="{{式}}"` / `data-attr-checked` / `option` の `selected="{{式}}"` / `data-attr-selected`）は再評価のたびに評価結果を入力欄へ書き戻していたため、**評価結果が変わっていない再評価でも編集値が上書きされていた**。利用者が `change` / `input` で確定した入力欄には「編集済みの印」を付け、明示的な値の供給を受けるまでその欄への再適用を抑止する。属性の反映は従来どおり行い、DOM の値・チェック状態と内部値（収集値）は編集値のまま保つ。
  - 印を解除する（= 宣言バインドが権威を取り戻す）操作は次のとおり。`data-fetch` / `data-{event}-fetch` の応答反映と `data-{event}-bind`（リクエストを組み立てた時点までの編集のみ。それより後の編集は 0.28.0 と同じく保持）、`data-{event}-reset` / `data-{event}-reset-before`、`data-{event}-copy`、`data-each` の行の再利用で要素データが入れ替わったとき（その行の中だけ）、`Core.setBindingData()` の直接呼び出し。
  - 解除しない更新は次のとおり。`change` / `input` の双方向コミット、`data-url-param` の再評価（再評価ごとに走るため）、`data-poll` の応答反映（利用者が要求していない自動取得なので、これまでの編集をすべて応答へ上書きし直す）、`_poll` / `_fetch` / 可視範囲などエンジン管理変数の更新。
  - これにより、0.27.0 で案内した「式が参照するキーと `name` が書き込むキーを揃える」という制約は、編集値を守るためには不要になった。揃えることは依然として推奨（再取得の応答で編集前の値へ更新される点と、画面とバインドデータが一致する点で素直）だが、揃っていない既存画面の書き換えは不要。
  - フォーカス中の要素へ再適用しない従来の規則は変更していない。今回追加したのは「フォーカスが外れた後の、確定済みの編集」の保護。
  - `Core.setBindingData()` に第 6 引数 `userEditBaseline` を追加した（既定は現在の通し番号 = すべて解除、`null` は解除しない）。外部から呼ぶ場合は既定のままで「明示的な供給」として扱われる。

### Fixed

- **`data-attr-value` で表示した編集フォームで、次の項目を編集すると直前の項目が取得時の値へ戻る不具合を修正**（上記 Changed による）。レコードオブジェクトを参照する式で値を表示し、入力欄は `name` で平坦キーへ書き込む構成（`<input name="name" data-attr-value="{{detail?.name || ''}}">`）で、3 項目を順に入力すると最後の 1 項目しか残らなかった。画面表示だけでなく収集値も古くなるため、保存すると**利用者が気付かないまま編集が失われる**状態だった。引き金は `change` の双方向コミットが起こす再評価に限らず、無関係なキーの更新など**あらゆる再評価**だった。
- **`change` / `input` の双方向コミットが、祖先のバインドデータをフォーム自身へ焼き付ける不具合を修正**。コミット先の土台に「祖先とのマージ結果」（`ElementFragment.getBindingData()`）を使っていたため、一度編集するとその時点の祖先のキーがフォーム自身の `data-bind` へコピーされ、**以降その祖先を更新してもフォーム自身の古いコピーにシャドーされて届かなくなっていた**。一覧を更新したのに詳細フォームの表示が古いまま残る、レコードを空にしても入力欄が前のレコードの値を表示し続ける、といった形で現れる。土台をフォーム自身のバインドデータ（`getRawBindingData()`）に限定した。
  - 併せて、マージ結果のキャッシュオブジェクトへ直接 `Object.assign` していた問題も解消した（キャッシュを書き換えるため、同じフラグメントを参照する後続の評価にも影響していた）。
- **`data-form-arg` を指定したフォームで、双方向コミットが平坦なキーへ書き込む不具合を修正**。`Core.changeValue()` と `Form.reset()` は指定キーの配下へ書いていたのに、`change` / `input` のコミットだけが平坦に書いていた。指定キーを参照する式（`data-attr-value="{{detail?.name}}"` など）から編集値が見えず、**参照キーと書込キーを揃える設定にしても揃わない**状態だった。コミットも指定キーの配下へ書くようにした。
- **`data-{event}-reset` で宣言バインド由来の値が空にならない不具合を修正**。`form.reset()` は `value` / `checked` / `selected` 属性を既定値として復元するが、宣言バインドは評価結果をこれらの属性へ書くため、**「前回の評価結果」が既定値として復元されていた**。復元された値は続く値収集と双方向コミットでバインドデータへ固定されるため、編集ダイアログを閉じて新規追加を開くと前回の全項目が残留した。リセット時に宣言バインドで値・状態が決まる入力の DOM 側を空へ揃え、その後の再評価で現在の評価結果を入れ直すようにした。
  - 併せて、リセットがフォーム自身のバインドデータを初期 `data-bind` 宣言（宣言がなければ空）へ戻すようにした。従来は宣言が無いフォームで何もしなかったため、リセット前のコミットで書かれた値が残り、そのキーを参照する式が古い入力値を復元していた。
  - 再評価とバインドデータ更新の順序を入れ替えた。従来の順序では、宣言バインドで値が決まる入力について「収集した空の値」をバインドデータへ書いた後に DOM だけが評価結果へ変わり、画面とバインドデータが食い違ったまま残っていた。

### Added

- **未解決参照により `data-attr-*` の反映を見送ったことを開発モードで警告するようにした**。未解決参照のときは属性を削除し値も空へ揃えるが、画面上は「なぜか空になる」ようにしか見えず、原因の宣言に辿り着けなかった。属性名とテンプレートを名指しで報告する（同じ宣言は一度だけ。再評価ごとには出さない）。評価結果が素直に `null` / `false` になった場合は宣言どおりの動作なので報告しない。

### Docs

- `docs/ja/specs.md`: `data-attr-*` に「ユーザー編集と宣言バインドの権威」節を追加し、印の解除条件を表で明記した。`data-form-arg` にコミット先の記述、`data-{event}-reset` に手順の記述を追加した。未解決参照の規則が `src` / `href` に限らずすべての `data-attr-*` 対象属性へ適用されること、`?.` / `??` / `||` / `&&` の短絡で値が確定する式は未解決参照にならないこと、上記の開発モード警告を明記した。
- `docs/ja/guide.md`: 「`name` 付き入力では参照スコープを書込スコープに揃える」レシピを現行仕様へ更新した。巻き戻りが起きないこと、それでも揃えることを推奨する理由を明記した。

### Tests

- `tests/user-edit-priority.test.ts` を追加（10 件）。順に編集しても前の項目が保持されること（DOM 値と収集値の双方）、未編集の欄は依存値の変化で更新されること、選択とチェック状態も巻き戻らないこと、明示的な供給では評価結果が反映されること、`data-{event}-reset` で空になること、フェッチ応答が送信前の編集を更新して送信後の編集を残すこと、ポーリングでは編集が保持されること、コミットが祖先を焼き付けないこと、`data-form-arg` の配下へ書くこと、再利用行で行データが入れ替わると反映されることを検証する。
- `playwright/user-edit-priority.spec.cjs` と `playwright/user-edit-priority-repro.html` を追加（2 件、いずれも修正前に失敗）。実ブラウザは dirty value flag を持ち、jsdom とは値の権威の伝わり方が異なるため、実際に 3 項目を順に入力して確認する。
- `tests/attr-value-collection.test.ts`: 未解決参照で反映を見送ったことを開発モードで警告すること（1 件、追加前に失敗）と、短絡で値が確定する式（`{{detail?.id || dialog?.id || id || ''}}`）は未解決参照にならず `''` が反映されること（1 件、対照）を追加した。

## [0.28.0] - 2026-07-28

### Changed

- **`data-each` の行が行外データの更新へ追従するようにした（破壊的変更）**。一覧（`data-each` の配列）が同値のときは行の再評価そのものを省略していたため、**行の中から行の外を参照している式が更新されなかった**。行内で別の一覧を `data-each` で描画する構成や、行内で親スコープの値を参照する構成（`{{row.amount}}{{unit}}`）が該当する。省略の判定を「一覧が同値」から「一覧が同値 **かつ** テンプレートが行スコープの名前だけを参照している」へ変更した。
  - 行スコープの名前とは `data-each-arg` と `data-each-index` で公開されるものを指す。判定できない場合はすべて「行の外を参照している」とみなして再評価する（テンプレート未確定、`data-each-arg` の無い構成、解析できない式、既知でない `data-*` 属性）。
  - この結果、行の外を参照するテンプレートでは、祖先のバインド更新のたびに行の子孫が再評価される。省略が効かなくなるぶん描画コストは増えるが、更新が届かない状態を優先して解消した。行スコープだけで完結するテンプレート（`data-each-arg` を指定し、行データのキーだけを参照する構成）では従来どおり省略される。
  - 判定はテンプレート単位で不変なため、一度求めた結果を保存して再利用する。

### Fixed

- **行内の `data-each` が後から届いた選択肢を描画しない不具合を修正**（上記 Changed による）。一覧を別途取得して行内の `<select>` へ流し込む構成（行ごとの外部キー選択肢）で、行の描画が先に終わっていると選択肢が空のままになり、利用者が選べないまま**空の外部キーが送信される**状態だった。実ブラウザで確認したところ、選択肢は 42 件届いてスコープからも見えているのに `<select>` は `data-each-before` の 1 件だけで、無関係な項目を編集して行の要素データが変わって初めて 43 件が描画されていた。
- **送信後に行ったユーザーの編集が、遅れて届いた応答で静かに巻き戻る不具合を修正**。フェッチの応答はリクエストを組み立てた時点の内容を反映したものなので、その後の編集より古い。それをそのままフォームへバインドしていたため、送信中に入力した値が画面からも収集値からも消えていた。応答を `<form>` へバインドする直前に、送信データを確定した後に編集された入力欄の値だけをバインドデータへ上書きし直すようにした。
  - 判定は入力欄ごとの「ユーザー編集の通し番号」で行う。番号は `change` / `input` の委譲で内部値を DOM から同期した直後に発番するため、利用者の操作だけが対象で、`Core.setBindingData()` やバインド由来の書き戻しでは発番しない。
  - 上書きはバインドデータの段階で行う。入力欄への書き戻し（`Form.syncValues`）と宣言バインドの再評価（`data-attr-selected` など）はどちらもバインドデータを基準にするため、この 1 か所で両方が整合する。書き込み側だけを止める方式では、宣言バインドで選択状態を決める `<select>` の巻き戻りを防げない。
  - 上書きするのは編集された入力欄のキーだけなので、編集していない項目には応答の値がそのまま反映される（サーバが計算した合計や整形済みコードなどの派生値は失われない）。送信のきっかけになった編集はリクエストに含まれているため、応答の値が権威となる。
  - ラジオボタンは、排他で未チェックになった同名の兄弟もまとめて保護する。起点要素だけを保護すると、グループの一部だけが巻き戻ってチェック状態が食い違う。
  - `data-form-list` の行は位置合わせで対応付ける。応答が返した行数が変わっている場合、対応する位置が無い編集は反映されない。

- **入力要素へ付けた `data-form-list`（同名リスト）が機能しない不具合を修正**。仕様書が示す `<input name="tags" value="js" data-form-list>` の形が、収集でも書き戻しでも配列として扱われていなかった。
  - 収集は属性値の有無で判定していたため、値を省略した宣言（仕様書の例そのもの）が配列にならず、同名の入力が互いを上書きして**最後の 1 件だけ**が残っていた（`{"tags":"ts"}`）。キーは `name` が決めるので、属性の有無で判定するようにした。
  - 書き戻しは配列をそのまま 1 つの入力へ渡していたため、カンマ連結された 1 つの文字列が**同名の入力すべてに複写**されていた（`["js,ts","js,ts"]`）。同じ収集キーの出現順に配列の要素を配るようにした。出現順は収集時の並びと同じなので、収集 → 書き戻しで値の対応が保たれる。要素数より入力欄が多い場合、余りの入力欄は空になる。出現順は `values` が切り替わる単位（`data-form-object` の中、`data-form-list` の行ごと）で数え直す。
  - チェックボックスグループと複数選択 `<select>` は例外とし、`data-form-list` を併記していても従来どおり配列そのものを選択状態として解釈する（位置で配ると選択状態を決められないため）。
  - これに伴い `Form.syncRowValues()` の第 3 引数（行のインデックス）を削除した。出現順で対応付けるようになり、行のインデックスは配布に使わなくなったため（仕様書のパブリック API 一覧には含まれていないメソッド）。

- **`<form>` の named access で開発モードのバインド更新が例外になる不具合を修正**。HTML 仕様の named access により、`<input name="id">` を含むフォームでは `form.id` が文字列ではなく input 要素を返す。診断用の識別子を組み立てる箇所が `target.id.trim()` を呼んでいたため `TypeError: id.trim is not a function` となり、`Core.setBindingData()` ごと reject していた。`id` 属性から直接読み取るようにした。
  - 条件は「開発モード（`<script data-dev>`）」かつ「`data-derive` を持つ要素の祖先に `name="id"` の入力を含む `<form>` がある」。本番モードでは診断が早期 return するため発生しない。`Core.dumpScope()` のラベルにも同じ読み取りがあり、こちらは `#[object HTMLInputElement]` と表示されるだけだった。
  - 0.27.0 で式の識別子解決を window から切り離したが、ソース内の直接的な DOM プロパティ読み取りは対象外だった。今回その残りを塞いだ。
- **入れ子データをテキストで送る経路で `[object Object]` になる不具合を修正**。`data-form-list` の行データのようなオブジェクトの配列を GET のクエリ・`application/x-www-form-urlencoded`・`multipart/form-data` で送ると、配列の要素だけ `String()` に任せていたため `materials=%5Bobject+Object%5D` となり、サーバ側で復元できなかった。単一のオブジェクト値は以前から JSON 文字列にしていたため、配列の要素も同じ規則へ揃えた（要素ごとに JSON 文字列化し、同名キーを繰り返す）。
  - スカラーの配列は従来どおり値をそのまま同名キーの繰り返しで送る（`tags=js&tags=ts`）。
  - 配列要素の `null` / `undefined` は、これまでの `'null'` / `'undefined'` という文字列ではなく空文字になる。単一の値のときの扱いと揃えた。
  - 入れ子の構造を素直に送る場合は JSON body（既定の POST）を使う。テキストで送る経路は 1 つの値が 1 つの文字列になるため、構造を保つには要素ごとの JSON 文字列にするしかない。
- `package-lock.json` の版数が 0.23.0 のまま取り残されていたため 0.28.0 へ揃えた。0.24.0 以降の版数繰り上げで更新されていなかった。

### Tests

- `playwright/form-named-access.spec.cjs` と `playwright/form-named-access-repro.html` を追加。jsdom は named access の組み込み上書きを実装しないため、実ブラウザで確認する。修正前は `thrown: TypeError: i.id.trim is not a function` で失敗する。
- `tests/payload-nested-serialization.test.ts` を追加（3 件、いずれも修正前に失敗）。クエリと urlencoded での行データの JSON 文字列化、スカラー配列が従来どおりであることを検証する。
- `tests/form-value-list.test.ts` を追加（6 件、うち 5 件は修正前に失敗）。属性値の省略・明示の両方での収集、出現順の配布、要素数より入力欄が多い場合、`data-form-object` と `data-form-list` の行での数え直しを検証する。
- `tests/each-outer-scope-reactivity.test.ts` を追加（3 件）。行内 `data-each` の選択肢描画、行内の宣言バインドの再評価、行外更新後も選択が保たれることを検証する。
- `tests/user-edit-after-request.test.ts` を追加（5 件）。応答より後の編集が保護されること、送信前の編集は応答が権威であること、編集が無ければ応答がそのまま反映されること、宣言バインドの `<select>` とラジオグループでも保護されることを検証する。
- 追加した 8 件のうち 5 件は修正前に失敗する。残る 3 件は適用範囲が広がりすぎないことを守るガードで、修正前後のどちらでも通る。
- `tests/core.test.ts` の「同じ行データなら data-each の再利用行を再評価しない」を、テンプレートが行スコープだけを参照する構成へ改めた（従来のテンプレートは行外の `renderSpy` を参照しており、上記 Changed では再評価が正しい）。併せて「行の外を参照する行は行データが同値でも再評価する」を追加した。

## [0.27.0] - 2026-07-28

### Added

- **定期取得トリガー `data-poll-*` を追加**。一定間隔で手続きを繰り返し実行する専用トリガー属性で、別端末や別プロセスでの操作完了をサーバへ問い合わせ続ける「完了待ち」画面に使う。アクション語彙は `data-{event}-*` と共通のため、`data-poll-fetch` / `data-poll-data` / `data-poll-bind` / `data-poll-bind-arg` / `data-poll-bind-merge` / `data-poll-fetch-method` / `data-poll-fetch-state` などがそのまま使え、バインドの適用規則とエラーメッセージの振り分けも通常のイベント属性と同一。`data-intersect-*` と同じ「専用トリガー属性」の構成を踏襲している。
  - `data-poll-interval`: 取得間隔（ミリ秒）。省略時 5000、下限 100（下回る値と数値化できない値は補正し開発モードで警告）。初回は間隔を待たずに即時実行し、2 回目以降は**前回の手続き完了時点**から計測するため、応答が間隔より遅い場合もリクエストは多重化しない。
  - `data-poll-timeout`: 開始からの打ち切り時間（ミリ秒）。省略時は無制限。間隔タイマーとは独立に計測するため、バックグラウンドタブでタイマーが抑制されていても指定時刻に到達する。
  - `data-poll-until`: `{{式}}` が真になった時点で恒久停止。各リクエストの**実行前**と取得成功後の**バインド反映後**の両方で評価するため、初期状態で条件が成立している場合は 1 回も通信しない。式は対象要素自身のバインドスコープで評価する。未解決参照は「成立していない」として扱い停止しない（開発モードで警告）。
  - `data-poll-error-limit`: 連続失敗回数の上限に達したら恒久停止。省略時は無制限（回線の一時的な切断でポーリングが停止しないようにするため）。成功時に連続失敗回数は 0 へ戻る。HTTP エラー応答とネットワーク断のどちらも失敗として数える。
  - `data-poll-disabled`: 真の間は実行を抑止する一時停止。抑止中も `data-poll-timeout` の経過時間は進み、`data-poll-until` は評価しない。
  - `data-poll-state`: ポーリング状態 `_poll`（`running` / `paused` / `stopped` / `timedOut` / `stopReason` / `count` / `elapsedMs`）の注入先セレクタ。値省略で自要素。`data-fetch-state` と同様に `data-bind` 属性は汚さない。注入は状態遷移時のみ行い、リクエスト単位の `loading` / `success` / `error` は `data-poll-fetch-state`（`_fetch`）が担う。`data-poll-bind` は既定でバインド先を全置換するため、注入先が bind 先と同じ要素になる構成では取得成功のたびに `_poll` が失われる。これを検知した場合のみ書き戻すことで、毎周期の注入コストを避けつつ状態を維持する。
  - 恒久停止は `until` / `timeout` / `error` / `detached`（DOM からの除去）の 4 つで、`data-if` による非表示（祖先が非表示の場合を含む）と `data-poll-disabled` は条件が戻れば再開する一時停止として扱う。`data-if` は要素を DOM から削除せず祖先へ `data-if-false` を付与するため、非表示の判定は毎周期祖先方向を確認して行う（`MutationObserver` の属性通知は変更のあった要素自身にしか届かないため、配線に依存しない方式にしている）。
  - 停止はリクエストを中断しない。打ち切り時点で通信中だったリクエストの応答は通常どおりバインドまで処理されるため、`_poll.timedOut` が真になった直後に取得結果が反映されることがある。
  - タブが非表示から表示へ戻った時点（`visibilitychange`）で待機中の間隔を打ち切って即時実行する。バックグラウンドタブではブラウザがタイマーを大きく抑制するため、復帰直後の検知遅延を抑える補正。指定した間隔はバックグラウンドでは保証されない。
  - 登録の同一性は `data-poll-*` の**生の属性値**（テンプレート展開前）だけで判定する。評価値は呼び出しごとに再評価されるため（`data-poll-until` の `false` → `true`、`data-poll-data` の `{{...}}` など）、評価値を含めると設定変更と誤認して登録を張り直し、`data-poll-timeout` の起点がリセットされて永久に到達しなくなる。
- **`haori:polltimeout` / `haori:pollstop` イベントを追加**。`data-poll-timeout` への到達時と恒久停止時に発火する（打ち切り時は `polltimeout` → `pollstop` の順）。`detail` は `polltimeout` が `{count, elapsedMs}`、`pollstop` が `{reason, count, elapsedMs}`。`data-on` で宣言的にも受け取れる。停止理由が `detached` の場合は要素が既に切り離されているため発火しない。

- **`data-{event}-row-remove-empty` を追加**。`data-{event}-row-remove` の「最後の 1 行を残す」動作を解除し、0 件まで削除できるようにする。可変件数（0〜N 件）の入力に使う。既定を変えずオプトインとしたのは、「最低 1 行は消えない」前提で作られた既存画面の挙動を変えないため。
- **`data-form-name` を追加**。値収集のキーを宣言する属性で、`name` 属性の代わりに、または `name` と併記して使う。収集・逆方向同期・サーバのエラー応答の振り分けのすべてで、`data-form-name` があればそちらを収集キーとし、無ければ `name` を使う。
  - **主な用途は `data-form-list` の行内のラジオボタン**。HTML のラジオグループは「同じフォームオーナー内の同名要素」で構成されるため、行内で同じ `name` を使うと行をまたいで排他になり、1 行しか選択を保持できなかった（別の行を選ぶと前の行の選択が外れ、初期 `data-bind` からの復元も最後の 1 行しか反映されない）。`data-attr-name` で行ごとに DOM の `name` を変える回避策では値が収集されないため、行内でラジオボタンを使う手段が存在しなかった。
  - `data-form-name` を指定して `name` を書かなかった `input[type="radio"]` には、DOM の `name` を行ごとにユニークな値へ自動生成する。生成する `name` は同じ行の同じ収集キーで同一、行をまたぐと別の値になる（値の形式は内部仕様）。行の複製で引き継がれた生成済みの `name` は作り直す。
  - `name` を書いた場合は尊重して生成しない。行をまたぐ 1 グループになるため、「複数行の中から 1 行を選ぶ」構成（代表行の選択など）はこちらで表現する。
  - スコープは最近傍の `data-form-list` コンテナ直下の要素（= 行）。`data-form-object` を間に挟んでも行が単位になる。行の外では最近傍のフォーム（`<form>` または `data-form`）単位となり、通常の HTML と同じ排他になる。
  - 収集キーが空になる指定（`data-form-name=""`、評価結果が空文字・`false`・`null`、未解決参照）は、`name` があればそちらへフォールバックし、無ければその入力が値収集から静かに外れる。原因が追いにくいため開発モードで警告を出す。
- **`data-{event}-row-add` / `-row-remove` / `-row-prev` / `-row-next` に CSS セレクタ指定を追加**。`data-click-row-add="#list"` のように対象の `data-each` コンテナを直接指定でき、**行の外に置いたボタン**や**行が 0 件で複製元が存在しない状態からの追加**に対応する。値を省略した場合は従来どおり対象要素が属する行が対象。

### Changed

- **バインドに無いキーの参照を正常系として扱うようにした（破壊的変更）**。初期表示のようにまだ値が届いていない状態でキーを参照しても `error` ログを出さない。これまでは未解決参照のたびに `console.error` を出力していたため（`Log.error` は開発モード判定を通らないので本番でも出力されていた）、エラーを消す目的だけで `data-bind` にキーを種まきする定型作業が必要だった。診断は開発モード限定へ変更し、レンダリングキューが安定した時点で「一度も供給されなかったキー」だけを 1 件の `warn` に集約する（後から `data-fetch` や `data-bind` で解決したキーは報告しない）。検出時点で `error` を出す従来の挙動は `<script src="haori.js" data-strict-bind>` でオプトインできる。
- **メンバーアクセスを暗黙のオプショナルチェーンとして評価するようにした（破壊的変更）**。式の `a.b` は `a?.b`、`a[i]` は `a?.[i]`、`a.b()` は `a?.b?.()` として評価する。途中の値が `null` / `undefined` でも `TypeError` にならず結果は `undefined`（未解決参照）になるため、`{{view?.totalCount}}` のような `?.` や `data-bind='{"view":{}}'` のような宣言を書く必要がなくなった。値が存在しても呼び出せない場合（`a.b` が数値なのに `a.b()`）は従来どおり `TypeError` になる。既存の `?.` 記述はそのまま動作する（二重には適用しない）。
  - これに伴い、評価結果が `undefined` になる参照はすべて未解決参照として扱う（従来は `undefined` と未解決参照を区別していた）。`src` / `href` などブラウザ先行解釈属性の**文字列埋め込み**では、未解決参照が 1 つでもあると属性全体が未反映になるため、`undefined` を含む式の結果が変わる。
  - 遮断された識別子・プロパティ（`Object` / `Reflect` / `constructor` など）へのアクセスも例外ではなく `undefined` になる。遮断そのものは変わらないが原因が見えにくくなるため、遮断された識別子を参照している式には開発モードで警告を出力する。
- **バインドに無いキーを含む「判定する式」は結論を出すようにした**。バインドに無いキーを `undefined` とみなして評価し直した結果が真偽値になる場合だけ、その値を採用する。`data-if="!_fetch.status"` は `true`、`{{count > 0}}` は `false` になり、宣言もオプショナルチェーンも書かずに「まだ何も起きていないとき」の表示が成立する。文字列連結（`'x' + missing`）や算術（`a + b`）は真偽値にならないため採用せず、`'xundefined'` や `NaN` を描画することはない。
- **行操作（`data-{event}-row-*`）をバインディングデータの配列操作へ変更した（破壊的変更）**。従来は行の DOM を複製・削除・移動するだけで `data-each` が参照する配列を変更しなかったため、DOM とバインディングデータが不整合になり、次のバインド更新（`data-fetch` 結果の反映など）で差分更新が古い配列から再描画して**追加・削除・並べ替えが黙って取り消される**問題があった。複製時に `data-row` キーが重複する問題も併せて解消した。
  - 追加する行は空のオブジェクト（`{}`）を挿入する。従来の「複製元の行を複製してフォーム要素をリセットする」方式から変わるが、仕様書に記載していた「追加された行のフォーム要素は空の状態にリセットされます」と一致する挙動になる。
  - 対象配列は `data-each` の式を単純な識別子パス（`contracts` / `form.contracts` など）とみなし、根の識別子を持つ最も近い祖先（自身を含む）のバインディングデータを所有者として解決する。関数呼び出しや演算を含む式は書き戻し先を一意に決められないため、エラーログを出して何もしない。
  - 行操作は再描画を経るため、操作完了までの非同期の段数が増えた。E2E やテストで操作直後の DOM を確認する場合は、描画確定を待つ必要がある。
- **`data-form-list` の行が 0 件のとき、収集結果にキーを空配列として出すようにした（破壊的変更）**。従来はキー自体が出力されず（`{}`）、サーバ側で「0 件」と「そのフィールドが未送信」を区別できなかったため、全件削除を表現できなかった。今後は `{"contracts":[]}` を出力する。
- **`<form>` の初期 `data-bind` を入力欄へ反映するようにした（破壊的変更）**。初期スキャンの完了時に一度だけ逆方向同期を適用する。これにより `name` と `data-bind` のキーが一致する `<select>` やチェックボックスは、`value="{{...}}"` や `data-attr-selected` / `data-attr-checked` を書かなくても初期表示で選択状態になる。`data-bind` に含まれないキーの入力欄は HTML の `value` 属性で与えた初期値が保たれる（「未指定のキーは既存の入力値を維持する」規則）。`data-bind` に空文字を置きつつ `value` 属性を初期値として使っていた構成では入力欄が空になるため、初期値は `data-bind` 側へ寄せる必要がある。復元は初回スキャン時のみで、`data-if` の表示切替などで再スキャンされても繰り返さない（利用者の編集を初期値へ巻き戻さないため）。
- **`data-attr-value` が `value` の DOM プロパティを同期するようになった（破壊的変更）**。仕様書は 0.16.0 の時点で「`data-attr-*` も `value` の DOM property を同期する（フォーカス中の入力を除く）」と定めていたが、実装が追随していなかったため（下記 Fixed 参照）、`data-attr-value` は属性だけを更新していた。`name` を持つ**編集可能な**入力へ `data-attr-value`（または `value="{{式}}"`）を併用している画面では、値の権威がバインドの評価結果になるため、フォーカスが外れた後の再評価で**利用者のコミット済み入力が評価結果へ巻き戻る**。式が参照するキーと `name` が書き込むキーを揃えるか、`data-attr-value` を使わず `name` 束縛に任せてください（ガイドの「`name` 付き入力では参照スコープを書込スコープに揃える」を参照）。`readonly` の入力や hidden、`name` を持たない入力では衝突しません。
- **式の識別子をグローバルへ解決しないようにした（破壊的変更）**。`window.someKey = ...` のようにグローバル変数を定義して式から参照する構成は動作しなくなり、未解決参照として扱われる。バインディングデータを唯一の参照元とするための変更で、`id` 属性由来の named access による誤解決（下記 Fixed 参照）を断つのと同じ仕組みによる。値は `data-bind` で宣言するか、`data-{event}-run` など実 JS 実行の属性から扱ってください。`Math` / `JSON` / `Array` などの標準組み込みは従来どおり参照できる（仕様書に一覧を明記）。
- `Observer` の属性変更・ノード追加・ノード除去の各経路へ `PollObserver` の同期処理を追加した（`IntersectObserver` / `VisibleRangeObserver` と同じ配線）。これにより `data-import` などで後から挿入された `data-poll-*` も監視対象になり、DOM から除去された要素のタイマーは確実に破棄される。属性変更経路は全要素に対して呼ばれるため、`data-poll-*` を持たず登録も無い要素は DOM の属性を直接走査して早期に抜ける（`getAttributeNames()` の配列確保を避ける）。

### Fixed

- **非ブラウザ環境で `src/env.ts` の読み込みが失敗する不具合を修正**。モジュール末尾の起動処理が `try` の外で `document.readyState` を参照していたため、SSR やモジュール単体の import で `ReferenceError: document is not defined` になっていた。`typeof document` で保護した。
- **`data-each-arg` と `data-each-index` を併用すると `data-each-index` が解決できない不具合を修正**。オブジェクト要素の場合にインデックスを要素データの**内側**へ入れてから `data-each-arg` で包んでいたため、行のバインドが `{arg: {..., i: 0}}` となり `{{i}}` が未解決参照（ReferenceError）になっていた。プリミティブ要素の分岐は順序が正しく、`data-each-arg` を指定しない場合も正常だったため、両者を併用したときだけ発生した。インデックスを包んだ外側へ置くよう修正した。併せて、要素データがインデックスキーで汚染される（`{"c":{"name":"A","i":0}}`）問題も解消した。この汚染は双方向バインドの書き戻しや差分比較へ混入していた。
- **`data-each` で新しく生成された行の入力欄に値が入らない不具合を修正**。`Core.setBindingData` の逆方向同期（`Form.syncValues`）は `Core.evaluateAll`（= `data-each` の行生成）より**前**に走るため、その更新で生成された行には値が反映されていなかった。`data-fetch` でリストを取得しても行内の `<select>` やチェックボックス、テキスト入力に値が入らず、同じデータで 2 回更新すると 2 回目に初めて入るという状態だった。行の生成・更新に合わせて行単位でも値を反映するようにした。適用するのは「新規生成した行」と「要素データが変化した再利用行」だけで、変化していない行へは再適用しない（描画待ちの間の利用者の編集を巻き戻さないため。0.26.1 で修正した問題と同種）。
  - 行単位の反映では、要素データに無いキーの入力欄は空にする。要素データが行全体を規定するため、行の途中への挿入や並べ替えで担当する要素が変わったときに前の行の入力値が残らないようにするためで、フォーム全体への逆方向同期（未指定のキーは既存値を維持）とは扱いが異なる。
- **上記の帰結として、1 項目の変更で行内の他項目の値が失われる不具合を修正**。復元されていない `<select>` やチェックボックスが、最初の `change` による全項目収集で空値として確定し、バインディングデータを上書きしていた。テキスト入力は `value="{{...}}"` があれば保たれるため、選択系の項目だけが静かに失われていた。
- **`data-attr-value` で設定した値がフォーム収集に載らない不具合を修正**。`data-attr-*` の反映経路（`ElementFragment.setAliasedAttribute`）が DOM プロパティ同期を無効にして呼び出していたため、`value` 属性は更新されるのに `element.value` と内部値（値収集や式評価が参照する値）が更新されず、送信データが空になっていた。仕様書は 0.16.0 の時点で「`data-attr-*` も `value` の DOM property を同期する（フォーカス中の入力を除く）」と定めていたが、実装は直接属性へ `{{...}}` を書いた経路にしか対応していなかった。`type="hidden"` に限らずテキスト入力でも発生し、`value` 属性を持たない `<textarea>` と `<select>` では DOM 値そのものが反映されていなかった。
- **`type="hidden"` へ `value="{{式}}"` を書くと、評価結果ではなくテンプレート文字列そのものが収集・送信される不具合を修正**。DOM プロパティ同期の対象判定に `input` イベントを発火する型の一覧（`INPUT_EVENT_TYPES`）を流用しており、`hidden` が含まれていなかった。このため内部値が初期スキャン時の DOM 値（= 未評価のテンプレート文字列）のまま残っていた。`hidden` は利用者が編集できず送信される値を持つため、常に同期対象とした。
- **`data-form-list` の行内で、宣言バインドの値・状態が行単位反映によって消える問題を修正**。行の要素データに該当キーが無い場合に空へ上書きしていたため、行ごとの hidden へ `data-attr-value` で値を載せる構成などで値が失われた。宣言バインドで値・状態が決まる入力は、その権威がバインドの評価結果にあるため上書き対象から除外した。判定する属性は要素の種類で変わり、テキスト系 / hidden / textarea / select は `value="{{式}}"` と `data-attr-value`、checkbox / radio は `checked="{{式}}"` と `data-attr-checked`、select は配下の `<option>` の `selected="{{式}}"` と `data-attr-selected` を見る。チェック系の `value` は送信値であってチェック状態ではないため判定に使わない（使うと、送信値をテンプレート式で決めているだけのチェックボックスが空化されず、前の行のチェック状態が残る）。要素データにキーが**ある**場合は従来どおり要素データを優先する。
- **宣言バインドの評価結果が値を持たなくなったとき、収集値に旧値が残る不具合を修正**。評価結果が `false` / `null` / `undefined`、または未解決参照になった場合は `value` 属性を削除するが、内部値（値収集や式評価が参照する値）を空にしていなかったため、画面には何も表示されていないのに旧値が送信されていた。属性削除に合わせて `element.value` と内部値も空へ揃えるようにした（フォーカス中の要素は対象外）。`type="hidden"` は `value` プロパティへの代入が content attribute へ反映される HTML 仕様のため、属性は削除ではなく `value=""` として残る。
- **バインドに無い識別子が `window` 経由で解決される不具合を修正**。評価関数のスコープチェーンがグローバルまで届いていたため、未解決参照になるべき識別子が「値のある参照」に化けていた。`id="agencyCode"` の要素があると `{{agencyCode}}` がその要素オブジェクトへ解決され（HTML の named access on window）、`data-attr-value` に書くと入力欄へ `[object HTMLInputElement]` が書き込まれて `required` の必須検証も通ってしまう状態だった。`{{name}}` `{{status}}` `{{length}}` `{{origin}}` のような `window` の既存プロパティも同様に解決され、`unresolvedReference` が立たないため `data-if` の判定や `data-fetch` の未実行判定も効いていなかった。式に現れる識別子のうちバインドに無いものを評価関数のスコープ内へ宣言して遮蔽し、参照された場合は従来の「バインドに無いキーを参照した」場合と同じ扱い（未解決参照）にした。`?.` / `??` / `||` / `&&` を含む式で `undefined` として評価を続ける挙動、短絡評価や三項演算子で参照されなかった識別子を未解決にしない挙動はいずれも変わらない。`Math` / `JSON` / `Array` などの標準組み込みは従来どおり参照できる（仕様書に一覧を明記）。**意図せずグローバルへ解決されていた式に依存している画面では、その参照が未解決参照（空表示）になる**ため、値はバインディングデータ側から供給する必要がある。同梱デモにもこの依存が多数あり（`window.username = ...` のようなグローバル変数の定義や、`<div id="result">` の内側での `{{result}}` 参照）、あわせて修正した（下記 Docs 参照）。`{{result}}` の例は、修正前は id 由来の named access で div 要素へ解決され、画面に `[object HTMLDivElement]` と表示されていた。
- **`data-runtime="demo"` の通信の正規化が `data-{event}-before-run` の上書きで打ち消される不具合を修正**。demo ランタイムは非 GET をクエリ付き GET へ正規化するが、`before-run` が返す `fetchOptions` の適用が正規化より後だったため、上書きでメソッドや body を指定すると実 POST が送信され、静的ファイルサーバから `405` が返っていた。正規化のログとイベント（`transportMode: 'query-get'`）は出るのに実際の送信は非 GET という不整合もあった。正規化を独立した処理へ切り出し、`before-run` の上書き後に送信直前で再適用するようにした。すでに正規化済みなら何もしないため通常経路への影響はない。上書きされた body は JSON オブジェクト文字列・`application/x-www-form-urlencoded` 形式の文字列・`URLSearchParams`・`FormData`（文字列値のみ）であればクエリへ移し、`Blob` などクエリ化できない内容は破棄して開発モードで警告する。`haori:fetchstart` の `requestedMethod` は上書き後に要求されたメソッドを報告する。上書きの body は送信データの**置き換え**として扱い、`data-{event}-data` / `data-{event}-form` から正規化でクエリへ移した値は引き継がない。再正規化を正規化前の URL から行うためで、embedded ランタイムで body ごと差し替わるのと挙動が揃う。これをしないと置き換えではなく追記になり、同じキーを指定すると `?a=1&a=1` のようにクエリが二重になっていた。`data-{event}-fetch` の URL にもともと書いてあるクエリは残り、ヘッダーだけを差し替えるなど body を伴わない上書きでは正規化済みのクエリを保つ。置き換えとして扱うのは上書きの body が実際にクエリへ移る場合だけで、メソッドを `GET` のままにして body を付けた上書き（fetch 自体が `TypeError` になる組み合わせ）では収集済みのクエリを落とさない。
- **`data-each-key` を指定した `data-each` で、配列を並べ替えても DOM の行順が追従しない不具合を修正**。差分更新が再利用行を新しい並び順の位置へ移動していなかった。`data-each-key` 指定時は並べ替えだけの更新でキーが変わらず全行が再利用されるため、DOM の順序が古いまま残っていた（`data-each-key` を指定しない場合はキーがインデックスのため症状が出なかった）。`data-fetch` で並び替えた配列を返す構成にも影響していた。併せて、削除対象のフラグメントを挿入位置の参照に使わないようにした。

### Docs

- `docs/ja/specs.md`: 「暗黙のオプショナルチェーン」「未解決参照の診断」「判定する式と値を求める式」の各節を追加。「バインドに無い識別子の扱い」を未解決参照が正常系である前提へ書き換え、未解決参照の用語定義に「評価結果が `undefined` になる参照」を含めた。環境検出に `data-strict-bind` を追加。`<title>` への実行時バインド・`data-url-param`・`input[type=file]`・`_fetch` / `_poll` の各節から、宣言とオプショナルチェーンを必須とする記述を削除。
- `docs/ja/guide.md`: `<title>` バインド・ファイル選択の有無判定・`data-fetch-state`（`_fetch`）・`data-poll-state`（`_poll`）・`data-url-arg` の説明と例から、エラー回避のための `data-bind` 宣言と `?.` を削除。
- デモ: 初期表示のエラーを消すためだけに置いていた `data-bind` の種まきを 10 ページから削除。`demo/index.html` はスコープ確立のみを目的とした `data-bind="{}"` に置き換え、`demo/fetch/data-fetch-state-demo.html` は宣言とオプショナルチェーンを両方外して新しい仕様のまま動作することを示す構成へ変更。
- `docs/ja/specs.md`: 「定期取得トリガー (`data-poll-*`)」節を追加し、各属性・実行タイミング・停止と一時停止の区別・定期実行と相性の悪い修飾子（`confirm` / `toast` / `dialog` / `history` / `scroll`）を明記。イベントシステムに「定期取得」節を追加。モジュール依存関係と Observer の監視対象に専用トリガーの同期を追記。
- `docs/ja/guide.md`: 「一定間隔で処理を実行する（`data-poll-*`）」節を追加し、完了待ち画面の実例と、バックグラウンドタブでのタイマー抑制に関する注意を記載。
- `_poll` の参照方法を明記。注入先は `_poll` を参照する要素の祖先にする必要があること（式は祖先方向へ辿って解決するため）、初期表示時点では `_poll` が存在しないが未解決参照として扱われるため宣言もオプショナルチェーンも不要であること（`_fetch` と同じ作法）、`false` はテキスト補間では空文字列になるため真偽の出し分けには `data-if` を使うこと。
- `README.md` / `README.ja.md`: 主な機能とイベント駆動アクションの一覧へ `data-poll-*` を追記。
- `docs/ja/specs.md`: 「バインドに無い識別子の扱い」節を追加し、グローバルへ解決しないこと、参照時の扱い（`ReferenceError` と未解決参照）、`?.` / `??` / `||` / `&&` と短絡評価での扱い、式から参照できる標準組み込みの一覧を明記。セキュリティレイヤーへも追記。`data-url-param` の「未定義キーの直接参照を避ける」へ、グローバルへ解決されない旨の参照を追加。
- `docs/ja/specs.md`: `data-{event}-before-run` / `data-{event}-after-run` の記述を補強。`before-run` の引数（`fetchUrl` / `fetchOptions`）、`after-run` の第 1 引数が**解析前の `Response`** であり本文を読むには `clone()` が必要であること、いずれも `this` が起点要素では**ない**こと（起点要素になるのは `data-{event}-run` だけ）を明記。`before-run` で `fetchUrl` を上書きすると収集済みの送信データから組み立てたクエリが引き継がれないことも追記。`after-run` の例が解析済みデータを受け取るように読めたため、`status` の参照と `clone()` を使う例へ差し替えた。
- `docs/ja/specs.md`: 「demo ランタイムでの通信の正規化」節を追加し、正規化の内容（メソッド・送信データ・`Content-Type`）、対象が `data-runtime="demo"` のみであること、ログとイベントでの確認方法、`before-run` の上書き後にも再適用されること、クエリ化できる body の形式を明記。あわせて、上書きの body が送信データの置き換えであること（URL にもともと書いたクエリは残り、body を伴わない上書きでは正規化済みのクエリを保つこと）を「demo ランタイムでの通信の正規化」と `data-{event}-before-run` の両節へ追記。
- 同梱デモがグローバル変数（`window.username = ...` など）や id 由来の named access に依存して式を解決していたため、仕様に沿った書き方へ修正した。`data-bind="haori"`（JSON ではない不正な値）と `window.haori = {...}`（予約名前空間との衝突）を廃止する、`data-url-param="user"` を仕様どおりの `data-url-param data-url-arg="params"` + `{{params.user}}` にする、といった変更を行っている。対象は `demo/index.html`、`demo/click/` の 6 ページ、`demo/fetch/` の 4 ページ、`demo/if/data-if-false-demo.html`、`demo/row/data-row-demo.html`、`demo/url/data-url-param-demo.html`。
- `demo/if/data-if-false-demo.html`: `window.haori` を直接書き換える JavaScript でのトグルを、`data-click-data='{"show":{{!show}}}'` と `data-click-bind` による宣言的な切替へ変更した（外部からバインディングデータを変更する公開 API は無いため、元の実装は動作していなかった）。
- `demo/each/data-each-pagination-repro.html`: グローバル関数 `Pagination.pages()` を組み込みヘルパー `haori.pages()` へ置き換えた。省略記号の要素は `page` が `null` になるため `data-each-key` の指定は外している。
- `demo/fetch/data-fetch-bind-demo.json` / `data-fetch-bind-params-demo.json`: 応答が `{"items":[...]}` で、画面が参照する `{{name}}` / `{{age}}` と食い違っていたため、画面に合わせた内容へ修正した。
- `demo/fetch/data-fetch-state-demo.html`: 0 件時に `data-each` の行テンプレートが行スコープ無しで評価されるため、行スコープのキーへ `?? ''` を付けた。`<pre>` 内のサンプルコードも同様に修正している（文字参照で書いた `{{...}}` もテキスト補間として評価されるため）。
- `demo/poll/data-poll-approval-demo.html`: 完了待ち画面のデモを追加（取得間隔・打ち切り時間・停止条件・一時停止・状態表示）。取得先 URL にテンプレート式を含めることで、生の属性値が変わらなければタイマーの起点がリセットされないことも確認できる。`demo/index.html` へ導線を追加。
- `docs/ja/specs.md`: 「`data-form-name`」節を追加し、収集キーの解決規則、行内ラジオボタンで DOM `name` を自動生成する条件とスコープ、`name` を書いた場合の扱い、`data-attr-name` では収集されないことを明記。
- `docs/ja/guide.md`: 「レシピ: 繰り返し行の中でラジオボタンを使う（`data-form-name`）」を追加し、「行ごとに独立して選ぶ」構成と「複数行から 1 行だけ選ぶ」構成の書き分けを記載。
- `docs/ja/specs.md`: 「初期 `data-bind` からの入力欄復元」「`data-each` で生成された行への値反映」「行操作の共通仕様（`data-{event}-row-*`）」の各節を追加。`data-each-index` の解決位置、`data-form-list` の 0 件収集を明記。`data-attr-*` の DOM プロパティ同期対象へ `type="hidden"` を追記し、値の権威がバインドにあること、属性削除時は値も空へ揃えること（`type="hidden"` では属性が `value=""` として残ること）、行単位反映が宣言バインドの値・状態を空にしないこと（判定する属性の対応表とチェック系で `value` を見ない理由）を明記。
- `docs/ja/guide.md`: 行操作の節へセレクタ指定・`row-remove-empty`・配列操作であることを追記。「`name` 付き select / checkbox では参照スコープを書込スコープに揃える」のレシピを `data-attr-value`（テキスト入力）へ拡張し、`value` については 0.27.0 から同じ巻き戻りが表面化することを追記。
- `demo/form/data-form-list-editable-demo.html`: 可変件数（0〜N 件）の明細を入力するデモを追加（初期値復元・行の追加/削除/並べ替え・行の外の追加ボタン・0 件表示）。`demo/index.html` へ導線を追加。

### Tests

- `tests/expression-unresolved-reference.test.ts`: 未解決参照でエラーを出さないこと、本番では警告も出ないこと、`null` 経由のプロパティ・添字・メソッド呼び出しが未解決参照になること、解決できる式と明示的な `?.` が壊れないこと、アロー関数の引数リストを呼び出しと誤認しないこと、判定する式が結論を出し値を求める式は未解決参照のままであること、集約警告が「一度も供給されなかったキー」だけを報告すること、厳格バインドモードで即時エラーになることを検証（15 件）。
- `playwright/unresolved-reference-initial.spec.cjs`: 実ブラウザで、宣言なしの初期表示がエラーを出さず空表示になり値の到着で反映されること、`_fetch` をオプショナルチェーンなしで参照しても状態表示（初期・成功・失敗）が成立することを検証。
- `tests/poll.test.ts`: 即時実行と間隔の繰り返し、応答が間隔より遅い場合の多重化防止、設定用属性のみでは監視しないこと、間隔の下限クランプ、`data-poll-until` の成立停止・初期成立での無通信・未解決参照での継続、`data-poll-timeout` の到達とイベント発火、`until` の評価値が変化しても打ち切りへ到達すること（登録の同一性判定に評価値を使うと打ち切りに到達しなくなる退行の検出）、祖先 `data-if` の非表示での一時停止と再表示での再開、`data-poll-disabled` の抑止と復帰、DOM 除去での恒久停止、エラー時の既定継続と `data-poll-error-limit` での停止、成功による連続失敗回数のリセットを検証。
- `playwright/poll-approval-wait.spec.cjs`: 実ブラウザで `until` 成立での停止、`timeout` での打ち切り、`data-if` 非表示での一時停止と再表示での再開を検証（応答は `page.route()` で差し替え）。
- `tests/each-form-list-editable-rows.test.ts`: 初期 `data-bind` からの select / チェックボックス / テキストの復元（`data-each` 行内・単純フォーム）、`data-bind` に無いキーの `value` 属性保持、1 項目の change で他項目が失われないこと、`setBindingData` で生成された行への値反映、行の途中への挿入で以降の行に前の値が残らないこと、`data-each-arg` 併用時の `data-each-index` 解決と要素データ非汚染、`row-add` / `row-remove` の配列反映、既定の 1 行下限と `row-remove-empty` での 0 件到達、行の外のボタンからの 0 件時追加、`row-prev` / `row-next` の並べ替え、追加行が後続のバインド更新で取り消されないこと、0 件収集の空配列、`data-each-key` 指定時の並べ替え追従を検証（修正前は 17 件中 15 件が失敗）。
- `playwright/editable-rows.spec.cjs`: 実ブラウザで初期値の復元、行の追加と行番号の振り直し、追加行への入力反映、並べ替え、0 件までの削除、0 件からの追加を検証。
- `tests/row-operations.test.ts`: 行操作が再描画を経るようになり非同期の段数が増えたため、複数操作を連続実行するケースの待機を `waitForCondition` による確定待ちへ変更した（アサーションは変更なし）。
- `tests/procedure-action-operations.test.ts`: 初期 `data-bind` の復元により「`data-bind` と入力欄の値が初期から食い違う」前提が作れなくなったため、`data-click-copy` の順序検証で使う収集値を初期化後に設定するよう変更した（検証意図は変更なし）。
- `tests/fetch_error_array.test.ts`: 再試行のケースで、1 回目の実行ロック（実行中のボタンへ付与される `disabled`）の解放を待ってから再クリックするよう修正した。メッセージ表示は処理完了より前に起きるため、待たずにクリックするとロック中の click が無視されて 2 回目が発火せず、負荷時に失敗することがあった（試行回数を広げても直らない同期漏れ）。判定内容は変更なし。
- `tests/attr-value-collection.test.ts`: `data-attr-value` による hidden / テキスト / textarea / select の値反映と収集、他項目の change での値保持、`type="hidden"` へのテンプレート式の評価結果収集とバインド更新への追従、`data-form-list` 行内での宣言バインド値・状態の保持（`data-attr-value` / `checked="{{式}}"` / `data-attr-checked` / `option` の `data-attr-selected`）、チェック系の `value` は例外にせず空化されること（リテラル `value` との対照）、評価結果が `null` / 未解決参照になった場合に収集値も空になること、`name` 併用時に宣言バインドが優先されることを検証（17 件のうち 15 件は修正前に失敗し、1 件はチェック系を例外にした場合に失敗する。残る 1 件は空化規則が生きていることの対照）。あわせて報告と同じ経路（`data-url-param` 由来の値を hidden の `data-attr-value` へ載せ、`data-{event}-form` で送信する）を end-to-end で検証するケースを追加した。`Form` の収集だけでなく送信ペイロードまで固定するため、Procedure 側の収集経路の退行も検出できる（修正前は失敗する）。同キーを持つ `data-{event}-data` がフォーム収集値を上書きすることも併せて固定した。
- `tests/form-name.test.ts`: `data-form-name` の収集キー解決（`name` 無し・`name` 併記・逆方向同期・キー検索・評価値が空のときの警告とフォールバック）、`data-form-list` 行内での行ごとの独立した選択・DOM `name` の行単位生成・初期 `data-bind` からの行ごとの復元・後から追加した行の独立・行データにキーが無い場合の解除・同一行の複数グループの分離・`data-form-object` 配下でも行スコープになること、`name` を書いた場合に生成せず行をまたぐ 1 グループになること、行操作（並べ替え・削除）で選択が行データに追従すること、行の外ではフォーム単位の排他になることを検証（うち 9 件は追加前に失敗する）。
- `tests/expression-global-shadowing.test.ts`: バインドに無い識別子が `window` の named access（同名 id の要素）や既存プロパティ（`name` / `status` / `length` / `origin`）へ解決されず未解決参照になること、`??` でフォールバックが効くこと、バインドに同名キーがあればその値が優先されること、識別子名を含むエラーログが出ることを検証。あわせて遮蔽が式を壊していないこと（標準組み込み `Math` / `JSON` / `Array` / `String` / `Number` / `parseInt` / `isNaN` / `encodeURIComponent`、アロー関数の引数、リテラル、オブジェクトリテラルのキーとショートハンド、短絡評価・三項演算子で参照されない識別子、`haori` の組み込みヘルパー）と、`data-attr-value` へ要素オブジェクトが書き込まれず `required` の必須検証が働くことを検証（17 件のうち 8 件は修正前に失敗し、残りは壊れていないことの対照）。jsdom は window の named access を実装しないため、同じスコープ解決になるグローバルを定義して再現している。
- `tests/demo-runtime-normalization.test.ts`: `data-{event}-before-run` が `fetchOptions` でメソッドや body を上書きしても、demo ランタイムではクエリ付き GET へ再正規化されること、上書きの body が送信データの置き換えになること（同じキーでもクエリが二重にならない、URL にもともと書いたクエリは残る、body を伴わない上書きでは正規化済みのクエリを保つ、メソッドを `GET` のままにした上書きでは置き換えとして扱わない、`fetchUrl` も上書きした場合はその URL を尊重する）、`URLSearchParams` の body もクエリへ移ること、クエリ化できない body を破棄して警告すること、`haori:fetchstart` が上書き後のメソッドを `requestedMethod` として報告することを検証。embedded ランタイムでは上書きしたメソッドと body がそのまま送られること、`before-run` が無い場合の正規化が変わらないことを対照として確認（11 件のうち 3 件は置き換え対応前に失敗する。メソッドを `GET` のままにした上書きのケースは、置き換えの適用範囲が広がりすぎないことを守るガードで、対応前後のどちらでも通る）。
- `tests/procedure-fetch-options.test.ts`: fetch や `setBindingData` の呼び出しを固定時間（50ms）で待っていた箇所を確定待ちへ変更した。負荷が高いと呼び出し前に進んで `mock.calls` が空のまま参照され、`preserves false in parameter-style data-fetch-data templates` や `bindArg binds response under key` が散発的に失敗していた（テストファイルの追加で並列実行の負荷が上がり表面化した）。「呼ばれないこと」を確認する箇所は性質上そのまま固定待機とした。判定内容は変更なし。
- `tests/core.test.ts`: `data-attr-value` の DOM プロパティ同期を検証するテストを現行仕様へ更新した。0.16.0 で「フォーカス中を除き `value` の DOM property も同期する」と仕様が変更された際に更新されておらず、変更前の前提（属性だけを更新する）を検証していたため、実装側の対応漏れを隠していた。フォーカス中は再適用しないケースのテストを追加。

## [0.26.2] - 2026-07-26

### Fixed

- **チェック状態の収集値が画面の見た目と食い違いうる問題を修正**。`value="true"` / `value="false"` の boolean チェックボックスだけが、収集値の根拠を DOM の `checked` ではなく内部値（`ElementFragment` が保持する値）に置いていた。内部値はバインドからの書き戻しで先に更新されて DOM 反映が描画キュー待ちになるなど、DOM と食い違う瞬間があるため、そこで収集すると「画面はチェック済みなのに送信値は `false`」という不一致が起こりうる。同名グループのチェックボックス・ラジオはすでに DOM を真としており（0.24.0 のラジオ群修正）、扱いを揃えて boolean チェックボックスも DOM の `checked` を真として収集するようにした。`value="false"` の反転指定（checked で `false`）も同じ経路で扱う。
- **宣言バインドで `checked` / `selected` を書き換えたとき、内部値が DOM に追従しない問題を修正**。`value` 属性の同期は内部値も更新していたのに対し、`checked` / `selected` は DOM プロパティだけを書き換えていたため、`checked="{{式}}"` / `data-attr-checked` / `data-attr-selected` でチェック状態を変えると内部値が古いまま残り、式評価や値収集が画面と異なる値を参照する経路があった。DOM への反映に合わせて内部値も同期するようにした（`data-attr-selected` は所属する `<select>` の内部値を同期する）。フォーカス中の要素は従来どおり再適用をスキップするため、内部値の同期も行わない。

### Docs

- `docs/ja/specs.md`: 「チェック状態の収集は DOM を真とする」節を追加し、その理由（内部値が DOM と食い違う瞬間）と宣言バインド時の内部値同期を明記。`value="false"` の反転指定を追記。

### Tests

- `tests/checkable-value-consistency.test.ts`: 内部値が古い状態での収集、`value="false"` の反転指定、グループ扱いチェックボックスの従来動作（回帰）、バインド書き戻し後の一致、宣言バインド（`data-attr-checked` / `data-attr-selected` / `checked="{{式}}"`）での内部値追従を検証。

## [0.26.1] - 2026-07-26

### Fixed

- **連続して入力を操作したときに、後から操作した入力の値が古い収集値で巻き戻る不具合を修正**。`Core.setBindingData` のワークは `data-bind` 属性の書き込み（`Queue` = requestAnimationFrame バッチ）を待ってから入力欄へ書き戻すが、書き戻す値としてワーク**開始時点**の収集値スナップショットを使っていた。このため「`<select>` の値変更 → 数百 ms 以内に別のチェックボックスを ON」のように連続操作すると、select のコミットが待機中に行われたチェックボックスの編集を古い収集値で上書きしていた。さらに、巻き戻された内部値を後続の入力操作（別のテキスト入力など）が再収集して確定させるため、誤った値がそのまま送信される。症状はフォーカス状態によって「DOM のチェックが外れる」場合と「見た目はチェック済みのまま収集値だけ `false` になる」場合があり、後者は視認できないまま誤った値が送信されていた。入力欄への書き戻しを**適用直前に読み直した最新のバインドデータ**基準に変更した（`data-bind` 属性のミラーは従来どおりワーク開始時点で、並行更新時に古い値を属性へ書いて `MutationObserver` が拾う競合を避ける）。

### Tests

- `tests/form-writeback-freshness.test.ts`: ワーク進行中にバインドデータが更新された場合、入力欄への書き戻しが最新値を使うことを検証（チェックボックス・テキスト入力・`data-form-arg` 付きフォーム、および割り込みがない場合の従来動作）。
- `playwright/concurrent-edit-rollback.spec.cjs`: 実ブラウザで「select 変更 → d1 ms 後にチェックボックス ON → d2 ms 後に別入力」を遅延グリッドで総当たりし、巻き戻りが発生しないことを検証。修正前は 48 試行中 27 件で再現した。

## [0.26.0] - 2026-07-26

### Added

- **`input[type=file]` のファイルをフォーム値として収集できるようにした**。従来は `element.value`（`C:\fakepath\...` の擬似パス）しか扱っておらず、`data-{event}-fetch-content-type="multipart/form-data"` を指定してもファイルが送信データに入らず、ファイルアップロードが実装できなかった。`Form.getValues`（`data-click-form`・双方向バインドを含むすべての値収集経路）が DOM の `files` から File を直接取得するようにし、単一選択は `File`（未選択は `null`）、`multiple` 指定は `File[]`（未選択は `[]`）として収集する。`data-form-list` と `multiple` の併用でも 1 次元配列に保つ。あわせて multipart の FormData 構築で**配列要素の Blob / File も実体のまま**個別エントリとして追加するようにし、複数ファイル送信に対応した。内部値（式から参照される値）は擬似パスではなく選択済みならファイル名、未選択なら `null` を保持するため `data-attr-disabled="{{!csvFile}}"` のような選択有無の判定に使える。なお File は収集結果のトップレベルに置く必要がある（`data-form-object` / `data-form-list` 配下の File は FormData で JSON 文字列化され送信できないため、検出時に警告する）。
- **`data-each-rendered-change`: 描画確定後に `change` を宣言的に発火する属性を追加**。API から取得した候補を `data-each` で `<select>` へ流し込み、「既定選択を確定して初期データを取得する」パターンを、インライン JS（`dataset` フラグ＋`setTimeout`）なしで宣言できるようにした。属性値を省略または `once` で「描画行が 1 件以上ある最初の描画確定時のみ」発火（既定）、`always` で描画確定ごとに毎回発火する。描画行が 0 件のときは発火せず初回発火の判定も消費しないため、`data-fetch` 完了前の空描画をガードする必要がない。既定を `once` にしているのは、`change` の手続きが `data-each` の取得元を再バインドする構成で毎回発火させると再帰的な発火ループになり得るため。`data-each-rendered-run` より後に実行されるため、外部ウィジェットの再同期を先に済ませた状態で発火する。

### Fixed

- **初期スキャン中に発火したイベントで手続きが実行されない不具合を修正**。`Observer.init()` が `EventDispatcher` の購読を初期スキャン完了後（`data-haori-ready` 付与後）に開始していたため、初期スキャン中に同期的に発火されたイベントはリスナー未登録のまま失われていた。`data-each-rendered-run` から `change` を発火して既定選択を確定する構成（静的 JSON や高速な API で `data-fetch` が初回マウント中に解決する場合）で `data-change-fetch` が実行されず、`setTimeout(..., 0)` で遅延させるワークアラウンドが必要だった。警告もエラーも出ないため原因の特定も困難だった。イベントリスナーの登録を初期スキャンより**前**に行い、手続きの実行だけを初期化完了後まで保留して発火順に再生する方式へ変更した。`data-each-rendered-run` 経由に限らず、外部ウィジェットの初期化に伴うイベントなど、初期スキャン中に発火するすべてのイベント（`click` / `change` / `input` / `load`、および初期 HTML に宣言済みの `data-on` カスタムイベント）が対象。`preventDefault`（`data-{event}-prevent`）と対象要素の解決はイベント発生時に同期で行うため保留の影響を受けない。保留中に DOM から外れた要素のイベントは再生しない（この判定は再生時のみで、通常のイベント処理では行わない）。初期化が途中で失敗しても保留は必ず解除する。
- **フォームコンテナを持たない単独入力の `change` で、バインド先が空オブジェクトで全置換される不具合を修正**。`change` / `input` の手続きはフォームコンテナ（`<form>` / `data-form`）の値収集のみを送信データの源としていたため、`<form>` の外に置いた入力（同意チェックボックス等）では送信データが空になり、`data-change-bind` がバインド先を `{}` で全置換して既存データと表示を破壊していた。回避には専用の `<form>` を用意する必要があった。フォームコンテナが祖先に無い場合は**イベント発生元の入力要素自身**を値収集の対象とし、その `name` と値を送信データに含めるようにした（`value="true"` の boolean チェックボックスは ON=`true` / OFF=`false`）。対象は `name` を持つ `<input>` / `<select>` / `<textarea>` 自身に限定する（コンテナ要素の `change` で配下を意図せず収集しないため）。あわせて多層防御として、**この経路で**収集値が空のときに**キー指定（`bind-arg`）もマージ指定（`bind-merge`）も無い全置換のみ**、警告を出してバインドをスキップするようにした（`data-click-bind` による意図的な空クリアは従来どおり有効）。
- **バインドデータに File / Blob が含まれる場合に差分が検出されない問題を修正**。File / Blob は列挙可能なプロパティを持たないため、バインドシグネチャの計算で別ファイルでも同じ `{}` になり「変更なし」と誤判定されて再評価が行われなかった。ファイル名・サイズ・更新日時・MIME タイプでシグネチャを作るようにした。
- **`input[type=file]` へバインドデータの値を書き戻そうとした際に例外になり得る問題を修正**。ブラウザは file input への非空文字の代入で `InvalidStateError` を投げるため、フォーム値の反映ではクリア（`null` / 空文字）のみを行い、それ以外の値は静かに無視するようにした（`Fragment.setValue` での直接設定のみ警告する）。あわせて双方向バインディングでバインドデータへ反映する値は File をファイル名へ正規化し、`data-bind` 属性が `{"csvFile":{}}` に壊れないようにした。
- **`data-each-rendered-change` の発火を `data-each` の再入制御解除後に行うようにした**。描画サイクル内で発火すると、`change` の手続きが同一コンテナの再評価を要求した場合に再評価要求を取りこぼす可能性があった。
- **`data-{event}-history-form` の履歴クエリと `data-{event}-copy` のコピー先で File が `{}` / `[object File]` になる問題を修正**。いずれもファイル名へ正規化するようにした（履歴 URL の復元やコピー先バインドではファイル自体を扱えないため）。`data-{event}-reset-before` 併用時のスナップショット経路も含め、バインド／履歴へ渡す収集はすべて共通ヘルパー経由に統一して適用漏れを防いだ。

### Changed

- **multipart 以外で File を送信しようとした場合に警告を出力するようにした**。JSON では `{}`、クエリや `application/x-www-form-urlencoded` では `[object File]` になり原因が分かりにくいため、`data-{event}-fetch-content-type="multipart/form-data"` と body を持つメソッドの併用を促す警告を出す。
- **フォームコンテナを持たない `change` / `input` で、対象入力の値が送信データ全体へ反映されるようになった**（挙動変更）。bind だけでなく `data-{event}-fetch` のクエリ・ボディにも含まれる。例えばフォーム外の `<select name="kind" data-change-fetch="/api/list">` は、これまでパラメータなしで取得していたが `/api/list?kind=B` を取得するようになる。フォーム内の入力と同じ扱いに揃えたもので、絞り込みをフォームなしで宣言できるようにする意図。既存ページでフォーム外入力に `data-change-fetch` を使っている場合はリクエスト URL が変わるため、サーバー側で未知のクエリを拒否する構成では確認が必要。

### Docs

- `docs/ja/specs.md`: 初期化フローを実装に合わせて更新し、「初期スキャン中に発火したイベントの扱い」節を追加。`data-each-rendered-change` の仕様、`input[type=file]` の値収集規則、フォームコンテナを持たない入力の値収集規則、multipart の配列 Blob 対応を追記。`data-url-param` の `data-url-arg` 有無による全置換／マージの違いと、未定義キーの直接参照がコンソールエラーになる点を明記。
- `docs/ja/guide.md`: 「ファイルをアップロードする」「認証切れをログインページで知らせる（401 リダイレクト + メッセージ）」「選択肢を描画したら既定選択で初期データを取得する（`data-each-rendered-change`）」「フォーム外の単独チェックボックスでボタンを活性化する」の各レシピを追加。`data-url-arg` を付けるべき 2 つの理由（既定値の全置換回避・未定義キー参照のエラー回避）を追記。

### Tests

- `tests/init-deferred-events.test.ts`: 保留モードでのイベント再生（発火順・DOM から外れた要素の除外・同期経路では除外しない回帰・`data-{event}-prevent` の同期性・カスタムイベントの保留）、`Observer.init` の購読順序、`data-each-rendered-change` の `once` / `always` / 0 件時の挙動を検証。
- `tests/file-input-collection.test.ts`: `input[type=file]` の値収集（単一・未選択・`multiple`・`data-form-list` 併用）、multipart 送信での File 実体、`data-form-object` 配下（トップレベルとの混在を含む）および multipart 以外での警告、書き戻し時の静かなスキップと直接設定時の警告、双方向バインディングと history クエリでのファイル名正規化を検証。
- `tests/standalone-input-bind.test.ts`: フォーム外の単独入力の ON / OFF 書き戻し、fetch クエリへの反映、`name` なしでの bind スキップ、コンテナ要素での非収集、`data-click-bind` による空クリアの維持（回帰）、フォーム内入力の従来動作（回帰）を検証。
- `playwright/init-scan-change.spec.cjs`: 実ブラウザで初期ロードの 1 パス（`data-fetch` → `data-each` 描画 → `change` 発火 → `data-change-fetch`）が `data-each-rendered-run` の同期 dispatch と `data-each-rendered-change` の両方で成立することを検証。

## [0.25.0] - 2026-06-29

### Added

- **組み込みヘルパー `haori.now` / `haori.today` を追加**。`data-bind` は JSON 専用で `{{}}` を解釈しないため、初期バインド値に動的な日付を宣言的に入れる手段がなかった。`haori.now(format?, timeZone?)` は評価時点の現在日時を `haori.date` と同じ規則で整形し（既定 `yyyy/MM/dd HH:mm`）、`haori.today(offsetDays?, format?, timeZone?)` は現在日付に日数を加減して整形する（既定 `offsetDays`=0、既定フォーマット `yyyy-MM-dd`＝`input[type=date]` 互換）。日付の加減算は UTC 基準のカレンダー演算で行うため月跨ぎ・年跨ぎを自動処理し DST の影響を受けない。`timeZone` 指定時はそのゾーンの現在日付を起点に計算する。両関数とも現在時刻に依存するため**冪等ではない**（式の再評価ごとに更新される）。`Haori.now(...)` / `Haori.today(...)` として静的メソッドでも公開。一覧の絞り込み初期値（前日・当日など）を非公式な `new Date()` ではなく宣言的に書けるようにするのが狙い。

### Docs

- `docs/ja/guide.md`: 「現在日時・相対日付を入れる（`haori.now` / `haori.today`）」節を追加。`data-attr-value` が初期値ではなく再評価される点（日跨ぎ・再描画で値が変わりうる）の注意も明記。
- `docs/ja/specs.md`: 組み込みヘルパー節に `haori.now` / `haori.today` を追記し、非冪等であること・`data-attr-value` の再評価注意を追加。

### Tests

- `tests/builtins.test.ts`: `vi.setSystemTime` で本日を固定し、`now` / `today` の既定書式・offset・月跨ぎ／年跨ぎ・`timeZone`・不正値、および `Haori.now` / `Haori.today` の共用を検証するケースを追加。

## [0.24.0] - 2026-06-27

### Added

- **`data-fetch-state` / `data-{event}-fetch-state`: フェッチ状態の宣言的参照を追加**。`data-fetch` / `data-{event}-fetch` の進行状況を `_fetch` というキーで対象要素のバインディングデータへ注入し、画面個別の JavaScript を書かずに `data-if="_fetch.error"` などで読み込み中・成功・失敗を出し分けられるようにした。`_fetch` は `{status, loading, success, error, statusCode, message}` を持つ（`status` は `"loading"` / `"success"` / `"error"`）。注入タイミングはフェッチ開始直前（loading）、HTTP エラー応答（error: `statusCode`=HTTP ステータス、`message`=`statusText`）、ネットワーク断・タイムアウト等の例外（error: `statusCode`=`null`、`message`=例外メッセージ）、バインド反映後（success: `statusCode`=HTTP ステータス）。値を省略すると自要素、CSS セレクタ指定で別要素を注入先にできる。`_fetch` は内部バインディングデータにのみ設定し `data-bind` 属性へは書き出さない（`bindchange` も発火しない）が、注入先要素の再評価（`data-if` 等）は行う。自動リトライは行わず、再取得は `data-click-fetch` 等で手動導線を宣言する。

### Fixed

- **同一 `name` のラジオボタン群を `data-change-bind` で1キーへ書く際、値が配列累積していた不具合を修正**。ラジオは排他制御で他要素が未チェックになるがその要素では `change` が発火しないため、フォーム値収集（`Form.getPartValues`）が未チェック要素の古い内部値もチェック済みとして集め、同一キーに複数値が集まり配列（例: `["none","fixed","ratio"]`）になっていた。これにより `data-if="x === 'fixed'"` のスカラ比較が当たらず、`ratio→none` など前の選択へ戻しても依存欄の `data-if` が再評価されず再非表示にならなかった。グループ収集では DOM の `checked` を真とし、未チェック要素の内部値を無視してスカラで上書きするよう修正（チェックボックスの複数チェック→配列収集は不変）。あわせて `change`/`input` 時に同一フォームスコープの同名ラジオの内部値も同期し、収集前の不整合を根治した。
- **`data-if` が `false` の分岐（`data-if-false`）配下のフォーム入力を、フォーム値収集の対象から除外するよう修正**。`data-if` が `false` の要素は DOM から削除されず `data-if-false` 属性付きで残るため、同一 `name` の入力を設定型ごとに `data-if` で出し分けると非表示分岐の入力も同名で DOM に共存し、`data-click-form` の直列化で送信値が競合する懸念があった。`Form.getValues`（`data-click-form`・双方向バインド・履歴フォーム収集を含む全経路）が `data-if-false` 属性を持つ要素とその配下をサブツリーごとスキップするようにし、表示中の分岐の値だけを収集することを保証した（`data-form-object` / `data-form-list` 配下も非表示なら除外）。なお同名要素の DOM 上の共存自体は解消されないため、セレクタの strict mode を避けたい場合は入力を1つに統一し `type` / `step` / `max` 等を `{{}}` 式で切り替える運用を推奨する。

### Docs

- `docs/ja/guide.md`: 「フェッチの状態を画面に表示する（`data-fetch-state`）」節を追加（`_fetch` 構造、自要素／別要素への注入例、手動再取得導線、401/403 は対象外・自動リトライなしの補足）。
- `docs/ja/specs.md`: `data-fetch` 関連属性に `data-fetch-state` を追記し、`data-fetch-state` / `data-{event}-fetch-state` の専用仕様（`_fetch` 構造・注入タイミング・`data-bind` へ書き出さない等）を追加。
- `docs/ja/guide.md`: 「非表示にした入力はフォーム送信に含まれない」節を追加（`data-if-false` 配下の入力がフォーム値収集対象外であること、同名要素は DOM に残る旨の補足）。
- `docs/ja/specs.md`: `data-if` の動作と双方向バインディング節に「`data-if-false` 分岐とフォーム送信」を追加（`Form.getValues` がサブツリーごと除外する仕様）。
- `docs/ja/guide.md`: 「`<title>` などページタイトルを実行時に変える」節を追加（`<head>` もスキャン対象であり、`<title>` 自身への `data-bind` / `data-fetch` 付与で `{{}}` 補間が効くこと、兄弟スコープは継承されないこと、`data-fetch-bind` / `*-copy` の対象セレクタは `<body>` 配下限定で `<head>` を狙えないことの補足）。
- `docs/ja/specs.md`: `data-fetch` 節に「`<head>` / `<title>` への実行時バインド」を追加し、`data-fetch-bind` の対象セレクタが `<body>` 配下限定である旨を明記。

### Tests

- `tests/fetch-state-binding.test.ts` を追加（成功時の `_fetch.success`/`statusCode`、HTTP 500 の `_fetch.error`/`statusCode`、別要素 `#panel` への注入、`data-bind` 属性を汚さないこと）。
- `tests/radio-group-scalar-bind.test.ts` を追加（別要素のネストキーへ書く構成で `none→fixed→ratio` の前進と `ratio→none` の後退の双方でスカラ更新・依存 `data-if` の再評価を検証）。
- `tests/checkbox-group-collection.test.ts` を追加（チェックボックス群の複数チェック→配列・単一→スカラ・未チェック→null・`change` 後の最新 DOM 反映の回帰）。
- `tests/if-false-form-exclusion.test.ts` を追加（`data-if-false` 配下の同名入力が `Form.getValues` の対象外になること、`mode` 切替で表示分岐の値だけが収集されること、`data-form-object` 配下も除外されることを検証）。
- `tests/title-head-bind.test.ts` を追加（`<title>` 自身への `data-bind` / `data-fetch` でテキストが補間・実行時更新されること、`data-fetch-arg` のネスト、兄弟スコープ非継承、`data-fetch-bind` では `<head>` 内要素を対象にできないことを検証）。

## [0.23.0] - 2026-06-23

### Added

- **`data-external`: 外部管理サブツリーの監視除外を宣言する公開属性を追加**。指定要素とその子孫で発生した DOM 変更（属性・ノード・テキスト）を Haori の自動監視（MutationObserver）対象から除外する。Choices.js など「元要素を隠して独自 DOM を生成・随時更新する」select 拡張ライブラリの生成 DOM に Haori が干渉しないようにするためのもの。`data-each` による `<option>` 生成はバインド評価パイプラインが駆動するため監視除外下でも維持される。外部生成 DOM とバインド対象を内側に含む**外側コンテナ**への付与を推奨。
- **`data-each-rendered-run`: 描画確定フックを追加**。`data-each` の再描画が安定し `data-each-done` が付与されるたびに、コンテナ単位で一度だけ任意 JS を実行する（`data-{event}-run` と同じ式評価、本体内 `this` はコンテナ要素に束縛）。外部ウィジェットの再同期フック（例: `data-each-rendered-run="window.__choicesRefresh(this)"`）として利用できる。
- **`<select multiple>` の配列値対応**。複数選択 select の値を選択済み `option` の文字列配列として扱うようにした。フォーム値収集では `name` キーに配列が格納され（例: `{"plans":["A","C"]}`）、バインドデータの配列を与えると各 `option` の選択状態へ反映される。外部ウィジェットが native `<select>` を更新して発火する `change` も同様に配列として双方向同期される。単一選択 select の挙動は不変（後方互換維持）。

### Changed

- **`data-each-done` の発火保証を仕様として明文化**。初回描画・再 fetch・再バインドのたびに、コンテナ単位で「除去 → 再付与」が必ず一度行われる（差分で実 DOM 変更がない場合も安定時に再付与）。外部ウィジェットの再同期契機として利用できることを保証。

### Removed

- **未発火だった `haori:render` イベント（`HaoriEvent.render`）を削除**。仕様書・guide に記載があったが実描画では一度も発火していないデッドコードだった。描画完了の検知は `data-each-done`（描画確定ごとに付与）または `data-each-rendered-run` に一本化した。

### Docs

- `docs/ja/specs.md`: `data-external` 節、`<select multiple>` の配列値、`data-each-rendered-run`、`data-each-done` 発火保証を追記。`haori:render` を整理。
- `docs/ja/guide.md`: Choices.js 連携の最小レシピ（`data-external` + `data-each-rendered-run` + `<select multiple>`）を追加。`haori:render` を整理。

### Tests

- `tests/external-select-integration.test.ts` を追加（複数選択 select の配列収集・change 同期・バインド反映、`data-external` 配下の監視除外と対照、`data-each-rendered-run` の描画確定ごとの一度実行）。

## [0.22.3] - 2026-06-22

### Fixed

- **`Haori.openDialog()`（`data-{event}-open`）でダイアログを開く際、対象ダイアログ内に残った `data-message` / `data-message-level` をクリアするよう修正**。従来は `showModal()` を呼ぶだけで前回のメッセージ属性を除去しなかったため、エラー表示後に閉じたダイアログを再度開くと前回の `data-message` / `data-message-level` が残っていた。開く前に対象ダイアログ自身とその子孫のメッセージ属性を `clearMessages` 相当で除去してから開くようにした。`closeDialog()` は従来どおりクリアしない（閉じても表示中メッセージは保持）。
  - 内部的に同期版ヘルパー `clearMessagesSync()` を抽出し、`clearMessages()` と `openDialog()` で共有することで重複を排し、`openDialog` の enqueue ブロック内で `showModal()` との実行順を保証する。

### Tests

- メッセージ付与済みダイアログを `openDialog` で開くと自身・子孫のメッセージ属性が除去されること、`closeDialog` では除去されないこと、を `tests/haori.test.ts` に追加。

## [0.22.2] - 2026-06-19

### Fixed

- **`data-{event}-close` / `data-{event}-open` の対象省略時、自要素ではなく祖先 `<dialog>` を対象にするよう修正**。従来は値を省略すると「トリガー要素自身」を対象にしていたため、`<dialog><button data-click-close>閉じる</button></dialog>` のようにダイアログ内の閉じるボタンへ値なしで付与すると、ダイアログ本体ではなくボタン自身が `closeDialog` に渡され、`HTMLDialogElement` でないため「Element is not a dialog」のログだけ出てダイアログが閉じなかった。値を省略した場合は `triggerElement.closest('dialog')` 相当で自要素の祖先方向に最も近い `<dialog>` を対象にする。祖先に `<dialog>` が無い場合はログ出力してスキップする。明示セレクタ指定時の挙動は不変（後方互換維持）。
  - **挙動差の注意**: `<dialog>` でない要素（例: `.modal` の `div`）内で値を省略した場合、従来は自要素を対象にしていたが、本修正後は祖先 `<dialog>` が無いためスキップ（ログ出力）する。

### Tests

- 値省略の `data-click-close` / `data-click-open` が祖先 `<dialog>` を対象にすること、祖先 `<dialog>` が無い場合にログ出力してスキップすること、を `tests/procedure-action-operations.test.ts` に追加。

## [0.22.1] - 2026-06-15

### Fixed

- **`data-{event}-run` の戻り値が `return` 省略時に捕捉されず、async ハンドラが await されない不具合を修正**。0.22.0 で「Promise を返した run は完了まで await（多重実行防止）」を導入したが、run スクリプトは `new Function('event', "use strict"; body)` で生成され**暗黙の `return` が無い**ため、`data-click-run="save()"` のような **`return` を書かない素の async 関数呼び出しでは戻り値が `undefined` となり await されず**、2 度押し防止が効いていなかった（`return save()` と明示した場合のみ機能していた）。本体が**単一式**として評価できる場合に `return (body)` で戻り値を捕捉するようにし、`return` 省略でも Promise が await され、同期 `false` で `preventDefault()` が働くようにした。
  - 複数文・`if` 文・明示 `return` を含む本体は従来どおり文ブロックとして実行され、戻り値は本体内の明示的な `return` に従う。
  - **挙動差の注意**: 単一式の本体がその式自体で同期的に `false` を評価する場合（例: `checkSomething()` が `false` を返す）、`return` を書かなくても `preventDefault()` が働く（素の `onclick="checkSomething()"` とは異なる）。

### Tests

- `return` 省略の単一式 async run が await されロックを保持すること、複数文（明示 return）でも await されること、を `tests/procedure-run-await.test.ts` に追加。

## [0.22.0] - 2026-06-15

### Changed

- **`data-{event}-run` が Promise（thenable）を返した場合、その完了まで `await` するようにした**。`click` 手続きでは await の間も多重実行防止ロック（対象要素の `disabled` 付与・`RUNNING_CLICK_TARGETS` 登録）を保持するため、**async ハンドラ（保存 POST 等）でも 2 度押しによる重複送信を防げる**ようになった。これまでは戻り値の Promise を await せず切り離していたため、`execute()` が合成レスポンスで即完了してロックが数マイクロ秒で解放され、数百 ms 間隔の 2 度押しでハンドラが 2 回実行され得た（重複レコード等）。利用側で再入ガード変数を持つ必要がなくなる。
  - **後方互換**: 同期戻り値（非 thenable）の挙動は不変（即時にロック解放、`false` で `preventDefault()`）。
  - **run + fetch 併用時は run の完了後に fetch が直列実行**される（仕様の「run → fetch の順」と整合。従来は実質並行だった）。
  - 多重実行防止ロックは `click` のみ。クリック以外（`data-change-run` 等）でも await による直列化は行われるが、多重実行そのものは防止しない。
  - **注意（async ハンドラ）**: `async` 関数や Promise を返すハンドラは戻り値が常に Promise（truthy）になるため `return false` では `preventDefault()` できない（`preventDefault()` は同期段でしか効かない）。既定動作を抑止する場合はハンドラ先頭で同期的に `event.preventDefault()` を呼ぶか `data-{event}-prevent` を併用する。返した Promise が settle しないとボタンが `disabled` のままになるため、必ず settle する Promise を返すこと。reject 時は `Log.error` で報告し例外は外へ投げず、後続処理は継続する。

### Documentation

- 仕様書 `data-{event}-run` 節に、Promise 戻り値時の多重実行防止・run → fetch 直列化・async での `preventDefault` の扱い・必ず settle すること・ロックは `click` のみ、を追記した。

### Library

- 回帰テストを追加した（`tests/procedure-run-await.test.ts`: async run 実行中の 2 度押しで 1 回のみ実行、解決後の再クリック実行、同期 run の即時ロック解放）。

## [0.21.0] - 2026-06-14

### Changed

- バインド対象オブジェクトのトップレベルキーが予約名（禁止識別子）と衝突したときの挙動を改善した。
  - **粒度改善**: これまでは禁止名キーが 1 つでもあるとバインド**全体**が破棄され、配下の `data-each`・`{{ }}` がすべて無言で空になっていた。今後は**該当キーのみ無視**し、残りの正常なキーはそのまま評価・描画する。無視したキーがある場合は原因特定のため `Log.warn` ではなく **`Log.error` で該当キー名を明示**する（`Binding keys are reserved and ignored: …`）。
  - **名前空間衝突名の再バインド許可**: `history` / `document` / `navigator` / `localStorage` / `sessionStorage` / `IndexedDB`（`location` は従来から許可）を、トップレベルのバインドキーとして利用できるようにした（`REBINDABLE_FORBIDDEN_NAMES`）。式中ではバインド値が同名グローバルを遮蔽するため実グローバルへは到達せず安全。これにより `{ "history": [ … ] }` を `data-each="history"` でそのまま繰り返せる。`window` / `self` / `globalThis` / `Object` / `Function` / `eval` / `constructor` / `__proto__` / `prototype` / `setTimeout` などの**実行系・プロトタイプ脱出名は引き続き禁止**（該当キーは無視）。
  - バインド値に実ホストオブジェクト等の**危険な値**が含まれる場合の拒否（再帰チェック）は従来どおり維持する。README / 仕様書に予約名のトップレベルキー方針を明記した。

### Added

- 組み込みヘルパー（予約名前空間 `haori`）に **`haori.distinct(array, key?)`** と **`haori.groupBy(array, key)`** を追加した。`distinct` は重複を取り除いた配列を返し（`key` 省略時は要素自体、指定時は `item[key]` で判定。`findBy`／`sum` と同じく文字列化比較で最初の出現を保持）、`groupBy` は `item[key]` ごとに `{key, items}` の配列へ分ける（グループ・要素とも出現順を保持。`key` には最初の要素の生値を格納）。明細レスポンスを「1 件 = 1 行」にまとめたり、入れ子の `data-each` でグループ描画したりを JavaScript なしで宣言的に書ける。`Haori.distinct` / `Haori.groupBy` としても同一実装を公開。
- **`haori.date` に第3引数 `timeZone` を追加**した。省略時は従来どおりブラウザのローカル時刻で整形し、IANA タイムゾーン名（例 `'Asia/Tokyo'`）を渡すと端末のタイムゾーンに依存せずその地域の時刻で整形する（`Intl.DateTimeFormat` を利用、24 時間表記）。不正なタイムゾーン名は空文字を返す。後方互換（既存の 2 引数呼び出しは挙動不変）。
- **スクロール追従の可視行範囲を公開する `data-each-visible` を追加**した。`data-each` コンテナに `data-each-visible="<変数名>"` を付けると、いまビューポートに見えている行範囲を**最近接の上位 `data-bind` スコープ**へ指定名の変数（`first` / `last` / `firstLabel` / `lastLabel` / `count` / `total`（読込済行数）/ `empty`）として公開する。無限スクロールのフッタ「x - y / z 件」を JavaScript なしで実現できる。`data-each-visible-root`（スクロール枠）・`data-each-visible-margin`（rootMargin）で調整可能。各行を `IntersectionObserver`（しきい値 0＝1px でも見えたら可視）で監視し、交差変化を `requestAnimationFrame` でまとめて集計する。公開は前回値と異なるときだけ行い、再評価では一覧本体を `skipFragments` で枝刈りするため行数に依存しない。
  - 可視範囲は実行時の一時変数のため、公開時は `data-bind` 属性への全データ直列化（`JSON.stringify`）を抑止し、`haori:bindchange` イベントも発火しない（高頻度更新でのコスト・通知の氾濫を避ける。値は `Haori.Core.getBindingData(...)` の in-memory 値で参照可能）。この一時更新用に内部 API `Core.setBindingData` へ `reflectToAttribute` 引数を追加した（既定 `true` で既存挙動は不変）。

## [0.20.1] - 2026-06-14

### Fixed

- `data-click-fetch` / `data-fetch` でサーバがエラー応答を返したとき、再試行のたびに同じコンテナへエラーメッセージが積み増される（累積する）不具合を修正した。`handleFetchError` がメッセージ描画を始める前に、**フェッチ単位で対象スコープの既存メッセージを 1 度だけクリア**するようにした（フォーム／対象フラグメントが特定できる場合はそのスコープ、特定できない場合は `document.body`）。これにより表示は常に最新の 1 応答分へ置き換わり、再試行間の累積が解消する。1 応答内に複数のフィールドエラー／全体エラーが含まれる場合は、クリア後にまとめて追加されるため同一応答内のメッセージは従来どおり並んで表示される（同一応答内の挙動は不変）。
  - 累積はメッセージ表示を append 方式で実装した実装（haori-bootstrap 等）と併用したときに顕在化するが、クリア責務は単一の共有コードパスである core（`handleFetchError`）側にある。この修正に伴い `window.Haori` 差し替え時の必須メソッドへ `clearMessages` を追加した。

## [0.20.0] - 2026-06-13

### Added

- 認証ガードの戻り先クエリ自動付与（`*-return-param`、送り手）と対称な**受け手側**として、`data-{event}-redirect-return-param="クエリ名"` を追加した。手続きの**成功後**リダイレクト先を URL クエリパラメータから**安全に**解決する。`data-{event}-redirect`（静的な既定遷移先）と併用し、その遷移先を「安全な戻り先で上書きする」修飾子として動作する。`data-{event}-redirect` が無い場合は本属性を無視する（オプトイン。属性が無ければ従来挙動は不変）。
  - 動作: 成功時に現在ページ URL から指定クエリ値を `URLSearchParams.get()` で**1回だけ**デコードして読み取り（二重デコードによる検証回避を防ぐため追加デコードは行わない）、`trim()` 後に**安全な同一オリジンのローカルパス**であればそこへ遷移する。安全でない／値が無い場合は `data-{event}-redirect` へフォールバックする。
  - 安全判定（ライブラリ内蔵、`Url.isSafeLocalPath`）: `trim()` 後の値が**単一の `/` で始まる**こと。`//`・`/\`（プロトコル相対と解釈され得る）、スキーム・オーソリティを含むものは拒否し、さらに現在オリジンを基準に解決したオリジンの一致も確認する。判定 NG は `Log.warn('Haori', ...)` で警告してフォールバックする。これによりオープンリダイレクトをライブラリ側で構造的に防ぎ、利用側で必要だった戻り先検証の手書き JS が不要になる。
  - 全 `{event}`（click / submit / change 等）と全 fetch 経路で一貫して利用でき、認証ガードの `*-return-param` と同名クエリを使えば付与 → 消費が対称になる。

### Documentation

- 仕様書に `data-{event}-redirect-return-param` 節を追加し、手続き擬似コードの redirect 処理に戻り先解決ロジックを反映した。認証ガードの `*-return-param` 節の「受け手は利用側の責務」の注記を、本属性で宣言的に対応できる旨へ更新した。
- README / README.ja に受け手側属性の項目を追加した。

### Library

- 受け手側の回帰テストを追加した（`tests/procedure-redirect-return-param.test.ts`: 安全なローカルパスへの遷移、外部 URL／プロトコル相対の拒否＋フォールバック、クエリ無し時の既定遷移、`redirect` 無し時の無視、認証ガード送り手との往復）。安全判定の単体テストを `tests/url.test.ts` に追加した。

## [0.19.0] - 2026-06-13

### Added

- 認証ガードに戻り先クエリの自動付与を追加した。`data-unauthorized-redirect-return-param="クエリ名"` / `data-forbidden-redirect-return-param="クエリ名"` を遷移先属性と同じ要素（`<body>`／`<html>`、`<body>` 優先）に宣言すると、401／403 で遷移する直前に現在ページの `pathname + search + hash` を指定クエリ名で遷移先へ自動付与する（ログイン後の復帰用）。値は `encodeURIComponent` でエンコードされ（半角空白は `%20`）、宣言された遷移先 URL の形式（相対／絶対）を保持してフラグメント手前へ挿入する。遷移先 URL の既存クエリは保持してマージするが、**同名クエリが既にある場合は宣言された遷移先 URL 側を優先**して付与しない。無限ループ防止の自ページ判定は戻り先付与後の最終 URL に対して行う。属性が無い／値が空のときは何もしない（オプトイン）。

## [0.18.1] - 2026-06-13

### Fixed

- `data-click-fetch`（および `data-fetch` 系）でバインド先を明示せず、サーバが **2xx かつ空ボディ**（`204 No Content` / 本文なしの `200`、Spring の `void` 戻り値等）を返したとき、内部の結果バインドが reject し、後続アクション（`*-toast` / `*-close` / `*-click` による再取得 / refetch 等）がすべて中断されていた不具合を修正した。**根本原因**は、fetch 時にバインド先未指定だと自要素を既定バインド先に補う（既定 self-bind）一方で、空ボディは `response.text()` で空文字（＝文字列）になり、バインドキー名（`data-fetch-arg`）無しの文字列 bind が `bindResult` で reject し、`handleFetchResult` の `Promise.all` を巻き込んで以降の toast/close/click/refetch を止めていた点。`Content-Type: application/json` の空ボディでも `response.json()` のパース失敗で同様に停止していた。症状例として、`204` を返す削除 API の削除自体は成功するのに、成功トーストが出ず一覧も再取得されなかった。
  - 修正後は、(1) **2xx 空ボディをバインド対象なしとして正常スキップ**するようにした（reject せず後続を実行）。応答ボディは先に `text()` として読み、空ならスキップ、非空のみ `Content-Type` に応じて JSON 解析する（不正 JSON は従来どおりエラー）。(2) **既定 self-bind**（バインド先を明示していない）の場合に限り、JSON オブジェクトでない文字列応答が返ってもバインドを意図していないものとみなし、reject ではなく**警告にとどめてスキップ**するようにした。バインド先を**明示指定**したうえで `data-fetch-arg` 無しの文字列が返るケースは、誤用として従来どおりエラーで停止し気付けるようにしている。

### Documentation

- 仕様書の `data-fetch` 節に「レスポンスのバインド挙動」を追記した（既定 self-bind、`Content-Type` による解釈、2xx 空ボディの正常スキップ、明示 bind 時と既定 self-bind 時の文字列応答の扱いの違い）。

### Library

- 上記の回帰テストを追加した（`tests/fetch-and-procedure-scenarios.test.ts`: `204` 空ボディ、`200` 空ボディ+`application/json`、既定 self-bind の文字列応答での後続実行、明示 bind 時の文字列応答での reject）。

## [0.18.0] - 2026-06-12

### Fixed

- `data-click-bind` の対象が `data-url-param` 要素のとき、同一クリック内の後続アクション `data-click-open`（modal を開く）が発火しない退行（0.17.1〜0.17.2）を修正した。**根本原因**は 0.17.1 の純粋 FIFO 直列化との相互作用による**自己デッドロック**。バインドワーク（`Core.setBindingData` の work）は `reevaluateReactiveSpecialAttributes` 内で配下の `data-fetch` を起動・await するが、その `data-fetch-bind` が**同一フラグメント自身を指す**（例: `#invoice-dialog-state` 配下の charges 取得が `data-fetch-bind="#invoice-dialog-state"`）と、その bind が同じ FIFO チェーンへ次のワークを積むため、実行中ワークと相互に待ち合って永久にブロックし、`await` していたクリック処理が `data-click-open` へ到達しなかった（0.17.0 までは旧再入バイパスがこの bind をインライン実行していたため顕在化しなかった）。
  - 修正後は、マネージド `data-fetch`（`Core.executeManagedFetch`）から起動された bind を、**bind 先フラグメントが実行中のバインドワークを持つ（= その bind を await している自己再入の）場合に限り** reentrant（即時実行）するようにした。reentrant 実行は FIFO チェーンを占有せず即時完了するためデッドロックしない。bind 先が実行中でない（一般的な別コンテナへの bind 等）場合は従来どおり FIFO でキューへ積むため適用順を保証する。独立した並行呼出（ユーザー入力起点等）も純粋 FIFO のままで、0.17.1 で修正した「古い値の後勝ち」競合は再発しない。
  - 実ブラウザ回帰ガード `playwright/urlparam-open-repro.spec.cjs`（＋フィクスチャ）を追加。`data-url-param` 要素へ `data-click-bind`→`data-click-open` する構成（配下に同一フラグメントへ bind-back する `data-fetch` を含む）で、修正前 CDN 0.17.2 は modal が開かず、本修正のローカルビルドでは開くことを確認（実 Chromium）。既存の `setbindingdata-stale.spec.cjs`（古い値後勝ち）・`form-fetch-arg-wipe.spec.cjs` も引き続き緑。

### Added

- fetch エラー応答が**トップレベル JSON 配列** `[{ "key": "field", "message": "..." }]`（一部のサーバ実装が返す例外ハンドラ／バリデーションメッセージ等）の形式でも、各要素をフィールド別エラーへ自動振り分けできるようにした。`key` を持つ要素は対応する `name` のフィールドへ（`Form.addErrorMessage` 経由、haori-bootstrap 併用時は `invalid-feedback` 生成＋`is-invalid` 付与）、`key` を持たない（または空）要素はフォーム全体エラーとする。同一 `key` が複数あれば改行で連結する。振り分けは**ステータスコードに依存しない**（`400` だけでなく業務エラーの `409` 等でも振り分く）。既存の `{ "errors": {...} }` / `{ "message" }` / `{ "messages": [] }` 形式は従来どおり（後方互換）。`application/json` 以外（プレーンテキスト等）のエラーボディは従来どおりボディ全文を全体エラーとして表示する。

### Documentation

- ガイドに「チェック状態はバインドデータで操作する（手書き `.checked` を避ける）」レシピを追記した。`data-attr-checked` で束縛したチェックボックス群を、別要素の `change` から `Core.getBindingData` / `Core.setBindingData` で配列を集合更新して宣言的に保持する書き方（権限セット連動の集合加算の例）を示す。
- 仕様書のエラーハンドリング節を、トップレベル配列形式・ステータス非依存・プレーンテキスト対応の記述に更新した（旧「配列は未対応」の補足を削除）。

### Library

- 上記の回帰テストを追加した（`tests/fetch_error_array.test.ts`、`playwright/urlparam-open-repro.spec.cjs`）。

## [0.17.2] - 2026-06-11

### Fixed

- `form[data-fetch][data-fetch-arg]` 配下の入力を変更したとき、change のフォーム値自動同期がフォーム自身の `data-bind` を確定する際に、フォーム値以外のキー（`data-fetch-arg` で束縛された一覧データ等）が脱落する不具合を修正した。退行すると `data-each` が未解決になり選択肢が placeholder 1個に潰れる（`required` と併用すると submit も止まる）。
  - 原因: change のフォーム値同期で、フォーム値のみを `data-bind` 属性へ直接書き込む冗長な処理があり、その属性変更を MutationObserver が in-memory へ再取り込みしてフォーム値以外のキーを脱落させていた。従来はその後の再取り込みでエコー復元されていたが、0.17.1 の堅牢化(B)（Observer の自己エコー無視）でエコー復元が止まり顕在化した。修正後はこの冗長な直接書き込みを廃し、フォーム値同期は解決済みスコープへフォーム値を重ねた `setBindingData` のみで行う（`data-fetch-arg` 等のキーを保持）。
  - 実ブラウザ回帰ガード `playwright/form-fetch-arg-wipe.spec.cjs`（＋フィクスチャ）を追加。報告者提供の再現構成で、修正後は change 後も選択肢と fetch-arg が保持されることを確認（実 Chromium）。

## [0.17.1] - 2026-06-11

### Fixed

- `Core.setBindingData` の並行呼出で適用順が逆転し、先に呼んだ古いデータが後から確定する競合（負荷依存）を修正した。**根本原因**は、直列化に用いていた再入バイパス（`processingBindingWork` フラグが true の間の呼出を「再入」とみなし即時インライン実行）が、`await` をまたいで届く**独立した並行呼出**まで再入扱いしてしまう点にあった。push① の `data-bind` 書き込み（`setAttribute`）は `skipMutationAttributes` を同期的に立て、その実 DOM 書き込み（描画キュー）まで解除しない。この間（実測で 5〜10ms）にインライン実行された push② の `data-bind` 書き込みは冒頭の `skipMutationAttributes` ガードで**丸ごと破棄**され、push① の古い値が最終確定していた（重い再描画で窓が広がるほど発生しやすい）。
  - 修正後は **純粋 FIFO 直列化**に変更し、並行呼出は決して即時インライン実行せず必ず呼出順でキューへ積む。唯一の真の再入（`data-url-param` の再評価が `evaluateAll` 内から同一フラグメントへ再帰する経路）のみ、自己デッドロック防止のため明示的に即時実行する。
  - あわせて堅牢化として、(A) `data-bind` 属性書き込みを実行時点の最新 in-memory から行い、(B) MutationObserver が Haori 自身の `data-bind` 書き込みのエコーを再取り込みしないようにした（外部からの `data-bind` 変更の取り込みは維持）。
  - 実ブラウザ回帰ガード `playwright/setbindingdata-stale.spec.cjs`（＋フィクスチャ `playwright/serialization-stale-repro.html`）を追加。報告者提供の再現構成（200行 data-each＋数量 change／検索 input、利用側直列化なし）で、修正前 CDN 0.17.0 が 11/60 失敗、本修正のローカルビルドは 0/60 で再現せず（実 Chromium で確認）。

## [0.17.0] - 2026-06-11

### Fixed

- `data-attr-selected` / `data-attr-checked`（および `checked="{{式}}"`）の DOM プロパティ同期が、ユーザー操作中の選択・チェックを再評価で巻き戻す不具合を修正した。0.16.0 で属性に加えてライブプロパティを同期するようにした際、`data-fetch` ラッパ配下のフォーム select などで、change 起因の再評価が `data-attr-selected` の参照キー（select の `name` と別キー等）を未更新のまま `option.selected` を元の値へ戻していた。`value="{{式}}"` のフォーカス中スキップと挙動を統一し、操作中（select / radio / checkbox がフォーカス中）の要素には再適用しないようにした（フォーカスが外れれば次回以降の再評価で宣言状態を反映する）。

### Added

- `Core.getBindingData(element, {resolved?})` を追加した。`setBindingData` の対となるバインドデータの公式読み取り API。既定では要素自身の生バインドデータ（無ければ `null`）を、`resolved: true` で DOM ネストを解決済みのスコープを返す。`Haori.Core.getBindingData(...)` として利用できる。
- 組み込みヘルパー `haori.sum(array, key?)` を追加した。配列の数値合計を求める（`key` 省略時は要素自体、指定時は `item[key]`、数値化できない値は無視、非配列は `0`）。集計テーブルの合計行を `{{haori.number(haori.sum(rows, 'total'))}}` のように宣言的に書ける。`Haori.sum` としても公開。
- 任意（カスタム）イベントで手続きを起動する `data-on` を追加した。`data-on="イベント名"` ＋ `data-on-*`（`data-on-run` / `data-on-fetch` / `data-on-bind` …、アクション語彙は `data-{event}-*` と共通）で、`window` / `document` へ dispatch された任意のカスタムイベントを契機に手続きを実行できる。ネイティブ橋の準備完了通知など、click / change / input / load 以外のイベントで初期化処理を宣言的に書ける。イベント名は属性値で保持する（属性名の小文字化で camelCase が壊れないため）。`window` のキャプチャ購読1本で `window` / `document` いずれの dispatch も二重発火なく受け、`data-import` 等で後から挿入された `data-on` も購読対象へ追加する。`click` / `change` / `input` / `load` を `data-on` に指定した場合は警告ログを出し購読しない（組み込みは `data-{event}-*` を使用）。
- 認証ガード `data-unauthorized-redirect` / `data-forbidden-redirect` を追加した。`<body>` または `<html>` に宣言すると、Haori の fetch 応答が 401（Unauthorized）/ 403（Forbidden）のときに指定 URL へ遷移する。イベント発火の fetch・宣言的 `data-fetch`・`data-import` の全 fetch 経路に適用し、属性値は `{{...}}` 式で記述できる。ステータス別のオプトイン（属性の有無）で、401/403 で意味の異なる遷移を個別に制御できる。現在ページ自身への遷移は無限ループ防止のため行わない。

### Library

- 上記の回帰テストを追加した（`tests/checked_selected_focus_guard.test.ts`・`tests/repro_report5_full.test.ts`・`tests/get_binding_data.test.ts`・`tests/custom_event_trigger.test.ts`・`tests/auth_guard.test.ts`、`tests/builtins.test.ts` の `sum` ケース）。

## [0.16.0] - 2026-06-11

### Fixed

- `Core.setBindingData` の並行呼出で適用順が逆転する不具合を修正した。同一要素へ短い間隔で複数回呼ぶと、内部キューの完了順が呼出順と逆転し、先に呼んだ古いデータの `data-bind` 属性が後から確定して新しい値を上書きすることがあった（`skipMutationAttributes` ガードにより2回目以降の属性書き込みがスキップされていた）。修正後は、フラグメント単位で DOM 反映・再評価を呼出順（FIFO）に直列化する。内部バインドデータは従来どおり呼出時点で同期確定し、`data-url-param` 等の再評価中の再入は即時実行してデッドロックを避ける。

### Added

- `data-input-*` を追加した。`data-change-*` と同様の手続きを `input` イベント（1文字ごとの逐次入力）でも起動できる。テキスト系入力での逐次絞り込み等を、`input`→`change` 転送なしに宣言的に書ける。`data-input-*` を明示した要素のみが対象（オプトイン）で、`data-input-form` 等の指定がなくても自動的にフォームを検出して双方向バインディングへ反映する点は `change` と同じ。
- radio / checkbox の `checked` と option の `selected` を宣言バインドで DOM プロパティまで同期するようにした。`checked="{{式}}"`・`data-attr-checked`・`data-attr-selected` で、属性だけでなくチェック状態・選択状態（`element.checked` / `option.selected`）が反映される。

### Changed

- `value="{{式}}"` を持つ入力は、フォーカス中（ユーザー編集中）はサブツリーの再評価で式の評価結果を再適用しないようにした。別要素起因の `setBindingData` や `data-fetch` 完了でユーザーの未コミット入力が巻き戻る問題を防ぐ。コミット済みの値は `change` イベントでバインド側へ反映される。フォーカスが外れている入力には従来どおり再適用する（light DOM / Shadow DOM 双方のフォーカスを判定）。

### Library

- 上記の回帰テストを追加した（`tests/binding_serialization.test.ts`・`tests/value_focus_guard.test.ts`・`tests/input_event_trigger.test.ts`・`tests/checked_bind.test.ts`）。

## [0.15.1] - 2026-06-10

### Fixed

- `Form.reset`（`data-{event}-reset` / `data-{event}-reset-before`）がリセット後に内部値と双方向バインディングのバインドデータを再同期するよう修正した。従来は内部値のクリアとネイティブリセットのみ行っていたため、リセット前に変更イベントでバインドデータへ書き込まれた値が再評価時に復元され、画面上は既定値に戻っているのに古い値が送信される不具合があった。修正後は、(1) `data-bind` 属性で宣言された初期バインドデータの復元、(2) HTML 属性（`value` / `selected` / `checked`）で宣言された既定値の DOM 反映、(3) 内部値とバインドデータの DOM 値への再同期、の順で初期状態へ戻る。
- コンテナ要素のリセット時、配下の `<form>` がネイティブリセットの対象に含まれない問題を修正した（入れ子フォームを個別にリセットする）。
- 同名チェックボックスグループの値収集を修正した。従来は `getValues` で最後の同名要素の値が勝つため、未チェックの要素があるとチェック済みの値が `null` で上書きされ、change イベントの双方向バインディング反映でチェックが即座に解除される不具合があった。修正後は、チェック済みの値だけを収集し（複数チェック時は配列）、配列値の反映時は自身の `value` が配列に含まれるかでチェック状態を決定する。`value="true"` / `value="false"` の boolean チェックボックスとラジオボタンの単一値挙動は維持する。なお、リセット後のバインドデータ更新は、バインドデータを一度も持っていないフォームには行わない（祖先バインド参照のシャドーイング防止）。

### Changed

- 上記修正に伴い、`data-{event}-reset-before` 後の payload は `null` ではなく HTML 属性・初期 `data-bind` で宣言された既定値を使用するようになった（仕様書の「リセット後の値を基準に評価します」に準拠）。

### Library

- リセット後の値再同期（セレクトボックスの `selected` 既定値・テキスト入力・入れ子フォーム・`data-form-arg`）の回帰テストを追加した。

## [0.15.0] - 2026-06-08

### Added

- 組み込みヘルパー（予約名前空間 `haori`）に `haori.findBy(array, key, value)` を追加した。配列から `item[key]` が `value` に一致する最初の要素を**文字列化比較**で返す（数値 ID と文字列 ID の差を吸収）。一致が無ければ `null`（非配列・空配列も `null`）。先頭フォールバックは式側で `haori.findBy(...) ?? items[0]` と書く。公開 API `Haori.findBy` としても提供。
- クリックの祖先委譲に境界属性 `data-click-passive` を追加した。これを持つ要素に到達すると、それより外側の `data-click-*` へは遡上しない。フォーム入力欄を囲むコンテナに付けると、入力欄クリックが外側のクリックアクションを誤発火させるのを防げる。境界自体はトリガーではなく、内側に `data-click-*` を持つ要素があれば最近接優先で従来どおり発火する（既定無効・後方互換）。
- フェッチ結果をバインド前に式変換する `data-{event}-bind-transform`（非イベント: `data-fetch-bind-transform`）を追加した。式中ではレスポンス全体を `response` として参照でき（例 `response.map(item => ({...item, id: null}))`）、戻り値をバインド対象とする。`bind-params` / `bind-arg` / `bind-append` より前に適用される。

### Documentation

- 上記3機能の仕様・用例を specs.md・README に追加した。あわせて、フェッチ結果を状態のサブキーへマージする用途は既存の `data-click-bind-arg`（対象自身の最新 binding を基底に当該キーだけ patch）で実現できること、`type="number"` の入力は 0.13.0 以降すでに数値型で収集されるため `data-form-type` のような追加属性は不要であることを明記した。

### Library

- `findBy`（一致・文字列化比較・不一致 null・非配列/空配列・要素 null 混在）、`data-click-passive`（境界での遡上停止・内側トリガーの発火・対照ケース）、`data-click-bind-transform`（配列の式変換＋ bind-arg キーへの反映と他キー保持）の回帰テストを追加した。

## [0.14.0] - 2026-06-08

### Added

- 組み込みヘルパー（予約名前空間 `haori`）に月次ユーティリティ `haori.monthAdd(value, delta)` と `haori.monthRange(count, base?)` を追加した。`monthAdd` は `YYYY-MM` 形式の年月へ月数を加算して `YYYY-MM` で返す（`Date` を介さない整数演算でタイムゾーン非依存。不正な入力・月が 1〜12 外は空文字、`delta` が 0 のときは正規化して返す）。`monthRange` は基準月から過去方向へ `count + 1` 個の `{targetMonth, label}`（`label` は `YYYY/MM`）を降順で返す（`base` 省略時は現在月。月セレクト・月次ナビゲーション向け）。公開 API `Haori.monthAdd` / `Haori.monthRange` としても同一実装を提供。
- 組み込みヘルパーにページ件数サマリー `haori.pageSummary(page, visibleCount?)` を追加した。Spring Data の `Page` 相当（`number`・`size`・`totalElements`／`totalCount`）から `{start, end, total, empty}` を計算する（`number` は 0 始まり、末尾ページの端数は `visibleCount` →`page.numberOfElements`→`size` の順で算出、総件数 0・非オブジェクトは `empty`）。`1 - 20 / 100 件` のような表示の算出元。公開 API `Haori.pageSummary` としても提供。

### Documentation

- 上記ヘルパーの仕様・用例を specs.md・guide.md・README に追加した。`monthRange` は `base` 省略時に現在月へ依存する（他ヘルパーの冪等性とは異なる）点、再評価で結果を固定したい場合は `base` を明示する点を明記した。
- 専用属性を追加せず既存機能の組み合わせで実現できるパターンを guide.md にレシピとして追加した（いずれもライブラリ実装は変更なし。end-to-end の捨てテストで動作を確認済み）: ①state 配列の追加・削除（式の `[...arr, x]`／`filter` ＋ JSON 形式 `data-click-data` ＋ `data-click-bind-merge`）、②フォーム値と state 配列の合成送信（`data-click-form` ＋ `data-click-data` の payload 統合）、③クリップボードコピー（`data-click-run` の `navigator.clipboard.writeText` ＋ `data-click-toast`）、④送信前のペイロード加工・空行除外（`data-click-before-run`）。

### Library

- `monthAdd`（加算・桁またぎ・正規化・不正入力）・`monthRange`（降順生成・`count=0`・現在月基準・不正入力）・`pageSummary`（先頭/途中/末尾端数・`numberOfElements`/`totalCount`・空）の単体テストと、式評価エンジン経由の呼び出しテストを追加した。

## [0.13.2] - 2026-06-07

### Fixed

- `type="number"` の数値化を `value="{{...}}"`（value 属性のテンプレート評価）経由にも適用した。従来は `syncValue` / `applyValue` 経由のみ正規化され、属性評価経由では文字列のまま内部値に残っていた（`data-attr-value` は仕様上 `input.value` を同期しないため対象外）。
- `haori.date()` でフォーマット中の英字（`y M d H m s`）が常にトークンとして解釈され、リテラル文字（例 `'Month'` の `M`）が誤って置換される問題に対し、シングルクォートによるリテラルエスケープ（例 `yyyy-MM-dd'T'HH:mm`、`''` は `'` 1文字）を追加した。
- `haori.number()` で空白のみの文字列が `0` になっていたのを空文字に修正し、数値文字列の前後空白を無視するようにした。
- `haori.pages()` で「隠れるページが1つだけ」のとき（ギャップが2）でも省略記号を出していたのを、その番号を表示するよう改善した（例 `pages(5, 2, {window: 0})` → `1 2 [3] 4 5`）。
- `EventDispatcher` のクリック委譲・`data-{event}-prevent`・`data-click-defer` 判定がプレフィックスを `data-` でハードコードしていたのを `Env.prefix` 基準に修正した。`data-prefix` でプレフィックスを変更した場合に委譲・prevent・defer が機能しなかった既存の不具合を解消（既定 `data-` では挙動不変）。
- 式評価で予約名前空間 `haori` の参照を検出する際、文字列リテラル（`'...'` / `"..."`）内の `haori` を識別子と誤認して不要な注入・警告を出していたのを、検出前に文字列リテラルを除外して回避した（テンプレートリテラルの `${...}` は対象外）。

### Documentation

- 上記の挙動（date のエスケープ、number の空白扱い、pages の単一ページ表示、number 収集経路と `data-attr-value` の非対象、エラー応答 `errors` 配列の改行連結）を specs.md・guide.md に反映した。

### Library

- date エスケープ、number の3桁丸め・空白、range の非整数/上限、pages の単一ページ/boundary、式の文字列リテラル除外、value 属性経由の number 収集、カスタムプレフィックス時の prevent/委譲、change での prevent 無害性を検証する回帰テストを追加した。

## [0.13.1] - 2026-06-07

### Packaging

- npm パッケージの同梱物を実行時成果物だけに絞った。`files` を `dist` 一括指定から `dist/haori.es.js` / `dist/haori.cjs.js` / `dist/haori.iife.js` / `dist/index.d.ts` の明示指定へ変更し、これまで同梱されていたコンパイル済みテスト（`dist/tests/**`）・ソースマップ（`*.map`）・型宣言の個別ファイル（`dist/src/**`）・設定ファイル（`dist/vite.config.*` 等）を公開物から除外した（unpacked size を縮小。ランタイム動作・CDN 配信・型解決には影響なし）。
- `exports` を実在ファイルへ修正した。`import` 条件が存在しない `./dist/haori.js` を指していた不整合を `./dist/haori.es.js` に直し、`require`（`./dist/haori.cjs.js`）と `types`（`./dist/index.d.ts`）条件、および `module` / `unpkg` / `jsdelivr` フィールドを追加して、ESM `import`・CJS `require`・型・CDN の各解決を正しくした。型宣言（`dist/index.d.ts`）はロールアップ済みで自己完結のため、`dist/src` を除外しても型解決に影響しない。

## [0.13.0] - 2026-06-07

### Changed

- `type="number"` の `<input>` を**数値型**として収集・バインドするようにした。HTML の入力値は常に文字列だが、バックエンドの DTO が `Double` / `Integer` を期待する場合に文字列で送られて型不一致になるのを防ぐ。空文字・数値化できない値は `null`、小数はそのまま数値になる。`ElementFragment` の内部値正規化（`normalizeValueForElement`）を、DOM→内部値（`syncValue`：`change`・構築時）とバインド→内部値（`applyValue`）の両経路に適用し、`Form.getValues()` の結果や JSON 送信ボディに数値として現れる。**挙動変更**: 0.12.x までは `type="number"` も文字列で収集していた。文字列のまま扱いたい場合は `type="text"` を使う

### Library

- `type="number"` の数値型変換について、静的 value 属性・小数・空値（`null`）・`change` 入力後・数値文字列バインド経由の各ケースを検証する回帰テストを追加した。挙動変更に伴い、既存のフォーム収集テストの期待値（`type="number"` の項目）を数値に更新した

### Documentation

- 既存機能で実現できる利用パターンを文書化した（コード変更なし）:
  - サーバの 4xx レスポンスが `{"errors": {"フィールド名": "メッセージ"}}`（配列も可）形式のとき、`key` が `name` 一致のフィールドへ自動振り分けされ、haori-bootstrap 併用時は `invalid-feedback` / `is-invalid` が自動描画されること（フィールド別エラー表示。トップレベル配列形式は未対応）
  - `data-if` + 1つの状態キーで「同時に1つだけ開く」排他パネル／アコーディオンを宣言的に表現できること
  - `data-click-fetch` を伴わない `data-click-data` + `data-click-bind`（`data-click-bind-merge` 併用可）で、フェッチなしに任意 JSON を state へ反映できること
  - `data-click-click`（複数要素にマッチ可）で複数の隠し要素のクリックを発火し、各々の `data-click-bind-arg` で同じ要素の別キーへマージすることで、連番属性なしに複数エンドポイントの取得結果を1つの state へ統合できること（取得は非同期で完了は待たない点に注意）

## [0.12.0] - 2026-06-05

### Added

- テンプレート式から呼び出せる組み込みヘルパーを追加した。式中の予約名前空間 `haori` として注入され、`haori.date(value, format?)`（日時整形）・`haori.number(value, decimals?)`（数値整形）・`haori.range(start, end?, step?)`（整数レンジ）・`haori.pages(totalPages, current, options?)`（省略記号付き番号ページネーション）を提供する。いずれも副作用のない純粋関数で、`data-each="haori.pages(...)"` や `{{ haori.date(lastUpdatedAt, 'yyyy/MM/dd HH:mm') }}` のように宣言的に利用できる。同じ関数は公開 API `Haori.date` / `Haori.number` / `Haori.range` / `Haori.pages` としても利用できる。`pages()` の `current` は 0 始まり（Spring の `Page.number` 等）を想定し、各要素の `label` は表示用に 1 始まりへ変換される
- `data-{event}-prevent`（主に `data-click-prevent`）を追加した。指定要素のイベントでブラウザのネイティブなデフォルト動作（`type="submit"` のフォーム送信や `<a href>` の遷移）を抑止する。クリックの同期区間で `preventDefault()` を呼ぶため `data-click-defer` と併用しても確実に抑止でき、`stopPropagation()` は呼ばないので他ライブラリのイベント伝播には影響しない。これにより `type="submit"` のまま `data-click-fetch` 等を付けても、ページ再読込なしにフェッチ・トースト・リダイレクトが機能する

### Library

- `haori` は式評価エンジンの予約名前空間となり、`data-bind` で同名のキーを与えても式中では組み込みヘルパーが優先される（開発モードでは警告を出す）。式が `haori` を独立した識別子として参照する場合のみヘルパーを注入し、参照しない式には影響しない
- 組み込みヘルパー（date/number/range/pages）の単体テスト、式評価への注入・予約名の上書き不可・プロパティアクセス非干渉の回帰テスト、`data-{event}-prevent` の送信抑止・オプトイン・`data-click-defer` 併用の回帰テスト（vitest）、および実ブラウザでの日時整形・番号ページネーション・送信抑止を検証する Playwright e2e（`demo/builtins/haori-builtins-demo.html`）を追加した

## [0.11.1] - 2026-06-03

### Documentation

- `data-{event}-run` の `{{...}}` 展開結果が実行コードへ文字列結合される（信頼できない値を入れると任意コード実行＝XSS になり得る）旨のセキュリティ警告を、guide.md・specs.md（属性詳細とセキュリティ章）に追記した。信頼できる値のみを `{{...}}` に入れ、信頼できない値は `data-bind` 経由で関数内部から参照する旨も明記した

## [0.11.0] - 2026-06-03

### Added

- `data-{event}-run` を追加した。フェッチを伴わないクリック等のイベントで任意の JavaScript を実行する（`data-click-run` / `data-change-run` など）。属性値は `{{...}}` をレンダリング時に展開した文字列を本体として `new Function('event', ...)` で実行する（`data-{event}-before-run` / `-after-run` と同じ実 JS 実行方式）。本体が `false` を返した場合のみ `event.preventDefault()` を呼ぶ（`onclick="return false"` / jQuery と同じ慣習）。`event` を引数で受け取れるため `stopPropagation()` 等も明示できる。`data-click-fetch` と併用した場合は run を同期実行してから fetch を継続する（run の戻り値は preventDefault のみを制御し fetch は中止しない）。評価・実行エラーは `Log.error` で報告する

### Library

- `data-{event}-run` のクリック実行・`{{...}}` 展開・`return false` での preventDefault・戻り値なしでの非抑止・エラー時の Log.error・change イベントでの動作を検証する回帰テストを追加した

## [0.10.1] - 2026-06-03

### Changed

- 式評価で使用できないキーワード（`function`・`return` などのステートメント系キーワード）を含む式が失敗した場合、従来の汎用的な `Expression contains dangerous patterns` ではなく、検出したキーワード名と「アロー関数を使う（`x => ({key: value})`）」具体的なヒントを併記した警告を出すようにした。`data-derive` で `function(m){return {...}}` を使うと式が `null` になり行が描画されない、といった原因の分かりにくい事象の特定を容易にする（プロパティ名や部分一致は誤検出しない）

### Library

- 使用できないキーワードを含む式の評価失敗時に、具体的なヒント付き警告を出すこと・正常なアロー関数式や部分一致識別子では出さないことの回帰テストを追加した
- `data-derive` → `data-each` 連鎖の行描画が `Haori.waitForRenders()` で待機できること、各行に nested `data-fetch` を含む場合も待機できることの回帰テストを追加した

## [0.10.0] - 2026-06-03

### Fixed

- `data-each` がテンプレート（最初の要素子）をフラグメント木から見つけられない場合に、対象要素の DOM 要素子からテンプレートを復旧するフォールバックを追加した。Bootstrap タブペイン内のネスト `data-if` 配下を `data-click-bind-arg` で更新する特定フローで、フラグメント木と DOM の子が同期せずテンプレート未検出となり一覧が描画されない（`{{...}}` が未評価のまま1行残る）不具合を修正した。フォールバック時は先頭の要素子をテンプレートとし、`each-before`/`each-after` 以外の残留要素子（未追跡の素の DOM ノードを含む）もまとめて除去してから差分更新を開始するため、複数の残留行があっても全行が正しく再構成される
- `data-*-bind-arg` のバインドを、バインド先「自身の」binding（`getRawBindingData`）のみを基底に `bindArg` キーだけ更新するようにした。従来は `getBindingData()`（継承込み）を基底にしていたため、継承キーがバインド先の `data-bind` に混入していた。読み取り〜書き込みは同期で行われ呼び出し単位で原子的なため、並行・リアクティブな複数 `bind-arg` が重なっても各キーが保持される（互いに上書きしない）

### Added

- `data-click-defer` を追加した（依頼4）。指定時は Haori のクリック手続きをクリックイベントの同期実行中ではなく次フレーム（`requestAnimationFrame` / `setTimeout(0)`）へ遅延し、Bootstrap の collapse トグルなど他ライブラリの同期 click ハンドラを先に完了させる。`data-click-no-disabled` と併用できる（ただし再描画による命令的クラスの上書き＝別要因はこれでは解消しない）

### Changed

- 式評価で使用できない（ブロックされた）グローバル識別子（`Object` など）を参照して評価に失敗した場合、コンソールに「`blocked identifier(s): …`」という明示的な警告を出すようにした。`Object.assign(...)` のように原因の分かりにくい `TypeError` になるケースで、原因特定とスプレッド構文への移行を促す（プロパティアクセス `foo.Object` は誤検出しない）
- 開発モード（`Dev.enable()`）で `data-if` 式が falsy（非表示）と評価されたとき、その式と参照識別子の解決値・由来（`dumpScope` の `sources`）をコンソールに出力するようにした（依頼3）。`data-if="!(dialog?.id || id)"` の `id` がフォーム入力等の想定外スコープで解決される問題のデバッグに役立つ

### Documentation

- `data-each` の配置ルール（コンテナ要素に付与し最初の子要素をテンプレートとする。テーブルは `<tbody data-each>`、`<tr data-each>` は誤り）を `guide.md` に明記した
- 式でブロックされる識別子に `Object` を明記し、`Object.assign` の代わりにスプレッド構文 `{...a, ...b}` を使う旨と、ブロック識別子参照時の警告について `guide.md` に追記した
- `name="id"` フォーム内のスコープ競合を避ける `data-derive` 推奨パターン（クリーンなスコープで判定値を計算し一意名で公開）と、開発モードの falsy `data-if` 診断出力について `guide.md` に追記した（依頼3）

### Library

- フラグメント子と DOM の子が同期しない状況でも `data-each` がテンプレートを復旧して全行描画することの回帰テストを追加した
- `data-*-bind-arg` が継承キーを混入させず、連続バインドで各 `bindArg` キーを保持することの回帰テストを追加した
- 式評価でブロック識別子（`Object` 等）を参照したとき警告を出すこと・プロパティアクセスを誤検出しないことの回帰テストを追加した
- `data-click-defer` 指定時に Procedure がクリック同期中ではなく次フレームで起動すること、未指定時は同期起動することの回帰テストを追加した
- 開発モードで falsy な `data-if` の式と参照スコープを出力すること・無効時は出力しないことの回帰テストを追加した

## [0.9.0] - 2026-06-03

### Fixed

- `haori:bindcomplete` を、バインド操作だけでなく**バインド起因の `data-if` 表示切り替えと `data-each` 差分描画の DOM 反映完了後**に発火するよう強化した。`evaluateEach` の再入時の再実行を fire-and-forget から待機可能（loop-until-stable な単一 settle Promise を共有）へ変更し、`evaluateAll`→`setBindingData`→`haori:bindcomplete` が最終描画まで確実に待つようにした

### Added

- `Core.dumpScope(element)` を追加した（ブラウザでは `Haori.Core.dumpScope(element)`）。式の識別子解決で見えるスコープ（`resolved`）と各キーの由来要素・種類（`sources`: `bind` / `derive`）を返し、開発モード（`Dev.enable()`）時はコンソールにも出力する。スコープ衝突のデバッグに利用できる
- `data-each` の描画完了を外部テストから検知する手段を追加した（依頼3）。`data-each` が最新データで全行の描画を完了すると対象要素に `data-each-done` 属性を付与する（更新開始で除去、安定完了で再付与）。Playwright 等は `waitForSelector('[data-each-done]')` で待機できる
- `Haori.waitForRenders()`（および `Queue.waitForIdle()` / 最上位 `waitForRenders` エクスポート）を追加した。進行中・追従投入分を含むすべてのレンダリングタスクの完了を待つ `Promise<void>` を返す。タブ切り替えやクリック後の複数描画をまとめて待機でき、イベント購読タイミングの競合を回避できる
- `data-click-no-disabled` を追加した（依頼4）。指定時はクリック手続き中にボタンへ native `disabled` を付与しない。Bootstrap など他ライブラリの click ハンドラや CSS が `disabled` 要素を無視する問題を回避でき、Haori 内部の多重実行ガード（`data-haori-click-lock` / WeakSet）は維持される
- `data-form` 属性を追加した。`<form>` を直接置けない場所（`<table>` 内の `<tr>` など）でも、`data-form` を付けた任意の要素を `data-click-form` 等の値収集コンテナとして扱える。`Form.getFormFragment` が `<form>` に加えて `data-form` 要素を認識する（値収集専用で、双方向バインディングは行わないため `data-each` 行の binding data を壊さない）

### Documentation

- 式の識別子解決スコープ（DOM ネスト優先・`data-derive` の位置づけ・グローバル）と、**フォーム入力値は change／明示同期までスコープに投入されない**ことを `guide.md` に明文化した。あわせて `haori:bindcomplete` の発火タイミング保証（DOM 反映完了まで）を追記した
- 外部テスト（Playwright 等）から `data-each` の描画完了を待機する手段（`data-each-done` 属性 / `Haori.waitForRenders()`）を `guide.md` に追記した
- `data-click-no-disabled` と他ライブラリ（Bootstrap 等）との共存（Haori はイベント伝播を止めないこと、`disabled` 付与の回避、命令的クラス上書きの注意）を `guide.md` に追記した

### Library

- `haori:bindcomplete` 発火時点で `data-if` 表示・`data-each` 全行が反映済みであること、`Core.dumpScope` のスコープ解決・由来情報（内側優先、フォーム未同期時の外側フォールバック）に関する回帰テストを追加した
- `data-each-done` の付与/除去タイミングと `Haori.waitForRenders()` / `Queue.waitForIdle()` による描画完了待機に関する回帰テストを追加した
- `data-click-no-disabled` 指定時に `disabled` を付与しないこと・多重実行ガードが維持されることの回帰テストを追加した
- `data-form` を form コンテナとして認識すること、`<tr data-form>` のテーブル行で `data-click-form` が行 id を保持したまま入力値を収集・送信できることの回帰テストを追加した

## [0.8.0] - 2026-06-03

### Changed

- `data-load-*` を `data-if` の表示（`haori:show`）と連動させ、非表示→表示への遷移時にも発火するようにした。`<button>` や `<div>` などネイティブの `load` イベントが発生しない要素でも、表示を契機とした処理を定義できる。発火は遷移時のみで、表示状態のままの再評価では再発火しない
- `data-if` の非表示判定を JavaScript の falsy 準拠に統一し、数値 `0` と空文字列 `''` も非表示とした（従来は `false`・`null`・`undefined`・`NaN` のみ非表示）。`data-if="items.length"` が要素数 0 のとき意図どおり非表示になる。空配列 `[]`・空オブジェクト `{}` は従来どおり truthy として表示される

### Fixed

- `data-each` の差分更新（`updateDiff`）を再入制御で直列化し、`data-*-bind-arg` などでバインド直後にリアクティブ再評価が重なった際に、同一 `data-each` への並行評価で行が重複・欠落したり描画が停止したりする不具合を修正した。実行中の再評価要求は記録し、現在の更新完了後に最新データで一度だけ再実行する

### Added

- `data-*-bind-merge`（`data-click-bind-merge`・`data-change-bind-merge`・`data-load-bind-merge`・`data-intersect-bind-merge`・`data-fetch-bind-merge`）を追加した。指定時はバインド先の既存 `data-bind` を保持したまま解決済みデータを浅くマージ（未指定キーを保持）する。未指定時は従来どおり全置換。計算値（例: `selectedId={{items[0].id}}`）を既存 state にマージしたいケースで利用できる
- `haori:bindcomplete` イベントを追加した。`data-*-bind` / `data-*-bind-arg` などによるバインドと対象要素配下の再評価（`data-if` / `data-each` 等）の完了時に対象要素で発火し、`detail.bindArg`（使用したネストキー、無指定なら `null`）を提供する。外部スクリプトからのバインド完了同期に利用できる

### Documentation

- `haori:eachupdate` の発火タイミングを仕様として明文化した。`data-each` の差分で追加・削除・並べ替えされた全行が DOM に反映され、各行の内容描画（`{{...}}` 補間等）が完了した後に発火することを保証し、大量行が複数フレームに分割描画される場合でも全フレーム完了後に1回発火するため**外部からの描画完了検知に利用できる**ことを `guide.md` に記載した（`detail`: `added` / `removed` / `order` / `total`）

### Library

- `data-load-*` の表示連動発火（遷移時の1回発火、表示維持時の再発火なし、`data-load-*` 非保持要素では未起動）に関する回帰テストを追加した
- `data-if` の falsy 判定（`0`・`''` で非表示、`[]`・`{}` で表示）に関する回帰テストを追加した
- `data-*-bind-merge` の浅いマージ（既存キー保持・上書き）と未指定時の全置換に関する回帰テストを追加した
- `data-*-bind-arg` バインド後の `data-if` 表示・`data-each` 全行描画、同一 `data-each` への並行評価の安全性、`haori:bindcomplete` 発火に関する回帰テストを追加した
- `haori:eachupdate` が「全行の描画完了後」に発火すること（25行で発火時点に全行補間済み）と、`detail` の `added` / `removed` / `order` / `total` に関する回帰テストを追加した

## [0.7.0] - 2026-06-01

### Changed

- `data-click-copy-source` 属性を追加し、`data-click-copy` のコピー元要素を CSS セレクタで明示指定できるようにした。指定がない場合は従来通り `data-click-form` またはトリガー要素の binding data を参照する。空文字指定は自要素をコピー元とする
- `data-click-click` で click を発火する前に `Core.evaluateAll()` を呼ぶようにし、bind 完了後の最新 DOM をクリック先要素が確実に参照できるようにした

### Library

- `data-click-copy-source` のフォーム・binding data・空文字・優先順位・エラーケースを確認する回帰テストを追加した
- `data-click-click` の post-bind 再評価に関するコメントを追加した

## [0.6.2] - 2026-05-26

### Changed

- `data-derive` で入力シグネチャが不変な subtree の再評価をスキップし、重い一覧の再描画コストを抑えた
- development mode の evaluation profiler で、属性・テキスト・プレースホルダごとの `calls`、`totalDurationMs`、`maxDurationMs` を取得できるようにした
- 未宣言識別子を含む optional chaining とフォールバック式では、必要な識別子だけを `undefined` として補完し、初期 `data-bind` が空でも後段の代替値まで評価できるようにした

### Library

- `data-derive` のスキップ判定と evaluation profiler の duration 集計に関する回帰テストを追加した
- 未宣言識別子を含む optional chaining の回帰テストを追加し、未解決参照の副作用抑止を維持したままフォールバック評価できることを確認した
- npm publish の権限不足が `E404` に見えるケースを公開手順へ追記し、リリース時の切り分けをしやすくした

## [0.6.1] - 2026-05-22

### Changed

- `data-each` の描画経路を見直し、入力シグネチャ比較・差分更新・再利用行評価・fresh clone 初期化の無駄を減らして大きな一覧の描画負荷を下げた
- plain nested `data-each` では、入力と要素自身の動的状態が不変な場合に子 `data-each` の再評価をスキップするようにした
- 式評価の危険値チェックと `MutationObserver` 周辺のオーバーヘッドを削減し、大量再評価時のコストを抑えた

### Library

- `data-each` の並び替え・追加削除同時更新・再利用行・fresh clone 初期化・nested `data-each` の同値スキップを確認する回帰テストを追加した
- `customers.html` 相当の重い描画パスを前提に最適化を見直し、Vitest・Playwright・lint を通して回帰がないことを確認した

## [0.6.0] - 2026-05-19

### Changed

- `data-derive` / `data-derive-name` を追加し、descendant-only な派生値を `data-if` / `data-each` / 通常式から参照できるようにした
- `data-derive` は初回 scan、`data-bind` 更新、form 更新、`data-derive` / `data-derive-name` の動的 add/change/remove 時に再評価され、子孫の `data-if` / `data-each` より先に反映されるようにした
- `select` に対する既存の `data-each` 経路で、`data-each-before` / `data-each-after` の前後位置が更新後も崩れないようにした

### Library

- `data-derive` のスコープ、名前衝突、未解決参照のクリア、form 更新、`data-if` false 枝、`data-fetch` / `data-import` / `data-fetch-bind` 連携、`data-each` 行ごとの独立性を確認する回帰テストを追加した
- 全体 Vitest 実行で不安定だった `tests/procedure-fetch-options.test.ts` を明示 `scan()` と DOM 後始末で安定化した

## [0.5.1] - 2026-05-16

### Changed

- `data-if` が false の枝では子孫評価を止め、非表示要素内の危険な式や不要な再評価が走らないようにした
- 初回に false だった `data-if` 配下が後から表示されたとき、未初期化の子孫を `scan()` で再初期化するようにした

### Library

- 初回 false の `data-if` 配下にある `data-each` / `data-fetch` / `data-import` が表示後に正しく初期化される回帰テストを追加した
- 表示済み subtree の `true -> false -> true` トグル時に `evaluateAll()` 側で再評価される回帰テストを追加した

## [0.5.0] - 2026-05-14

### Changed

- プレースホルダ解決規則を見直し、`data-if` / `data-each` / 通常属性 / テキストノード / `data-fetch` / `data-import` で、単体プレースホルダと文字列埋め込みの未解決参照・`false`・`null`・`undefined`・空文字の扱いを統一した
- `data-url-param` を入力系属性として `data-if` / `data-each` / 通常属性 / 副作用属性より先に反映するようにした
- `data-each` は未解決参照および `false` / `null` / `undefined` を空配列として扱うようにした
- 通常属性と `data-attr-*` は、テンプレート式を含む場合だけ空結果で削除し、リテラル空文字属性は保持するようにした

### Library

- プレースホルダ解決規則を種類別に網羅する回帰テストを追加し、`data-fetch` / `data-import` / `data-if` / `data-each` / 通常属性 / `data-attr-*` / テキストノード / 評価順を確認できるようにした
- 行追加・削除・移動系の回帰テストと full Vitest 実行で、空値属性を使う `data-click-row-*` が影響を受けないことを確認した
- jsdom 下の再初期化で stale な `MutationObserver` と `HTMLElement` 判定に引きずられないようにし、Observer / Intersect 周辺のテストを安定化した

## [0.4.17] - 2026-05-13

### Changed

- `data-fetch` と `data-import` で未解決参照を含む評価サイクルの副作用実行を止め、後続の bind 更新で参照解決後にだけ再評価するようにした
- `data-fetch` の自動再評価を URL 比較ではなく、URL・HTTP メソッド・ヘッダー・body を含む実行シグネチャ比較で判定するようにした

### Library

- 未解決参照時の `data-fetch` / `data-import` 抑止と、解決後の再評価条件を確認する回帰テストを追加した
- README、README.ja.md、`docs/ja/guide.md`、`docs/ja/specs.md` を新しい `data-fetch` / `data-import` 仕様に合わせて更新した

## [0.4.16] - 2026-05-06

### Changed

- `data-click-copy-params` で include / exclude を併用できるようにし、除外だけ指定した場合も全件コピーから差し引けるようにした

### Library

- `data-click-copy-params` の exclude-only、include/exclude 併用、空トークンと前後スペースの回帰テストを追加した

## [0.4.15] - 2026-05-06

### Changed

- `data-click-reset-before` の評価順を調整し、送信前のリセット後に `data` / `form` / `history` を評価するようにした

### Library

- `data-click-reset-before` と history スナップショットの回帰テストを追加した

## [0.4.14] - 2026-05-06

### Changed

- click 実行時の再入防止で `disabled` 属性の再付与を抑止し、非 form control の disabled をロックとして扱えるようにした

### Library

- 非 form control の disabled 抑止と `data-click-form` 経路の回帰テストを追加した

## [0.4.13] - 2026-05-05

### Changed

- クリック委譲の対象を `data-click-*` 属性を持つ要素に限定し、入力欄など属性のない要素をクリックしたときに不要な `disabled` 付与や処理起動が起きないようにした

### Library

- クリック委譲の対象判定と、`dispatchEvent()` 直後の `disabled` 状態および `Procedure.run` の未起動を確認する回帰テストを強化した

## [0.4.12] - 2026-05-03

### Changed

- `data-if` の非表示処理で inline style の `display: ... !important` を保持し、`hide()` / `show()` の再入でも元の表示状態を壊さないようにした

### Library

- `display: ... !important` の元値保持と `hide()` / `show()` の連続呼び出しを検証する回帰テストを追加した

## [0.4.11] - 2026-05-03

### Changed

- `MutationObserver` 経由の書き戻しで `attributeMap` がテンプレート式を失わないようにした
- `data-attr-*` も `Observer` 経由の書き戻し抑止対象に含め、展開済み値での上書きを防ぐようにした

### Library

- `fromObserver` フラグの伝播と `data-attr-src` の回帰テストを追加した

## [0.4.10] - 2026-05-02

### Changed

- `data-each` 配下で `data-fetch` 経由の再評価でも `data-if` と通常属性の展開が安定するようにした
- `waitForCondition` に `delayMs` を追加し、`scheduleEvaluateAll` の完了確認を DOM 状態ベースで行いやすくした

### Library

- `data-if` と `href` プレースホルダの共存、および `data-fetch` 経由の回帰テストを追加した
- `waitForCondition` の `delayMs` を使った CI 耐性のある待機テストを追加した

## [0.4.9] - 2026-05-01

### Changed

- `data-click-fetch` など click 手続きの実行中は起点要素へ `disabled` 属性を付与し、同一要素の重複実行を抑止するようにした

### Library

- click 手続きの `disabled` 属性付与と重複実行抑止の回帰テストを追加した

## [0.4.8] - 2026-05-01

### Changed

- `data-haori-ready` を `body` に付与し、初期化完了前のちらつきを CSS で抑えやすくした
- `data-import` の取り込み直後スキャンを初期化中に限定し、Observer 起動後の二重初期化を防いだ
- `popstate` で Haori 管理の history だけをリロード対象にし、ブラウザ戻る・進む後の状態崩れを抑えた

### Library

- `data-haori-ready`、`data-import`、`popstate` の回帰テストを追加した

## [0.4.7] - 2026-04-29

### Changed

- `data-each` 配下の `data-if` と通常属性の再評価順を見直し、ページネーションのような再利用行でも表示が崩れないようにした
- clone 時の runtime 表示状態を整理し、テンプレート由来の `hidden` が次の行へ持ち越されないようにした

### Library

- fetch / Procedure の統合テストを安定化し、`afterCallback` と `data-fetch-bind-params` の回帰を確認できるようにした

## [0.4.6] - 2026-04-28

### Changed

- 式評価で object literal を含む安全な式を扱えるようにし、ページネーションなどの表示モデル生成をテンプレート式で記述できるようにした
- object literal の key 位置でも `constructor`、`__proto__`、`prototype` を拒否し、getter / setter / async method / Unicode escape を使った危険キーの迂回を防止した

### Library

- object literal を返す `map()` と、ページネーション向けの `reduce()` 連鎖を評価する回帰テストを追加した
- object literal の危険キー拒否に関する回帰テストを追加した

## [0.4.5] - 2026-04-28

### Changed

- `data-{event}-scroll-error` を追加し、バリデーション失敗やエラー発生時に最初のエラー要素へスクロールできるようにした
- non-form target のエラー表示でも `data-message-level="error"` を起点にスクロールするようにした
- `validate` で最初の不正入力要素へフォーカスし、スクロールを 1 回だけ行うようにした
- `history.state` を使って Haori 管理の履歴だけを `popstate` リロード対象にし、外部の履歴遷移では `location.reload()` しないようにした

### Library

- `data-{event}-scroll-error` と non-form target / 複数 invalid の回帰テストを追加した
- `popstate` の state 判定と `start()` / `stop()` によるリスナー登録・解除の回帰テストを追加した

## [0.4.4] - 2026-04-27

### Changed

- `Haori.toast` で `success` レベルを扱えるようにし、`aria-live` をレベルに応じて切り替えるようにした
- `data-{event}-toast-level` を追加し、イベント属性からトーストレベルを指定できるようにした
- `Form.addMessage` / `Haori.addMessage` を追加し、`data-message-level` を使ったメッセージ表示に対応した
- `clearMessages` で `data-message-level` も削除するようにした
- `Haori.dialog` / `confirm` のリテラル `\n` 正規化を Procedure 経路に限定した
- `data-fetch-bind-arg` を `data-fetch-arg` の別名として扱い、仕様書とガイドで優先順位を整理した

### Library

- `toast` レベル、`data-{event}-toast-level`、`addMessage` / `clearMessages`、`dialog` / `confirm` の改行正規化、`data-fetch-bind-arg` の回帰テストを追加した

## [0.4.3] - 2026-04-25

### Changed

- `data-{event}-copy` と `data-{event}-copy-params` を追加し、イベント種別に依存しないバインディング値のコピーを扱えるようにした
- `reset` の後に `copy` を実行するようにし、フォームリセット後の値をコピーできるよう改善した
- `docs/ja/specs.md` と `docs/ja/guide.md` をコピー仕様に合わせて更新した
- `data-url-param` で URL に存在しないクエリパラメータをフォームへ書き戻さず、既存値を維持するよう修正した
- `data-attr-*` を追加し、ブラウザが先に解釈する属性を raw テンプレートを保持したまま安全に更新できるようにした
- `data-attr-value` は `value` 属性のみを更新し、DOM property を同期しない仕様に合わせて README とドキュメントを更新した

### Library

- `data-{event}-copy`、`copy-params`、`reset` 後のコピー順序を確認する回帰テストを追加した
- `data-url-param` の未存在キーをスキップする回帰テストを追加した
- `data-attr-src` の再評価と `data-attr-value` の属性専用更新を確認する回帰テストを追加した

## [0.4.2] - 2026-04-25

### Changed

- form 自身の `data-bind` / `Core.setBindingData()` 更新時に、フォーム配下の入力要素へ無イベントで逆方向同期するよう改善した
- `value="true"` を持つ checkbox を boolean モードとして扱い、未チェック時は `false` を返すよう改善した
- checkbox を含む `data-fetch-arg` デモを更新し、フォーム双方向バインディングの挙動を確認できるようにした

### Library

- フォーム逆方向同期、boolean checkbox、無限ループ防止の回帰テストを追加した

## [0.4.1] - 2026-04-25

### Changed

- `data-fetch` / `data-bind` 経由で `value="{{...}}"` を更新した際に、text input / textarea / select の `element.value` と内部値がずれないよう修正した
- フォーム入力バインディングの回帰テストを追加し、textarea / checkbox / `Form.getValues()` の経路を確認した

### Library

- 入力値同期の修正とフォーム回帰テストを追加した

## [0.4.0] - 2026-04-24

### Changed

- `data-runtime` を追加し、embedded / demo の実行モードで Procedure の挙動を切り替えられるようにした
- demo runtime では非 GET の `data-fetch` / `data-click-fetch` を GET + query に正規化し、`haori:fetchstart` の detail に runtime / requestedMethod / effectiveMethod / transportMode / queryString を追加した
- `data-click-history` のデモを追加し、`history.pushState` の URL 生成と履歴更新を画面上で確認できるようにした
- README、README.ja.md、docs/ja/specs.md を runtime / demo 仕様に合わせて更新した

### Library

- runtime / fetch 正規化 / history 更新の回帰テストを追加した
- runtime demo と history demo を追加した

## [0.3.0] - 2026-04-24

### Changed

- `data-intersect-*` による交差監視トリガーを追加し、無限スクロールや遅延読み込みに対応した
- `data-*-bind-append` を追加し、配列プロパティを既存データへ追記できるようにした
- `data-intersect-once` は実際に成功した場合のみ監視解除するように調整した

### Library

- `data-intersect-*` の動作確認テストを追加した
- `data-*-bind-append` の回帰テストを追加した

## [0.2.0] - 2026-04-23

### Changed

- `clone()` で `attributeMap` を引き継ぎ、`data-each` のクローン内でもテンプレート式が正しく評価されるよう修正した
- `setParent()` で親が変わらない場合はバインドデータキャッシュの再帰的なクリアを省略するよう改善した
- `history.pushState` を追加し、履歴 URL の生成と更新を扱えるようにした

### Library

- `clone()` と `setParent()` の回帰を確認するテストを追加した
- `history.pushState` の動作を確認する回帰テストを追加した

## [0.1.5] - 2026-04-23

### Changed

- `evaluateAll` で `{{...}}` を含む通常属性も再評価するよう改善し、`data-bind` 更新や `data-each` 再利用時の DOM 反映を揃えた

### Library

- `evaluateAll` の通常属性再評価と、false / null 評価時の属性削除を確認する回帰テストを追加

## [0.1.4] - 2026-04-22

### Changed

- `data-click-data` と `data-fetch-data` でテンプレート式を評価できるようにし、ページネーションなどで既存バインディングから送信値を組み立てられるよう改善
- JSON 形式の `data-*-data` で引用符を含む値が壊れないようにし、parameter 形式の `false` も空文字にせず送信できるよう改善

### Library

- `data` 属性解釈を event / non-event で共通化し、`&` や `=` を含むテンプレート値、JSON 文字列中の引用符、object 直返し、`false` 値の回帰テストを追加

## [0.1.3] - 2026-04-21

### Changed

- `data-each` を `tbody` に付けた場合の描画と再描画を修正し、テンプレート行が残る問題を解消
- Bootstrap モーダル連携時に `data-click-open` / `data-click-close` が `window.Haori` 経由でも正しく動作するよう改善

### Library

- Procedure と Form の Haori API 解決を見直し、`openDialog`、`closeDialog`、`addErrorMessage` の委譲先を差し替え可能に改善
- `data-each` とモーダル連携まわりの回帰テストを追加し、`max-len` を含む lint 失敗を解消

## [0.1.2] - 2026-04-09

### Changed

- 式評価の安全性を改善し、危険な識別子・予約語・プロトタイプ経由のアクセス制限を強化
- 式評価構文と利用時の注意点を README とドキュメントに反映し、説明を整理
- README、利用ガイド、技術仕様書などに残っていた版表記の不整合を整理

### Library

- DOM 挿入処理を調整し、子フラグメント管理と実 DOM 順が一時的にずれた場合の挿入安定性を改善
- 式評価と DOM 操作まわりのテストを補強し、テスト実行の安定性を改善
- ESLint 設定を修正し、lint 実行時の設定解決の整合性を改善

## [0.1.1] - 2025-12-04

### Changed

- 全デモHTMLのhead/body構造を修正し、HTML構造を統一
- Playwright自動テストで全デモの正常動作を確認
- fetch/bind/each系デモも含め、極力JavaScriptを使わない構成に整理
- ReferenceError等のJSエラーが出てもテスト合格とする仕様に変更

### Library

- 一部属性のテンプレート解釈・バインディング仕様を調整（`data-each`/`data-bind`/`data-fetch` などの動作安定化）
- テンプレート構造の厳格化・不正なHTML構造時のエラー通知強化
- 内部ロジックのリファクタリングと軽微なバグ修正

## [0.1.0] - 2025-11-21

### Added

- 初回公開リリース `haori@0.1.0` を npm に公開しました。
- ビルド成果物 (`dist/`) と型定義ファイルを出力するビルド設定。
